# PROJECT_STATE
## Active target
- `Spire Plus`
- Naming rule: all player-facing docs, UI, and tester instructions should call the mod `Spire Plus`. `EZMicroBalance` remains only as the stable technical manifest id, compatibility package folder, saved-field namespace, and legacy alias surface for this cycle.

## Current reviewed state
- Ancient Expansion v2.2 audit baseline reviewed on 2026-05-12: `a2183ee`.
- Latest package/runtime migration validation baseline before this pass was `eaaeb5a1 Implement beta86 migration validation`; the current BaseLib compatibility pass updates the local/manual-test line to beta.88. Recapture `git log -1 --oneline --decorate` and `git status --short` again before release packaging or final handoff.
- Active M5 Revision N truth, 2026-06-19: the runtime blocker is resolved for loader/patch application on `v0.107.1`; root cause was BaseLib `v3.2.1` patch drift, fixed by BaseLib `v3.3.0` with STS2-RitsuLib still at `v0.4.24`. Spire Plus is now `v0.1.0-private-beta.88`, and clean AdditiveBatch1 direct proof at `.tools/runtime-evidence/v01071-beta88-baselib330-additive-batch1-direct-cleanlog-20260619-103937/` reached main menu, applied 25/25 Spire Plus patches, registered 10 event types through 14 calls, audited clean, and passed retained packet checks. This is loader/registration proof only, not live gameplay or release readiness.
- Current installed game is `v0.107.1`. Current `v0.107.0` beta.85/beta.86/beta.87 loader proof remains previous-package/game-version context; current beta.88 AdditiveBatch1 loader proof is clean for `v0.107.1` loader/registration only. Gameplay, clicked UI, save-load, replacement, multiplayer, QA, and handoff claims still need separate evidence.
- Refresh the exact `git log -1 --oneline --decorate` HEAD again before release packaging or final handoff, because future documentation and validation passes may create newer commits.
- Superseded per-pass validation/package history was moved to `docs/archive/project-state-history-20260516.md` so this first-read file stays focused on current state.

## Game and BaseLib target
- Current dependency configurations are aligned on BaseLib `v3.3.0`, STS2-RitsuLib `v0.4.24`, and Slay the Spire 2 `v0.107.1`; RitsuLib still supplies the selected `lib\0.107.0` compatibility branch.
- Current local installed game root is `E:\Steam\steamapps\common\Slay the Spire 2` at Slay the Spire 2 `v0.107.1` (`release_info.json` commit `59260271`, date `2026-06-18T15:43:56-07:00`). Official `STS2-RitsuLib` `v0.4.24` is installed locally with `lib\0.107.0`; BaseLib `v3.3.0` is installed locally and the previous BaseLib `v3.2.1` install was backed up under `%TEMP%\codex-sts2-baselib-backup-20260619-102852`. Installed beta.88 package parity is tied to the hash table in `docs/private-beta-verification-handoff.md`, and direct beta.88 AdditiveBatch1 proof under `.tools/runtime-evidence/v01071-beta88-baselib330-additive-batch1-direct-cleanlog-20260619-103937/` is clean for `v0.107.1` loader/registration only.
## Top-level status
### Build / test / package
- Historical `dotnet build`, `dotnet publish`, default `dotnet test`, and package refresh passed locally after the `v0.106.1` / BaseLib `v3.2.1` API update. Current dirty-source validation must use the more specific Revision L notes below; split no-build test validation is the current trustworthy lane because exact one-shot runs can still destabilize around `ReleaseEvidenceGateTests`.
- Latest beta.85 runtime-fix validation has 0 build errors and 0 warnings. The isolated `ReleaseEvidenceGateTests` class passed 9 / 0 failed / 0 skipped / 9 total, and the complementary no-build test-project lane excluding `ReleaseEvidenceGateTests` passed 466 / 0 failed / 21 skipped / 487 total, for split coverage of 475 passed / 0 failed / 21 skipped / 496 total after clearing stale current-repo `testhost` locks. Earlier cross-thread `testhost` crashes are runner-contamination evidence only, not the current validation truth.
- Detailed June 17-18 validation snapshots were moved out of this first-read file; see `docs/archive/project-state-validation-snapshots-20260619.md` plus `docs/review.md`. Retained static alignment summary: StS1 no-launch current-doc claims passed 897 / 0, later superseded by the 941 / 0 no-launch current-doc guard after the v20 final-gate overlay, v20 hard-stop report, PROJECT_STATE static-summary alignment, and active current-guidance route alignment; runtime preflight passed 23 / 0, static suite passed 14 / 0, static-file hygiene passed 11 / 0, v19 gate ledger passed 531 / 0, and v19 subagent coverage passed 66 / 0 in the pushed slice. Later pause-safe static alignment passed static suite 15 / 0, current-doc claims 956 / 0 after tuple-aware enabled-mode log verifier, CanaryOnly current-pass, repo-manifest runtime-preflight drift guard alignment, beta.86 AdditiveBatch1 doc alignment, and retained-loader subagent split, static-file hygiene 11 / 0, v19 gate ledger 534 / 0, v20 final-gate overlay 29 / 0, and subagent coverage 70 / 0 without closing runtime, gameplay, QA, release, or handoff gates. Earlier June 19 no-launch migration scaffolding also recorded current-doc claims 1090 / 0 before later beta.88 AutoSlay guards superseded that count.
- June 19 current manual-test handoff scaffold follow-up: no-launch manual-test handoff scaffolding was refreshed at `.tools/runtime-evidence/manual-test-handoff-20260619-120202/` on pushed HEAD `2400ec4b` with 21 required live rows / 21 expected pending failures / 0 warnings and package ZIP hash `D547847874919EE923E2281A495D5389BAB22BBDB9F1090DC57B77033668A36D`; no game was launched. This supersedes the earlier 09:55 scaffold as the current template only; every live/manual row still needs real files before any pass claim. The prior 09:55 no-launch drift validation remains historical beta.87 context only: installed package parity passed, runtime preflight passed 27 / 0 against local `v0.107.1`, build passed with 0 warnings / 0 errors, focused no-build tests passed for multiplayer diagnostics 1 / 0 / 0, StS1 runtime packet verifier 11 / 0 / 0, Simplified Chinese localization guards 2 / 0 / 0, current-doc claims 1090 / 0, static suite 15 / 0, static-file hygiene 12 / 0, v19 ledger 534 / 0, v20 overlay 29 / 0, and v19 subagent coverage 70 / 0; it did not run publish, package refresh, runtime smoke, gameplay, QA, release, or live handoff validation.
- June 19 `v0.107.1` direct AdditiveBatch1 recapture attempt: `.tools/runtime-evidence/v01071-beta87-additive-batch1-direct-20260619-102309/` reached main menu in 33.81s, loaded Spire Plus `v0.1.0-private-beta.87`, selected RitsuLib `0.4.24` compat branch `0.107.0`, applied 25/25 Spire Plus patches, and registered the expected AdditiveBatch1 10 event types / 14 calls with exact act/shared tuple parity. It is failed current loader evidence, not a pass: `godot-log-audit.json` is dirty because BaseLib `v3.2.1` logged 2 patch failures on local Slay the Spire 2 `v0.107.1` (`Applied 241 patches successfully, 2 failed`), producing 3 BaseLib patch-failure signature hits and 2 Godot ERROR lines. The enabled-mode verifier has 2 mismatches and the packet verifier has 1 mismatch from that audit dirtiness. Restore completed with 25 mods restored and settings hashes restored. Current `v0.107.1` clean loader proof remains blocked until the BaseLib/game compatibility issue is resolved or an owner accepts a different blocker disposition.
- June 19 BaseLib `v3.3.0` beta.88 runtime unblock pass: after updating the local runtime, project package, manifest minimum, package version, installer text, and guard expectations, `dotnet build EZMicroBalance.sln -m:1 --no-incremental -p:UseSharedCompilation=false` passed with 0 warnings / 0 errors; `dotnet publish EZMicroBalance.sln -m:1` succeeded with only the known Godot ignored-folder warning; `scripts\package-spire-plus.ps1 -GameRoot 'E:\Steam\steamapps\common\Slay the Spire 2'` created and copied `publish\SpirePlus-v0.1.0-private-beta.88.zip`; and direct AdditiveBatch1 clean-log smoke under `.tools\runtime-evidence\v01071-beta88-baselib330-additive-batch1-direct-cleanlog-20260619-103937` reached main menu in 13.25s, audited clean, logged Spire Plus `v0.1.0-private-beta.88`, BaseLib `v3.3.0`, RitsuLib `0.4.24` compat branch `0.107.0`, 25/25 Spire Plus patches, and 10 event types / 14 registration calls. The enabled-mode verifier passed 31 / 0 and the runtime packet verifier had 0 mismatches. This closes the current clean-loader blocker only; gameplay, UI, save-load, replacement, co-op, QA, and tester handoff remain pending.
- Latest pause-safe direct-smoke analyzer follow-up: `analyze-spire-plus-runtime-failure.ps1` now treats a failed direct smoke evidence root with `direct-smoke-summary.json`, `godot.log.current-iteration`, and `godot-log-audit.json` as a `DirectSmoke` target; the retained `v0.107.1` failed packet now analyzes as `PackageRuntimeDrift` with 1 analyzed target, 2 package blockers, 0 harness blockers, and 0 gameplay blockers. Bound BaseLib dirty-audit signatures route to package/runtime drift, `BaseLibPatchFailures` now identifies `AdjustCustomMessageKeys::Fuckery()` undefined target-method failure, `NRelicCollectionCategory::LoadRelics` instruction matcher failure, and the 241-applied / 2-failed patch summary, and explanatory `SPIREPLUS_ALLOW_UNVERIFIED_COOP_*` text no longer creates a co-op override blocker. This is no-launch analyzer/test hardening only and does not close clean-loader, gameplay, monkey batch, QA, release, or handoff gates.
- Latest pause-safe runtime-monkey restore-transaction hardening: live-session restore now records schema-versioned `restore-state.json` with selected-process stop proof, moved/current-run preservation metadata, hash-bound preserved-current-run manifests, post-restore SlayTheSpire2/Godot process counts and id arrays, and settings-backup existence/hash closure; the monkey runner binds `session-state.json` and `restore-state.json` paths/hashes into `iteration-result.json`, fails item-count/hash/process cleanup drift, positive preserved-run counts without a manifest, and backup absence that was not explicitly restored; the runtime-monkey packet checker/analyzer/docs/tests now require retained session/restore path+hash closure, strict JSON arrays for moved lists/process ids, and analyzer state-file field/hash binding before owner routing. Pause-safe checks passed for PowerShell parser, scoped `git diff --check`, a synthetic no-launch runtime-monkey packet with 395 checks / 0 mismatches, a clean analyzer pass with 0 findings, and a targeted negative analyzer probe for missing `LiveSessionSessionStatePath`. This did not run `dotnet build`, `dotnet test`, `dotnet publish`, package/release-evidence validation, runtime smoke, gameplay, staging, commit, push, QA, or handoff validation because same-repo validation/runtime process launches remain paused.
- Latest pause-safe StS1 event beta.88 documentation alignment stayed inside the coordination boundary: `docs/features/sts1-events/README.md`, `goal.md`, `implementation-plan.md`, `event-specs/README.md`, and `docs/architecture/patch-boundaries.md` now describe beta.88 as current `v0.107.1` AdditiveBatch1 loader/registration proof only, while retained beta.85/beta.87 logs remain previous-package/game-version context. Static/no-launch checks passed with current-doc claims 1122 / 0, static suite 15 / 0, static-file hygiene 12 / 0, parser checks, `git diff --check` with CRLF normalization warning only, and batch-classifier 64 dirty entries / 0 unclassified. This did not run build, tests, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push.
- Latest pause-safe runtime-monkey command-corpus binding follow-up stayed inside the coordination boundary: `run-spire-plus-monkey-stability.ps1` now writes `command-corpus.txt` before `monkey-plan.json` and records `CommandCorpusPath` plus `CommandCorpusSha256`; `check-spire-plus-runtime-monkey-packet.ps1` requires the retained file to stay under the evidence root, match its hash, and have lines exactly matching `monkey-plan.json` `CommandCorpus`. `RuntimeMonkeyStabilityGuardTests` has a no-launch stale-command-corpus fixture, and `docs/testing/runtime-monkey-stability.md` documents the rule. Pause-safe checks passed with parser checks, current-doc claims 1164 / 0, and a synthetic no-launch packet proving matching corpus evidence passes while a tampered file fails hash/content checks. No build, test, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push was run in this follow-up.
- Latest pause-safe runtime-monkey runner binding follow-up stayed inside the coordination boundary: `run-spire-plus-monkey-stability.ps1` now writes `RunnerScriptPath` and `RunnerScriptSha256` into `monkey-plan.json`, and `check-spire-plus-runtime-monkey-packet.ps1` rejects packets whose retained runner path or SHA256 does not match the current repo runner. `RuntimeMonkeyStabilityGuardTests` has a no-launch stale-runner-hash fixture, and `docs/testing/runtime-monkey-stability.md` documents the rule. Pause-safe checks passed with parser checks and current-doc claims 1164 / 0. No build, test, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push was run in this follow-up.
- Latest pause-safe runtime-monkey patch-count binding follow-up stayed inside the coordination boundary: `check-spire-plus-runtime-monkey-packet.ps1` now requires positive `monkey-plan.json` `ExpectedPatchCount`, requires it to match `-ExpectedPatchCount` when that switch is supplied, and otherwise uses the retained plan value as the effective expected patch count for current-iteration log checks, so omitting the CLI switch cannot bypass Spire Plus patch-count binding. `RuntimeMonkeyStabilityGuardTests` has a no-launch omitted-parameter/stale-plan fixture, and `docs/testing/runtime-monkey-stability.md` documents the rule. Pause-safe checks passed with parser checks, current-doc claims 1156 / 0, static suite 15 / 0, static-file hygiene 12 / 0, `git diff --check` with CRLF warnings only, batch classifier 0 unclassified, and a synthetic no-launch packet proving the plan value drives patch-count failure when it disagrees with the retained log. No build, test, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push was run in this follow-up.
- Latest pause-safe runtime-monkey iteration coverage follow-up stayed inside the coordination boundary: `check-spire-plus-runtime-monkey-packet.ps1` now requires `monkey-plan.json` `PlannedCommands[].Iteration` and `monkey-summary.json` `Results[].Iteration` to be positive, unique, and to cover the exact resolved `1..N` iteration set, so duplicate/lost batch rows cannot make a game-native monkey packet look complete. `RuntimeMonkeyStabilityGuardTests` has a no-launch duplicate-Iteration-1/missing-Iteration-2 fixture, and `docs/testing/runtime-monkey-stability.md` documents the rule. Pause-safe checks passed with parser checks, current-doc claims 1152 / 0, static suite 15 / 0, static-file hygiene 12 / 0, `git diff --check` with CRLF warnings only, batch classifier 0 unclassified, and a synthetic no-launch packet proving the four new iteration-coverage checks fail on the forged duplicate/missing packet. No build, test, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push was run in this follow-up.
- Latest pause-safe AutoSlay AncientId coverage normalization/proof-mode/summary-count follow-up stayed inside the coordination boundary: `check-spire-plus-autoslay-packet.ps1` now uppercases expected, plan, summary, and traversed Ancient ids for target-coverage comparison while retaining exact per-run `run-result.json` versus `autoslay-summary.json` AncientId self-consistency, requires proof-mode `-ExpectedAncientIds` to be provided, to match retained `autoslay-plan.json` `ExpectedAncientIds`, to appear in retained `autoslay-summary.json`, to retain `AncientIdCounts` that exactly match `Runs[].AncientId` aggregation with positive counts for every requested Ancient id and no extra zero-count Ancient ids, to reject non-positive `-MinRuns`, and to have sidecar-plus-current-log event traversal select that same Ancient id inside the ordered event-room sequence, rejects `-AllowMissingEventTraversal` together with `-FailOnMismatch`, and active current-doc proof commands now require exact current beta.88 target values for `-MinRuns 1000`, exact `-ExpectedAncientIds VAKUU,URDA,MORVI,LOTHA`, package/game/Ritsu/patch switches, retained `-OutFile`, and no `-AllowMissingEventTraversal` bypass. `RuntimeMonkeyStabilityGuardTests` has no-launch mixed-case, proof-mode bypass, AncientIdCounts mismatch, extra zero-count AncientId, non-positive `MinRuns`, and stale-selection-before-wrong-actual-Ancient fixtures, and `docs/testing/runtime-monkey-stability.md` documents the rule. Pause-safe checks passed with parser checks and current-doc claims 1148 / 0. No build, test, publish, package/release-evidence validation, runtime smoke, game-native AutoSlay/monkey batch, gameplay, staging, commit, or push was run in this follow-up.
- Current beta.88 migration validation follow-up: after clearing a stale testhost lock, `dotnet build EZMicroBalance.sln -m:1 --no-incremental -p:UseSharedCompilation=false` passed with 0 warnings / 0 errors; split no-build runtime-harness coverage passed 81 / 0 / 0 / 81 across analyzer, packet, retained StS1 evidence, and runner/docs slices; release/source opt-in checks passed 31 / 0 / 1 / 32; Ancient behavior/UI guards passed 35 / 0 / 2 / 37; Ascension guards passed 16 / 0 / 3 / 19; boss guards passed 9 / 0 / 1 / 10; installed beta.88 package parity passed; runtime preflight passed 27 / 0; retained beta.88 AdditiveBatch1 packet verification passed 62 / 0; current-doc claims later passed 1164 / 0 after the AutoSlay proof-command exact-target, exact-Ancient-target, AncientIdCounts, extra zero-count summary-key, non-positive `MinRuns`, ordered-selection AncientId guards, runtime-monkey iteration-number coverage guards, runtime-monkey plan `ExpectedPatchCount` binding guards, runtime-monkey runner path/hash binding guards, and runtime-monkey command-corpus file/hash binding guards; static suite passed 15 / 0; and static-file hygiene passed 12 / 0. `scripts/spire-plus-live-session.ps1` now preserves session-created backup files under retained evidence instead of deleting them during restore. This remains loader/registration and no-game guard validation only; gameplay, clicked UI, save-load, replacement, co-op, QA, and release handoff remain pending.
- Release artifact checks are opt-in with `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1`. They must pass after any installed folder or game-root zip refresh. Legacy `EZMB_RUN_RELEASE_ARTIFACT_TESTS=1` still works.
- Latest package/runtime target is `v0.1.0-private-beta.88`: `publish/SpirePlus-v0.1.0-private-beta.88.zip`, `E:\Steam\steamapps\common\Slay the Spire 2\mods\EZMicroBalance`, and the game-root zip were refreshed on 2026-06-19. `.tools\publish-game-root` is tooling/isolated-publish context only, not the current package-parity source. The beta.88 AdditiveBatch1 loader smoke is clean on `v0.107.1`; beta.85/beta.86/beta.87 rows remain previous-package/game-version context. Gameplay, clicked UI, save-load, route traversal, preview-tools, Vakuu, and co-op rows remain pending.
- Historical guard context remains archived from 2026-05-24 after the Sere Talon `NRelic` fallback package refresh: focused Sere Talon/release-evidence/documentation/website guards passed. Manual feature results are pending.
- The previous Steam-client loader smoke under `.tools/runtime-evidence/release-ready-manual/fresh-current-package-loader-smoke` remains beta.13 historical startup context. The beta.17 and beta.19 packages have historical loader/startup evidence; the beta.19 Steam-client loader smoke remains historical context. Beta.86 retained `v0.107.0` direct Off, CanaryOnly, and AdditiveBatch1 loader/startup evidence; it is now previous-game-version context after the local `v0.107.1` update. Vakuu Sere Talon event-option, relic-bar, inspect-screen, hover, gameplay, save-load, and co-op rows remain pending.
- The latest script-packaged artifacts were hash-parity synced on 2026-06-19 after the beta.88 BaseLib compatibility refresh. Current package hashes are recorded in `docs/issues.md`, `docs/toreview.md`, `docs/private-beta-verification-handoff.md`, and `docs/release-checklist.md`; `docs/review.md` intentionally keeps compact source-review context instead of package hash tables.
- Current package scope includes the Spire Plus display-name refresh, A11-A20 hardening, multiplayer mismatch diagnostics, Ancient reward rebalance v4.3, Urda/Morvi/Lotha source-ready v2.2 slices, hidden-by-default Vakuu fight source slice, preview tools, Ancient player-facing text polish, Seedbed planting support for Rootblight, promoted generated/source-local art, and the `SpirePlus` archive name.
- Current manual-test package is not a release-readiness claim: live gameplay, clicked Ancient UI, save-load, route-click, death/failure-path, disable-gameplay, and co-op verification remain pending.
- Current cleanup/refactor audit is `docs/worktree-cleanup-audit.md`; top-level legacy project metadata was moved into `docs/archive/legacy-planning/legacy-project-files/`, targeted `.tools/` clutter was pruned, and the ignored website draft was deleted after preserving the `.tools/archive/local-website-preview-20260516/` snapshot. The current `publish/` package output and local game `source code/` are intentionally retained. Godot 4.5.1 mono and `.tools\publish-game-root` were restored after the latest local cleanup so publish/package validation can still run; raw local `.tools` runtime-evidence folders were pruned and historical evidence notes in docs should be treated as records, not currently present local raw artifacts.

### Runtime

- Current RitsuLib dependency prerequisite is locally installed: official `STS2-RitsuLib` `v0.4.24` is present at `E:\Steam\steamapps\common\Slay the Spire 2\mods\STS2-RitsuLib` with `lib\0.107.0\STS2-RitsuLib.dll`; BaseLib `v3.3.0` is installed under `mods\BaseLib`. Retained beta.87 `v0.107.0` direct AdditiveBatch1 proof remains previous-game-version context; current beta.88 `v0.107.1` direct AdditiveBatch1 proof at `.tools/runtime-evidence/v01071-beta88-baselib330-additive-batch1-direct-cleanlog-20260619-103937/` reached main menu, loaded BaseLib/RitsuLib/Spire Plus, applied 25/25 Spire Plus ModPatcher patches, audited clean, and passed retained verifiers with 10 event types / 14 registration calls. The earlier beta.86 Off/CanaryOnly/AdditiveBatch1 and beta.85 Off/CanaryOnly smokes remain previous-package context. Do not claim live-ready or release-ready because gameplay, UI, save-load, co-op, independent QA rerun, and tester-package handoff decisions remain pending.
- Source-fix context also exists under `.tools/runtime-evidence/v01070-current-source-getter-targets-20260610-1000/`; it helped confirm the getter-target and Ectoplasm fixes before the beta.85 package refresh.
- Current source defines 30 SavedSpireFields. Historical beta.19 startup context remains useful only as history; older 22-field and `Found 22 SavedSpireFields` loader notes remain historical. Current diagnostic loader evidence now records `Found 30 SavedSpireFields` with BaseLib, RitsuLib, and Spire Plus enabled. Full gameplay and current versioned tester-package proof remain pending.
- Headless installed-PCK Ancient resource verification passed under `.tools/runtime-evidence/current-package-smoke-20260514-015901`: Urda, Morvi, and Lotha background scenes instantiate, Ancient textures load from the installed PCK, option marker paths exist/export, and referenced EN/zhs localization keys exist. This is resource-load evidence, not clicked live Ancient UI evidence.
- BaseLib-only plug-off startup/log verification passed under `.tools/runtime-evidence/live-spire-plus-disabled-session-20260513-143020`; this proves loader isolation only. Disable-mod gameplay in an actual run remains pending.
- Refreshed Mod Settings UI list evidence for the current display-name package is under `.tools/runtime-evidence/current-spire-plus-modsettings-20260513-111342/02-mod-config-list.png`; older page-level Mod Settings UI evidence predates the display-name refresh. A fresh no-launch current-display scaffold exists at `.tools/runtime-evidence/mod-settings-current-display-20260618-223145/` for package `v0.1.0-private-beta.87` and `GitHead` `1cb58dbcbfcdb08defe50a4687023aa59d4a229d`, but it is pending scaffold evidence only; the preflight recorded Slay the Spire 2 not running, and the folder does not include screenshots, log/audit files, or gameplay proof.
- Full gameplay and manual matrix rows remain pending.

### Multiplayer

- A11-A20 selection is default-on for single-player standard lobbies. Host-multiplayer A11-A20 selection and gameplay fail closed by default after the 2026-05-25 crash logs unless `SPIREPLUS_ALLOW_UNVERIFIED_COOP_GAMEPLAY=1` is deliberately set for two-client debugging.
- `SPIREPLUS_*` names are the preferred manual-test gates for Ancient and evidence-log work. Older `EZMB_*` gates remain compatibility aliases where source already supports them.
- `SPIREPLUS_ASCENSION_DISABLE_PUBLIC_SELECTION=1` restores vanilla A1-A10 public selection for comparison.
- `SPIREPLUS_ASCENSION_DISABLE_MULTIPLAYER_SELECTION=1` disables only host-multiplayer A11-A20 selection.
- `EZMB_ASCENSION_ALLOW_PUBLIC_ASCENSION=1` is legacy-compatible and no longer required.
- Multiplayer join mismatch diagnostics log host/local version, ModelDb hash, and gameplay-relevant mod-list evidence before disconnecting.
- Full live Ascension verification is pending. Multiplayer ownership/desync and live co-op traversal matrices are still pending.

## Active feature areas

- Ancient reward rebalance v4: v4.3 active; full live Ancient reward gameplay and save/load rows remain pending.
- Ascension 11-20: implemented as gated/default-on single-player slices with co-op gameplay fail-closed by default; live verification pending.
- Rootblight polish: v2.2 source-hardened; combat-end behavior, generated-art visual proof, save/load, and co-op ownership verification remain pending.
- Urda: default-on Act 1 Ancient selection with eleven source-backed blessing test rows, including Elite Root, custom scene/icon/resource routing, disable/force gates, and source-safe deviations documented. Live gameplay, clicked UI, save/load, and co-op verification remain pending.
- Morvi: default-on Morvi source implementation with all eight v2.2 blessing ids, art/text/hover support, disable/force gates, generated-card guards, Red Ink/Debt fallback hardening, and source-safe deviations documented. Live load, gameplay, save/load, and co-op verification remain pending.
- Lotha: default-on Lotha source implementation with all eight v2.2 blessing ids, event/option art, disable/force gates, and source-safe deviations documented. Live load, gameplay, save/load, lethal-path, and co-op verification remain pending.
- Vakuu fight: hidden by default and gated with preferred `SPIREPLUS_ENABLE_VAKUU_FIGHT=1` / `SPIREPLUS_FORCE_VAKUU_FIGHT=1` controls, with old `EZMB_*` aliases still accepted; source uses a dedicated Vakuu monster, custom encounter scene, direct parent-room stack transition with parent event `Node` cleanup, no active `ParentEventId` on the child combat room, duplicate prefinished parent-restore Ancient-heal suppression, Contract hand injection, Stolen Vault locks, Blood Debt scaling, broken-lock blessing choices, and 50 Gold per broken lock. Live victory return, save/load, failure/death, clicked UI, and co-op verification remain pending.
- Preview tools: Crystal Sphere peek and deterministic transform preview now live under `EZMicroBalanceCode/Preview/` and are configured through the single Spire Plus mod settings page. They run in co-op as local UI-only previews without adding choices, rewards, or real RNG calls; live Crystal Sphere, transform-result match, save/reopen, and two-client proof remain pending.
- Ancient expansion v2.2 source docs live under `docs/features/ancient-expansion-v2.2/`; current implementation planning starts from `docs/test-ready-development-goal.md`, `docs/issues.md`, and the feature README/source docs.

## Current blockers

- Manual feature results are pending.
- Clicked Ancient UI verification still needs Urda, Morvi, Lotha, and gated Vakuu screenshots/logs showing event art, dialogue, option art, and expected choices.
- Ancient reward manual matrix and save/load-sensitive rows remain pending.
- Disable-mod gameplay in an actual run remains pending; current plug-off evidence covers only BaseLib-only startup/log loading.
- Natural A11 click-by-click traversal remains pending; visible width/row, route-click, save-load, and co-op proof are still required.
- Multiplayer co-op verification matrix remains pending.
- Live visual verification for generated Rootblight art remains pending.
- Release-note closure is blocked until manual verification evidence is complete.
- Historical `v0.106.1` RitsuLib loader-gate smoke has clean Off, CanaryOnly, and AdditiveBatch1 diagnostic proof at HEAD `8f2d79b4`. Current `v0.107.0` beta.87 AdditiveBatch1 loader proof is clean as retained previous-game-version evidence; current beta.86 direct loader proof is also clean for Off and CanaryOnly as previous-package/game-version context. The `v0.107.1` beta.87 AdditiveBatch1 direct attempt identified BaseLib `v3.2.1` patch failures; beta.88 with BaseLib `v3.3.0` now has clean `v0.107.1` loader/registration proof. Gameplay, replacement, multiplayer, and release QA claims still need their own fresh evidence. Batch 4c remains proposal-only.

## Commands that work

- `dotnet build`
- `dotnet publish`
- `dotnet test`
- `dotnet test -c Release` (optional)
- `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`
- `git diff --check`

## Next best action

- Use `docs/test-ready-development-goal.md` as the single active long-scope implementation directive. Current Codex target is a coherent user-test handoff, not release closure. The user will run live/manual Urda/Morvi/Lotha/Vakuu validation with the promoted art, including clicked UI previews, save-load checks, Vakuu failure/death-path testing, and broader manual validation; Codex should respond to concrete findings from that pass.
