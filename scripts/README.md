# Scripts

Repository helper scripts live here. Keep scripts small, idempotent where possible, and documented in this file when added.

| Script | Purpose |
| --- | --- |
| `analyze-spire-plus-runtime-failure.ps1` | No-launch triage analyzer for retained runtime monkey and future game-native AutoSlay evidence. It reads `monkey-summary.json` / per-iteration `iteration-result.json`, or `autoslay-summary.json` / per-seed `run-result.json`, plus copied `godot.log.after-launch`, sliced `godot.log.current-iteration`, `godot-log-audit.json`, AutoSlay `runtime-probe-samples.json`, and AutoSlay sidecar logs. It maps hang signals, command acknowledgement failures, trusted audit signatures, StS1 mode drift, package/API drift, restore failures, feature-specific text, missing AutoSlay launcher invocation, missing or unhealthy AutoSlay probe/observation telemetry, missing `EventKind: Ancient` / `AncientId`, missing before/after/current log binding, missing sidecar logs, and missing ordered Ancient event traversal to likely owner areas and next investigation steps. Audit signatures only route ownership after the audit `Path`/`Length`/`Sha256` bind to `godot.log.current-iteration` and an analyzer-side audit recomputation matches; GameNativeAutoSlay owner routing also requires byte-bound `godot.log.before` / `godot.log.after-launch` / `godot.log.current-iteration` slices. The JSON report includes `TriageDisposition`, harness/package/gameplay blocker buckets, and `RecommendedNextActions` so failed packets become concrete runner/package/gameplay follow-up work. Use after a failed launched monkey or AutoSlay packet; it does not prove or rerun gameplay. |
| `audit-godot-log.ps1` | Scan `godot.log` for known loader/API drift/runtime failure patterns and emit a JSON-style audit summary. |
| `audit-ancient-art-assets.ps1` | Audit `docs/features/ancient-expansion-v2.2/art-asset-manifest.json` for missing target files, SHA256 drift, temporary/final-art status, duplicate bytes, missing `export_presets.cfg` coverage, and final-generated assets that do not record `GPTimage2`. Use `-FailOnHashMismatch`, `-FailOnMissingFinal`, `-FailOnMissingExport`, or `-FailOnInvalidGenerationMode` when a pass should fail on those conditions. |
| `bootstrap-windows.ps1` | Bootstrap local Windows setup for this workspace. Use with care because local paths and installed tools vary by machine. |
| `check-installed-spire-plus-package.ps1` | Preferred Windows checker and source of truth for installed Spire Plus compatibility artifact hashes, the copied game-root `SpirePlus` ZIP, the installed PCK's Sere Talon imported textures, and the English/Simplified Chinese Sere Talon / Tanx Claws content split against the current handoff docs. Defaults are Windows-oriented; pass `-ModDirectory` or `-GameRootZipPath` explicitly for non-default paths, or `-SkipGameRootZipCheck` when checking only an installed mod folder. |
| `check-installed-spire-plus-package.sh` | Preferred macOS/Linux checker and source of truth for installed Spire Plus compatibility artifact hashes plus the installed PCK's Sere Talon imported textures and English/Simplified Chinese Sere Talon / Tanx Claws content split with `shasum -a 256` or `sha256sum`. Pass the installed compatibility mod directory as the first argument. |
| `check-installed-ezmb-package.ps1` | Compatibility alias for older local commands. It forwards to `check-installed-spire-plus-package.ps1`; new tester-facing docs should use the Spire Plus script. |
| `check-installed-ezmb-package.sh` | Compatibility alias for older local commands. It forwards to `check-installed-spire-plus-package.sh`; new tester-facing docs should use the Spire Plus script. |
| `check-local-godot-source-workspace.ps1` | No-launch checker for the local recovered Godot source workspace. It verifies `source code\project.godot`, source versus installed-game `release_info.json`, Godot 4.5.1 Mono executable presence, GDRE export-log summary, ignored/untracked source/tooling roots, installed RitsuLib manifest/variant DLL compatibility and hash, package manifest parity, recovered game-native AutoSlay source-contract signatures, and records that the RitsuLib viewer is a log viewer rather than an unpacker or monkey runner. When run with `-OutFile`, its JSON report includes `OpenProjectCommand`, `AutoSlay`, and `EvidenceUsePolicy` so future agents can open the local project deliberately while using only the owner-authorized local install, keeping original game source/resources untracked, rejecting third-party dumps, and not treating source inspection or AutoSlay signatures as runtime proof. Stale recovered source and imperfect GDRE export are warnings by default; add `-RequireCurrentSourceSnapshot` or `-RequireCleanGdreExport` with `-FailOnMismatch` only when those must block the task. |
| `check-spire-window-preflight.ps1` | Report the current foreground window, Slay the Spire 2 window state, and visible top-level windows before capturing live gameplay screenshots. Use `-RequireSpireForeground` to fail fast when another app is covering the game. |
| `check-spire-plus-autoslay-packet.ps1` | No-launch verifier for future game-native AutoSlay batch evidence folders. It checks `autoslay-plan.json`, `autoslay-summary.json`, retained `check-local-godot-source-workspace.ps1` output/SHA256/schema fields, source-workspace summary/policy flags, installed-game source parity, `GameNativeAutoSlay` runner identity, hashed launcher/mod-hook provenance for `AutoSlayer.Start(seed, logFile)`, required package/game/Ritsu target switches, `Sts1EventMode`, `-ExpectedPatchCount`, unique plan/run seed coverage, per-seed `run-result.json`, `Passed=true`, empty failure/hang arrays, `EventKind: Ancient`, `AncientId`, `RuntimeProbeSamplesPath`, clean `MainMenuObservation` / `RuntimeObservation` telemetry, retained `runtime-probe-samples.json` process/window samples, AutoSlay log paths and SHA256 hashes, `godot.log.before`, `godot.log.after-launch`, byte-sliced `godot.log.current-iteration`, current-log SHA256, `godot-log-audit.json` binding/recomputation, `sts1-mode-log-check.json` path/length/hash binding, start/completion/failure markers, exact Spire Plus patch-count markers, and ordered Ancient event-room traversal markers such as `Entering Event room`, `Detected Ancient event, clicking through dialogue`, and `Selecting event option:` in both the sidecar and current-iteration logs. Use it only after a launched AutoSlay-backed packet exists; it does not launch the game or make source-only AutoSlay checks runtime proof. |
| `check-spire-plus-runtime-monkey-packet.ps1` | No-launch verifier for launched runtime monkey evidence folders. It checks `monkey-plan.json`, `monkey-summary.json`, expected iteration folders, `iteration-result.json`, copied `godot.log.after-launch`, sliced `godot.log.current-iteration`, `godot-log-audit.json`, retained `sts1-mode-log-check.json` mode/log path/length/hash binding to the current-iteration slice, retained source-workspace report pass/mismatch/policy state and plan-summary parity, main-menu/runtime observation telemetry, no process exit, no missing main window, no missing current-iteration log slice, no hung window, no raw `Responding=false` probe samples, no pre-main-menu log-growth timeout, audit/restore/pass flags, expected package version, exact Spire Plus patch-count markers from the current-iteration slice, and probe sample/log path containment; pass `-FailOnMismatch` only for launched packets, not dry-run plans. |
| `check-sts1-enabled-mode-runtime-log.ps1` | No-launch verifier for copied StS1 `godot.log` plus `godot-log-audit.json` files; the audit path is required for enabled-mode copied-log proof and must bind `Path`, `Length`, and `Sha256` to the copied log while matching a verifier-side audit recomputation. Use `-PrintExpected` for source-derived CanaryOnly/AdditiveBatch1 expectations during static work; use `-LogPath`/`-AuditPath` only after a future smoke has produced copied logs, with `-ExpectedPackageVersion`, `-ExpectedRitsuCompatBranch`, `-ExpectedRitsuLibVersion`, and `-ExpectedGameVersion` required for enabled-mode copied logs; pass `-OutFile` to keep the verifier report in the evidence folder and `-FailOnMismatch` so evidence-command mismatches fail. It checks registration-call count and event class set; current logs do not prove Act-target tuples directly. |
| `check-sts1-runtime-preflight.ps1` | Optional no-launch StS1 runtime preflight for machines with the game installed. It reads repo `EZMicroBalance.json`, game `release_info.json`, installed `STS2-RitsuLib\mod_manifest.json`, RitsuLib `lib\<branch>\compat-target.txt`, installed `EZMicroBalance\EZMicroBalance.json`, and source-only CanaryOnly/AdditiveBatch1 expected shapes. It does not launch the game, audit a runtime log, or prove enabled-mode gameplay. |
| `check-sts1-event-asset-safety.ps1` | Static guard for StS1 asset manifest rows, ignored local extraction outputs, zero tracked original StS1 event images, and current redistribution-permission status. |
| `check-sts1-event-current-doc-claims.ps1` | Static guard for active current-facing StS1 docs, count matrix wording, beta.85 Off/CanaryOnly loader boundaries, beta.86 AdditiveBatch1 loader/registration boundaries, open gameplay rows, and references to the current static/runtime verifier scripts. |
| `check-sts1-event-feature-gates.ps1` | Static guard for StS1 default-Off behavior, safe mode routing, unsafe all-draft gating, replacement compile-symbol gating, and feature-module integration. |
| `check-sts1-event-multiplayer-shape.ps1` | Static guard for StS1 event model `IsShared` classification, combat shared-state expectations, and runtime co-op proof boundaries. |
| `check-sts1-event-parity-blockers.ps1` | Static guard for StS1 non-parity substitutes, combat blockers, N'loth RelicSelect blocker, Joust/Ssssserpent non-combat classification, and documented TODO/BLOCKED markers. |
| `check-sts1-event-registry-shape.ps1` | Static source-shape guard for the current StS1 `52 / 54 / 50 / 48 / 47 / 57 / 14` matrix, CanaryOnly identity, AdditiveBatch1 identity, and registry/model reconciliation. |
| `check-sts1-event-spec-registration-notes.ps1` | Static guard that all StS1 per-event specs contain exactly one current registration or non-registration authority note and no stale registration/API terms. |
| `check-sts1-event-static-suite.ps1` | Aggregate static StS1 suite. It wraps registry, enabled-log expected-shape, current-doc, static-file hygiene, v19 gate-ledger, v20 final-gate overlay, v19 subagent coverage, spec, feature-gate, parity, asset, multiplayer, localization source-key, and localization gap-baseline checks without launching the game or running `dotnet`. |
| `check-sts1-static-file-hygiene.ps1` | Static hygiene guard for current StS1-facing docs and helper scripts, including untracked files; checks trailing whitespace, final newline, and NUL bytes without launching the game or running `dotnet`. |
| `check-sts1-localization-gap-baseline.ps1` | Static guard for the known 33-key StS1 source-referenced localization gap, impact split, and closure-plan cue coverage; update only when the gap is intentionally closed or reclassified. |
| `check-sts1-localization-source-keys.ps1` | Static source/localization scanner for StS1 source-referenced localization keys. Use `-FailOnMissing` only after the known gap is expected to be closed. |
| `check-sts1-runtime-evidence-packet.ps1` | No-launch verifier for helper-created StS1 runtime evidence folders. It checks expected packet files, session/restore state, StS1 mode environment metadata for enabled-mode packets, expected package version, Ritsu compat branch, RitsuLib package version, game release version, settings restore hashes, allowed mod ids, retained enabled-mode log verifier JSON, and nested log/audit verifier results. Enabled-mode verification uses `godot.log.current-iteration` as the canonical log; retained current slices must byte-match `godot.log.after-launch` after the `godot.log.before` prefix, and if the slice is missing, the checker derives it only when `godot.log.before` is a byte prefix of `godot.log.after-launch`, then generates a fresh current-slice audit before invoking the nested verifier; pass `-OutFile` to keep the packet verifier report in the evidence folder and `-FailOnMismatch` so evidence-command mismatches fail. Enabled-mode packets must not use the legacy missing-session/restore switches. |
| `check-sts1-v19-gate-ledger.ps1` | Static guard for the machine-readable `docs/features/sts1-events/v19-gate-ledger.csv` O0-O76 ledger, including per-gate statuses and blocked/current-pending proof boundaries. |
| `check-sts1-v20-final-gate-overlay.ps1` | Static guard for the machine-readable `docs/features/sts1-events/v20-final-gate-overlay.csv` O76-O84 overlay, including final documentation, owner-action, no-unsupported-commit/push, release-claim, final-summary, and next-run boundaries from `docs/goals/event.md`. |
| `check-sts1-v19-subagent-coverage.ps1` | Static guard for the 15-role v20 `docs/goals/event.md` subagent coverage shape while retaining the v19 filename, their current static/runtime-blocked disposition, and the non-claim boundary for read-only explorer audits. |
| `check-github-workflow-runs.ps1` | Query GitHub Actions through the public API without requiring `gh`. Use it to check whether `Full Local Validation` has a completed successful run before closing `GOV-CI-FIRST-RUN`; pass `-RequireSuccessfulRun` when the absence of a run should fail the command. |
| `ci-full-validation.ps1` | Self-hosted Windows CI entry point for full no-game validation. Requires `STS2_PATH`/`GODOT_PATH`, writes a temporary ignored `Directory.Build.props` when needed, checks StS2 DLLs and BaseLib, then runs hygiene, build, split no-build tests by default, format, diff-check, publish, package, and split opt-in artifact tests for the single Spire Plus mod. |
| `collect-ancient-ui-evidence.ps1` | Prepare or restore a forced Ancient clicked-UI evidence session for Urda, Morvi, Lotha, or Vakuu. Prepare mode writes `ancient-ui-evidence-plan.json`, `manual-instructions.md`, `command.txt`, `environment.json`, `package-hashes.json`, and a pending `manual-rows-template.json`; it runs the preflight unless `-NoPreflight` is used, and launches only when `-Launch` is explicit. |
| `collect-coop-evidence.ps1` | Prepare pending two-client co-op evidence templates for host/client logs, A11-A20 selection, Ancient sync, Root Eyes, Rootblight, save/reconnect, and preview-tool disposition. It never proves co-op by itself and does not auto-launch a two-client session. |
| `collect-mod-settings-evidence.ps1` | Prepare the focused pending Mod Settings current-display evidence row, including `command.txt`, `environment.json`, package hashes, `window-preflight.json`, route/result notes, and a working `mod-settings-checklist.md`. Use `-NoLaunch` for templates only; after manually navigating Settings -> Mod Settings with Slay the Spire 2 in the foreground, use `-Capture List` and `-Capture Page` to retain screenshots without marking the row passed. |
| `collect-preview-tools-evidence.ps1` | Prepare live-proof templates for the integrated Spire Plus preview tools: Crystal Sphere peek and deterministic transform preview. Run without `-Launch` to create pending templates only. |
| `collect-future-peek-evidence.ps1` | Compatibility wrapper for older local commands. It forwards to `collect-preview-tools-evidence.ps1`; current work should use the preview-tools name because these tools now ship inside Spire Plus. |
| `collect-vakuu-fight-evidence.ps1` | Prepare focused pending evidence templates for the hidden Vakuu fight: dedicated scene/monster, Contract turns, locks/Blood Debt, victory return/no-black-screen, non-Vakuu rewards, failure/death, and active/prefinished save-load. Launches only with explicit `-Launch`. |
| `capture-spire-window.ps1` | Capture the visible Slay the Spire 2 window to a PNG. Use with `-RequireSpireForeground` after the preflight passes. |
| `generate-patch-inventory.ps1` | Scan active C# source for Harmony patches and refresh `docs/patch-inventory.md` with owner/risk labels. Use `-Check` to fail when the inventory is stale. |
| `invoke-ancient-art-gpt4free.ps1` | Build one Ancient art request from `art-generation-prompts.md` and `art-asset-manifest.json`, force `generation_mode`, `mode`, and `semantic_model` to `GPTimage2`, map the local g4f transport model to `gpt-image` by default, and POST it to `GPT4FREE_IMAGE_ENDPOINT`. Without an endpoint, it writes a dry-run request JSON only. |
| `package-spire-plus.ps1` | Build the player-facing `publish\SpirePlus-v...zip` from the installed compatibility artifacts. The visible package name is Spire Plus; the inner compatibility folder, manifest id, DLL, and PCK are still named `EZMicroBalance`. Run after `dotnet publish`; use `-NoRefreshFromInstalled` only to re-zip an already refreshed staging folder. The no-refresh path still requires the staged DLL, manifest, and PCK to exist. |
| `prepare-current-manual-test-handoff.ps1` | Rebuild the full no-launch manual-test handoff folder in one command. It creates release, Ancient UI, Vakuu fight, preview-tools, and co-op evidence templates, then confirms the pending release manifest fails closed unless `-SkipPendingVerifier` is used. |
| `prune-generated-sidecars.ps1` | Targeted local cleanup for regenerated Godot/editor sidecars only: `EZMicroBalanceCode/**/*.cs.uid`, `tests/**/*.cs.uid`, and `website/**/*.import`. Use `-DryRun` before pruning. It deliberately avoids broad `git clean -fdX` so `source code/`, `.tools/`, `publish/`, and local props are not touched. |
| `prune-stale-publish-packages.ps1` | Targeted local cleanup for old ignored `publish\SpirePlus-v0.1.0-private-beta.N` folders and ZIPs only. It derives the current private-beta version from `EZMicroBalance.json`, requires the current ZIP to exist first, and leaves `package-staging`, cover source, `.tools/`, `source code/`, and local props untouched. Use `-DryRun` before pruning. |
| `report-worktree-batches.ps1` | Read `git status --short` and classify dirty paths into the review batches from `docs/month-plan/commit-boundaries.md`. Use `-FailOnUnclassified` before staging or release handoff; add `-PathspecDirectory .tools\worktree-batches\current` to write per-batch pathspec files plus manifest `git add --pathspec-from-file=...` commands without staging anything. |
| `run-spire-plus-monkey-stability.ps1` | Runtime long-run stability lane. By default it writes a dry-run `monkey-plan.json` without launching the game. With explicit `-Launch`, it loops restore-safe Steam sessions through `spire-plus-live-session.ps1`, records a pre-launch `godot.log` baseline, fails closed when pre-existing `SlayTheSpire2` processes are observed because the shared log cannot be attributed to the launched process, samples current process/window/log health while waiting for an appended or reset main-menu log line, fails startup on process exit, sustained hung/not-responding samples, or pre-main-menu log-growth timeout, optionally sends one DevConsole command from a named scenario or custom corpus, records scenario/owner metadata for each planned command, supports round-robin or seeded random distribution, copies `godot.log`, runs `audit-godot-log.ps1`, verifies actual StS1 mode/package/game/Ritsu/patch-count shape through `check-sts1-enabled-mode-runtime-log.ps1`, restores state, and writes per-iteration plus summary JSON under `.tools\runtime-evidence`. |
| `send-spire-dev-console-command.ps1` | Bring Slay the Spire 2 to the foreground and send one simple DevConsole command with `SendKeys`. Use only for short ASCII commands such as `spireplus_test_ancient URDA confirm`. |
| `spire-plus-live-session.ps1` | Prepare and restore a restore-safe normal Steam live-test session with only BaseLib, STS2-RitsuLib, and Spire Plus enabled. Use `-Mode Prepare -MoveOtherMods -MoveCurrentRuns -Launch` to create evidence state and launch, or add `-DisableSpirePlus` with `-MoveOtherMods` to temporarily isolate the compatibility mod folder for BaseLib-only plug-off comparison evidence. Add `-PrepareDefaultSettings` only for direct `SlayTheSpire2.exe --force-steam off` smoke runs, because that path reads the default profile settings instead of the Steam user settings. Then run `-Mode Restore -EvidenceDir <dir> -StopGameOnRestore -PreserveNewCurrentRunsOnRestore` after copying screenshots/log notes from any session that starts or continues a run. |
| `spire-plus-package-evidence.ps1` | Shared helper for release-evidence scripts. It derives the current `publish\SpirePlus-v...` zip, versioned package artifact paths, and ZIP SHA256 from `EZMicroBalance.json` plus the current package file so evidence collectors do not duplicate the package version or hash in multiple places. Dot-source it from other scripts; do not run it directly. |
| `validate-repository-hygiene.ps1` | CI-safe repository checks for manifest identity, JSON validity, removed duplicate mod roots, ignored website clutter, and fresh patch inventory. |
| `verify-spire-plus-release-evidence.ps1` | Check a filled manual release evidence manifest before any release-ready claim. It hashes the package under test, rejects duplicate row ids, wrong row kinds, manifests/evidence dirs outside the evidence root, file/screenshot paths that escape their evidence dir, and empty evidence files, warns on unknown or blank manifest rows, keeps each row's default evidence files required even when extra files are listed, checks current package hash parity, requires `command.txt` for every passed row, clicked-UI screenshots with foreground preflight, valid non-empty PNG evidence at least 800x450 by default, clean `godot-log-audit.json` files, non-empty evidence notes that do not describe invalid/non-counting evidence, filled Mod Settings current-display, Ancient reward relic, player text QA, art/resource routing, Vakuu victory/failure/save-load, Rootblight behavior, preview-tools, co-op disposition, and A19/A20 boss-ability checklists for those rows, fresh loader, save-load, A11/disable-mod rows, or explicit owner-approved deferrals where allowed. |

## Ancient UI evidence helper

Use `collect-ancient-ui-evidence.ps1` for live-pending clicked Ancient UI evidence. It creates a timestamped `.tools\runtime-evidence\ancient-ui-click-<ancient>-<timestamp>` folder by default, records the force environment variables and expected option counts, writes the release-verifier template files, and prints the exact launch command when run without `-Launch`.

Example:

```powershell
.\scripts\collect-ancient-ui-evidence.ps1 -Mode Prepare -Ancient URDA -MoveOtherMods -MoveCurrentRuns
.\scripts\collect-ancient-ui-evidence.ps1 -Mode Prepare -Ancient VAKUU -ForceVakuuFight -MoveOtherMods -MoveCurrentRuns
.\scripts\collect-ancient-ui-evidence.ps1 -Mode Restore -EvidenceDir <evidence-dir>
```

The helper does not click the game UI, capture screenshots, audit a fresh log, or prove clicked UI by itself. Follow the generated `manual-instructions.md` and keep the clicked UI rows pending until the screenshot and log evidence exists.

Preferred main-menu UI-smoke command after launch:

```text
spireplus_test_ancient URDA confirm
spireplus_test_ancient MORVI confirm
spireplus_test_ancient LOTHA confirm
spireplus_test_ancient VAKUU confirm
spireplus_test_ancient VAKUU confirm fight
```

The Spire Plus command starts an unsaved single-player test run and refuses to run over an existing run. It is still only UI smoke; natural map routing, gameplay, save/load, and co-op rows need their own evidence.

Vakuu shows three normal options by default. Use `-ForceVakuuFight` only for the unfinished one-option fight smoke, or set `SPIREPLUS_ENABLE_VAKUU_FIGHT=1` / legacy `EZMB_ENABLE_VAKUU_FIGHT=1` deliberately when you need the fourth unfinished fight option.

Do not put downloaded binaries or generated tool output in this folder. Use ignored local folders such as `.tools/`, `publish/`, or `source code/` for machine-specific material.

## Release evidence verifier

`collect-release-evidence.ps1` writes pending manual rows with the same row ids and kinds required by `verify-spire-plus-release-evidence.ps1`, plus a verifier-readable `release-evidence-manifest.json`. Row folders include `*-checklist-template.md` references and directly editable `*-checklist.md` working files. The template is only a collection scaffold until live logs, screenshots, notes, and a verifier pass marker are added.

Use `prepare-current-manual-test-handoff.ps1` when you want the full current handoff folder for a manual test pass:

```powershell
.\scripts\prepare-current-manual-test-handoff.ps1 -EvidenceRoot .tools\runtime-evidence\manual-test-handoff-20260523-current
```

The generated handoff includes `TESTER_START_HERE.md`, `handoff-summary.json`, `release/`, `ancient-ui/`, `vakuu/`, `preview-tools/`, and `coop/`. `TESTER_START_HERE.md` repeats the package path and ZIP SHA256 from the generated manifest and prints the verifier command for the actual evidence root used in that run. `handoff-summary.json` records the expected no-launch verifier result: `PendingVerifierRequiredRowCount` is 21, `PendingVerifierFailureCount` is 21, and `PendingVerifierWarningCount` is 0. Release row folders include `*-checklist-template.md` references plus directly editable `*-checklist.md` working files. It does not launch the game and does not mark rows as passed.

Use `verify-spire-plus-release-evidence.ps1` after manual testing has produced evidence folders. You can either run `collect-release-evidence.ps1` to create the evidence folder, verifier-readable manifest, and one subfolder per required row, or use `-WriteTemplate` to create only the manifest. Fill every row with `Status`, `EvidenceDir`, screenshots, result notes, and any owner-approved deferrals:

```powershell
.\scripts\collect-release-evidence.ps1 -NoLaunch
.\scripts\verify-spire-plus-release-evidence.ps1 -WriteTemplate
.\scripts\verify-spire-plus-release-evidence.ps1 -WritePassMarker
```

By default, deferred rows fail. Use `-AllowDeferred` only when the project owner has explicitly accepted a release-note deferral and the row has `ExplicitOwnerDecision: true` plus a non-empty `ReleaseNote`. Pass rows also fail on duplicate row ids, wrong row kinds, manifests or evidence dirs outside the evidence root, required-file or screenshot paths that escape the row evidence dir, empty required files, missing `command.txt`, empty, invalid, or undersized clicked-UI PNG screenshots, empty required Markdown note files, or notes that say the evidence is invalid, covered, main-menu-only, loader-health-only, or otherwise not counted. Default evidence files cannot be removed from a row; `RequiredFiles` only adds extra files. Unknown or blank manifest rows are ignored but reported in `Warnings`.
The verifier derives the default package path from `EZMicroBalance.json` and computes that ZIP's SHA256 from the current package file; use `-PackagePath` only when intentionally auditing a different zip. `-WritePassMarker` writes `release-evidence-verifier-pass.json` only after all rows pass or are accepted deferrals.

## Full local CI lane

Use `ci-full-validation.ps1` directly on a machine with Slay the Spire 2, BaseLib, and Godot installed, or through `.github/workflows/full-local-validation.yml` on a self-hosted Windows runner.

```powershell
$env:STS2_PATH='D:\Steam\steamapps\common\Slay the Spire 2'
$env:GODOT_PATH='D:\Game\FOTN\dev-the-spire\.tools\godot-4.5.1-mono\Godot_v4.5.1-stable_mono_win64\Godot_v4.5.1-stable_mono_win64.exe'
.\scripts\ci-full-validation.ps1
```

This lane does not launch the game and does not satisfy live/manual rows. It only proves the local source, package, and artifact checks on a runner that has the required game dependencies.

Current beta.86 test strategy: `ci-full-validation.ps1` defaults to
`-TestStrategy SplitReleaseEvidence`, which runs isolated
`ReleaseEvidenceGateTests` and then the complementary test-project lane
excluding that class. Use `-TestStrategy Solution` only when deliberately
checking the legacy one-shot solution lane.

Check whether the GitHub self-hosted lane has actually run:

```powershell
.\scripts\check-github-workflow-runs.ps1
.\scripts\check-github-workflow-runs.ps1 -RequireSuccessfulRun
```

If the command reports `MatchingRunCount` as `0`, keep `GOV-CI-FIRST-RUN` open. Hosted `Repository Hygiene` or website workflow runs do not count as the full local lane.
