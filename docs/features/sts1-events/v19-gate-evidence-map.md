# StS1 Event Port v19 Gate Evidence Map

Date: 2026-06-15 (v19 opened 2026-06-11; latest pause-safe static-governance update)
Scope: `docs/goals/event.md` Mandatory Overnight Run v19, current `v0.107.0` / beta.85 truth.

This map is a status ledger, not a completion claim. Current beta.85 proof covers only default-Off loader startup and patch application. It does not prove CanaryOnly, AdditiveBatch1, event gameplay, save/load, EN/ZHS rendering, images, replacement, multiplayer, or QA.

## Current Gate Ranges

| Gate range | Current status | Current evidence | Required proof still missing |
|------------|----------------|------------------|------------------------------|
| `O0-O10` | historical beta.85 validation pass / current dirty-worktree revalidation paused | `PROJECT_STATE.md` records beta.85 build 0 errors / 0 warnings, split no-build lanes 475 / 0 / 21 / 496, opt-in artifact subset 67 / 0 / 0 / 67, package parity, and current Off loader proof | Recapture current HEAD build/test/format/package/release-evidence in one non-overlapping validation lane after the coordination pause is lifted; classify dirty worktree scope before handoff |
| `O11-O20` | static source/doc pass for current count and gate shape | `scripts/check-sts1-event-static-suite.ps1` now runs registry, enabled-log expected-shape, current-doc-claims, static-file hygiene, v19 gate-ledger, v19 subagent coverage, spec, feature-gate, parity, asset, multiplayer, localization source-key, and localization gap-baseline checks with the known localization gap non-failing | Keep rerunning the static suite after edits; run with `-FailOnLocalizationMissing` only after the 33-key localization gap is expected to be closed |
| `O21-O24` | current beta.85 default-Off loader pass | `.tools/runtime-evidence/v01070-beta85-current-package-runtime-fix-20260611-0510/` reached main menu, used RitsuLib compat `0.107.0`, applied 25/25 Spire Plus patches, logged Sts1Events default Off, and audited clean | None for default-Off loader proof unless source/package changes make the evidence stale |
| `O25-O29` | blocked / current CanaryOnly proof pending | Historical `v0.106.1` CanaryOnly proof exists only as old-loader evidence; current beta.85 Off proof is not enabled-mode proof. `scripts/check-sts1-enabled-mode-runtime-log.ps1` and `scripts/check-sts1-runtime-evidence-packet.ps1` are available to verify copied future logs/evidence packets, but no current CanaryOnly log has been captured here. | Fresh beta.85 `v0.107.0` CanaryOnly smoke proving 4 event types / 6 registration calls, then Big Fish / Golden Idol / The Lab / Divine Fountain encounter screenshots and result logs |
| `O30-O41` | mixed static/current-pending/blocked | `O30` canary code review is static-pass; `O33` AdditiveBatch1 enabled-mode smoke is current-pending; remaining rows in this range have no current gameplay proof | Canary result logs, pre/post state, save-load, EN/ZHS render, image/license/render decisions, parity-gap disposition, packet proof, gameplay-doc integration, and owner/QA acceptance |
| `O42-O52` | blocked | Simple-batch specs and source guards exist; QA remains impossible while runtime evidence is missing | Six simple-batch runtime event proofs, save-load, EN/ZHS, image proof, and independent QA/Red-Team pass/fail |
| `O53-O57` | source-guarded only / functional proof blocked | ReplacementPrototype is compile-symbol gated and unsafe-env gated; static feature-gate checks pass | Debug build with explicit unsafe env, unknown-room replacement proof, act-bucket proof, event-bag/no-repeat proof, and replacement save-load proof |
| `O58-O64` | runtime rows blocked / static classification rows pass | `O59-O63` have static multiplayer-shape, parity-blocker, asset-safety, and localization-source coverage; combat blockers and temporary substitutes are documented. `O58` multiplayer runtime proof and `O64` ZHS screenshots remain blocked. | Multiplayer fail-closed runtime proof and ZHS screenshots after enabled-mode/gameplay evidence; keep static classification/safety rows from being treated as runtime proof. |
| `O65-O76` | QA/final gates blocked or current-pending / docs in progress / invariant static | `O66-O71` are documentation-in-progress; `O76` preserves the non-completion invariant. `O65` independent QA and `O72-O75` owner/final handoff gates remain current-pending or blocked. | Independent QA/Red-Team after runtime evidence, documentation refresh from evidence paths, owner action decisions, unsupported commit/push prevention, final blocked-gate summary, and all gates green before completion. |

## Current Static Evidence

Run only static checks while the validation coordination pause is active:

```powershell
.\scripts\check-sts1-event-static-suite.ps1
.\scripts\check-sts1-event-current-doc-claims.ps1 -FailOnMismatch
.\scripts\check-sts1-v19-gate-ledger.ps1 -FailOnMismatch
.\scripts\check-sts1-enabled-mode-runtime-log.ps1 -Mode CanaryOnly -PrintExpected
.\scripts\check-sts1-enabled-mode-runtime-log.ps1 -Mode AdditiveBatch1 -PrintExpected
.\scripts\check-sts1-runtime-evidence-packet.ps1 -Mode Off -EvidenceDir ".tools\runtime-evidence\v01070-beta85-current-package-runtime-fix-20260611-0510" -ExpectedPackageVersion v0.1.0-private-beta.85 -ExpectedRitsuCompatBranch 0.107.0 -ExpectedRitsuLibVersion 0.4.16 -ExpectedGameVersion 0.107.0 -OutFile ".tools\runtime-evidence\v01070-beta85-current-package-runtime-fix-20260611-0510\runtime-evidence-packet-check.json" -FailOnMismatch
git diff --check --
```

The enabled-mode log verifier commands above are source-only expected-shape output while run with `-PrintExpected`. They do not close `O25` or `O33`; future smoke logs must be checked with `-LogPath`, `-AuditPath`, `-ExpectedPackageVersion`, `-ExpectedRitsuCompatBranch`, `-ExpectedRitsuLibVersion`, `-ExpectedGameVersion`, and `-OutFile <evidence>\enabled-mode-log-check.json`, and helper-created evidence folders should pass the packet verifier with `-OutFile <evidence>\runtime-evidence-packet-check.json`. For enabled-mode packets, the packet verifier requires `session-state.json` to record matching `Sts1EventModeEnvironment` metadata, no unsafe-mode env leakage, no `-AllowMissingSessionState` / `-AllowMissingRestoreState` bypass, and explicit package/Ritsu-compat/RitsuLib-version/game-version targets. The copied-log verifier checks registration-call count and class set; current RitsuLib logs are class-only, so Act-bucket registration tuples remain source-derived review output until future logs or gameplay evidence prove those targets directly.

2026-06-15 pause-safe rerun: the Off packet command above was run against the already-captured beta.85 evidence folder with explicit package, Ritsu compat-branch, RitsuLib package-version, and game-version targets; it returned packet `checks=34`, `mismatches=0`, with nested log verifier `checks=10`, `mismatches=0`; the retained verifier report is `.tools/runtime-evidence/v01070-beta85-current-package-runtime-fix-20260611-0510/runtime-evidence-packet-check.json`. This only re-verifies existing default-Off evidence; it does not close `O25`, `O33`, or gameplay gates.

2026-06-15 pause-safe static guard rerun: `scripts/check-sts1-event-static-suite.ps1` completed 14 static steps with 0 suite failures and kept the 33-key localization gap known/non-failing; `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` returned 715 checks / 0 mismatches in that pass, later superseded by the 872-check follow-up below; `scripts/check-sts1-v19-gate-ledger.ps1 -FailOnMismatch` returned 531 checks / 0 mismatches; `git diff --check --` exited 0 with CRLF normalization warnings only for `AGENTS.md` and `docs/goals/refactor.md`. This is no-launch/static evidence only.

2026-06-15 pause-safe subagent packet checklist hardening: `scripts/check-sts1-v19-subagent-coverage.ps1 -FailOnMismatch` returned 61 checks / 0 mismatches after adding future packet requirements for RitsuLib package-version checks, retained verifier reports, no legacy enabled-mode packet bypasses, and the class-only runtime-log limitation. The follow-up owner/final-handoff non-authorization static rerun returned static suite 14 / 0, current-doc claims 793 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and `git diff --check --` exit 0 with CRLF warnings only; this was later superseded by the direct enabled-mode audit-path guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe direct enabled-mode audit-path hardening: `scripts/check-sts1-enabled-mode-runtime-log.ps1` now requires `-AuditPath` for future CanaryOnly/AdditiveBatch1 copied-log checks, `scripts/README.md` documents the requirement, and `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` guards the script and active command examples. The follow-up static rerun returned static suite 14 / 0, current-doc claims 798 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the verifier fail-on-mismatch guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe verifier fail-on-mismatch hardening: `scripts/README.md` now states that copied-log and packet evidence commands must pass `-FailOnMismatch`, and `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` guards active command examples against dropping it. The follow-up static rerun returned static suite 14 / 0, current-doc claims 803 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the explicit target-switch guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe verifier target-switch hardening: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now rejects active copied-log and packet verifier command examples that drop `-ExpectedPackageVersion`, `-ExpectedRitsuCompatBranch`, `-ExpectedRitsuLibVersion`, or `-ExpectedGameVersion`. The follow-up static rerun returned static suite 14 / 0, current-doc claims 812 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the missing-state bypass guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe enabled-packet bypass hardening: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now rejects active CanaryOnly/AdditiveBatch1 packet verifier command examples that include `-AllowMissingSessionState` or `-AllowMissingRestoreState`. The follow-up static rerun returned static suite 14 / 0, current-doc claims 814 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the static-suite composition guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe static-suite composition hardening: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now guards the aggregate static suite's full 14-step composition, the fail-closed wrappers for guard scripts, and the suite step/failure summary output. The follow-up static rerun returned static suite 14 / 0, current-doc claims 832 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the retained audit-log command guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe retained audit-log command hardening: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now guards active `audit-godot-log.ps1` command examples so future enabled-mode smoke instructions keep `-OutFile`, `-FailOnHit`, and the retained `godot-log-audit.json` report, with a count check covering both CanaryOnly and AdditiveBatch1 smoke sections. The follow-up static rerun returned static suite 14 / 0, current-doc claims 838 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the live-session command guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe live-session command hardening: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now guards active `spire-plus-live-session.ps1` command examples so future enabled-mode smoke instructions keep explicit E-drive `-GameRoot` / `-SteamExe`, `-SteamUserId`, `-MoveOtherMods`, `-MoveCurrentRuns`, `-Launch`, `-StopGameOnRestore`, and `-PreserveNewCurrentRunsOnRestore`; count checks cover both CanaryOnly and AdditiveBatch1 smoke sections. The follow-up static rerun returned static suite 14 / 0, current-doc claims 850 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the runtime-smoke checklist prerequisite guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe runtime-smoke checklist live-session prerequisite guard: `docs/features/ritsulib-migration/runtime-smoke-checklist.md` now requires helper prepare with explicit E-drive `-GameRoot` / `-SteamExe`, `-SteamUserId`, `-Mode Prepare -MoveOtherMods -MoveCurrentRuns -Launch`, restore with `-Mode Restore -StopGameOnRestore -PreserveNewCurrentRunsOnRestore`, and keeping `STS2-RitsuLib` enabled. The follow-up static rerun returned static suite 14 / 0, current-doc claims 856 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the runtime-smoke checklist scan-scope guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe runtime-smoke checklist scan-scope guard: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now directly asserts that `docs/features/ritsulib-migration/runtime-smoke-checklist.md` is part of the broad current-claim stale scan, so its future command examples and current/historical runtime claims stay under the same no-overclaim rules as the StS1 event docs. The follow-up static rerun returned static suite 14 / 0, current-doc claims 859 / 0, static-file hygiene 7 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the runtime-smoke checklist static-file hygiene scope guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe runtime-smoke checklist static-file hygiene scope guard: `scripts/check-sts1-static-file-hygiene.ps1 -FailOnMismatch` now directly asserts that `docs/features/ritsulib-migration/runtime-smoke-checklist.md` is in static-file hygiene scope, so whitespace, final-newline, NUL-byte, and replacement-character checks cover that active runtime checklist. The follow-up static rerun returned static suite 14 / 0, current-doc claims 863 / 0, static-file hygiene 8 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the next-overnight scope guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe next-overnight runtime-plan scope guard: `scripts/check-sts1-event-current-doc-claims.ps1 -FailOnMismatch` now directly asserts that `docs/features/ritsulib-migration/next-overnight-run.md` is in broad current-claim stale-scan scope, and `scripts/check-sts1-static-file-hygiene.ps1 -FailOnMismatch` now directly asserts that it is in static-file hygiene scope. The follow-up static rerun returned static suite 14 / 0, current-doc claims 868 / 0, static-file hygiene 9 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0; this was later superseded by the RitsuLib planning-doc hygiene guard below. This is no-launch/static evidence only and does not close any runtime gate.

2026-06-15 pause-safe RitsuLib planning-doc static-file hygiene guard: `scripts/check-sts1-static-file-hygiene.ps1 -FailOnMismatch` now directly asserts that `docs/features/ritsulib-migration/monthly-dev-spec.md` and `docs/features/ritsulib-migration/batch-4c-candidates.md` are in static-file hygiene scope. The follow-up static rerun returned static suite 14 / 0, current-doc claims 872 / 0, static-file hygiene 11 / 0, v19 gate ledger 531 / 0, subagent coverage 63 / 0, and focused `git diff --check --` exit 0. This is no-launch/static evidence only and does not close any runtime gate.

`scripts/check-sts1-v19-gate-ledger.ps1 -FailOnMismatch` also guards exact gate-id membership for each status bucket, so blocked runtime gates cannot be moved into passing categories while preserving only the aggregate counts.

Current expected static-suite summary:

```text
known_localization_gap=33
localization_gap_status=known-gap-not-failing
static_suite_steps=14
static_suite_failures=0
```

## Non-Claims

- The `52 / 54 / 50 / 48 / 47 / 57 / 14` matrix is source shape, not StS1 event parity.
- Beta.85 default-Off proof does not prove CanaryOnly, AdditiveBatch1, replacement, gameplay, save-load, multiplayer, image rendering, or QA.
- Historical `v0.106.1` CanaryOnly/AdditiveBatch1 proof must not be used as current `v0.107.0` enabled-mode proof.
- The v19 hard-stop report is a pause record, not a completion claim.

For individual gate statuses, use `v19-gate-ledger.csv`; the range table above is only a compact summary.
