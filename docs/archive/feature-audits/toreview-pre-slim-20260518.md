# Spire Plus To Review

These items were implemented or researched in the 2026-05-17 test-ready loop and need user manual retest.

| ID | Result | Automated proof |
| --- | --- | --- |
| URDA-SEED-BANK-EXTRACTION-FAILURE-GUARD-20260518 | Seed Bank now refreshes the relic when stale stored card IDs settle, and preserves selected seeds if adding them to the deck fails. This prevents the relic from silently clearing stored cards on an exceptional deck-add failure. | `dotnet build EZMicroBalance.sln --no-restore`, focused Urda/player-facing/Vakuu/release guards (46 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (182 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (200 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live Seed Bank click, save-load, and Boss-transition proof remain pending. |
| MORVI-DEBT-COUNTER-ARTIFACT-SAFETY-20260518 | Debt Settlement and Red Ink Overdraft counters are now visible Buff counters, so Artifact cannot consume them or hide the amount owed. Debt payment math still uses saved Morvi state. | `dotnet build EZMicroBalance.sln --no-restore`, focused Morvi/release guard tests (31 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (182 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (200 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live Morvi gameplay/save-load/co-op proof remains pending. |
| BANNER-TEMP-STRENGTH-CLEANUP-20260518 | Vanguard and Last Stand temporary Strength now use `AscensionPowerAmountHelper.RemoveTemporaryStrength`, subtracting the recorded temporary amount directly so Artifact cannot keep that Strength permanently. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/release/player-facing guards (74 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (181 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (199 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live Banner combat timing/save-load/co-op proof remains pending. |
| ASCENSION-SIDE-TURN-STATE-SOURCE-20260518 | `RootBudCombatHook.BeforeSideTurnStart` now uses Core's hook-provided `ICombatState` directly for Banner/Boss Seal enemy-turn timing instead of reading the debug current combat state. The service entry point is now named `BeforeSideTurnStart` to match Core's hook. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/release guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (181 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (199 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live Banner/Boss Seal timing, save-load, and co-op proof remain pending. |
| VAKUU-NO-REWARD-RESUME-SPLIT-20260518 | Vakuu's no-normal-reward terminal resume and malformed-stack map fallback now live in `VakuuFightService.NoRewardResume.cs`, separate from victory option construction. | `dotnet build EZMicroBalance.sln --no-restore`, focused Vakuu/UI/release guards (63 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (181 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests (199 passed / 0 skipped) passed. Live Vakuu victory/no-black-screen/save-load/death/co-op proof remains pending. |
| VAKUU-COMBAT-HOOK-OWNERSHIP-20260518 | Vakuu fight no longer registers a run-hook model that also receives combat hooks. Contract injection, Stolen Vault setup, and damage-threshold lock breaking are handled by `VakuuFightCombatHook` only, while fight start/resume/save paths stay in Harmony patches. | `dotnet build EZMicroBalance.sln --no-restore`, focused Vakuu/Morvi/release guards (43 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (181 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live Vakuu victory/no-black-screen/save-load/death/co-op proof remains pending. |
| MEAT-CLEAVER-COOK-SPLIT-20260518 | Meat Cleaver's rest-site Cleaver option now lives in `MeatCleaverCookPatches.cs`, separate from Crossbow, Toasty Mittens, and Whispering Earring. Player text remains `Cleaver` / `切肉`, with 2-card removal and 5 HP loss unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ancient behavior / release coverage guards, full `dotnet test EZMicroBalance.sln --no-build` (181 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live rest-site proof remains pending. |
| MORVI-PAYMENT-SPLIT-20260518 | Morvi payment logic is split by responsibility: Forbidden Loan reward/settlement and borrowed-card play cost, Red Ink Overdraft card/payment flow, Debt Settlement pickup/payment flow, and shared nonlethal payment damage now live in separate partial service files. The 320 Debt, 40 Gold due, and 3 HP per 10 Gold shortfall behavior is unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Morvi/player-facing/release guards (45 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests (198 passed / 0 skipped) passed. Live Morvi gameplay/save-load/co-op proof remains pending. |
| PICKUP-REWARD-PATCH-SPLIT-20260518 | Black Star Act 3+ compensation now lives in `BlackStarCompensationPatches.cs`; temporary Sozu/Ectoplasm pickup gate overrides now live in `PickupRewardGatePatches.cs`; `PickupRewardPatches.cs` keeps pickup dispatch and direct pickup effects. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused pickup/Ancient/release guards (52 passed / 4 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, and `dotnet publish EZMicroBalance.sln --no-restore` passed. Live pickup-flow proof remains pending. |
| URDA-ROOT-EYES-RESERVATION-20260518 | Root Eyes now keeps future marked enemy groups/events from being consumed by earlier non-preview rooms when another valid queue result is available. English and Simplified Chinese titles now consistently use `Root Eyes` / `根眼`. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Eyes/player-facing/release guards (49 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, and `dotnet publish EZMicroBalance.sln --no-restore` passed. Live Root Eyes hover/click/entry/save-load proof remains pending. |
| PRISMATIC-GEM-REPLACEMENT-SPLIT-20260518 | Prismatic Gem reward-screen counter and trigger flow stay in `PrismaticGemPatches.cs`; off-color pool selection, all-slot replacement, failed-replacement cleanup, and upgrade preservation now live in `PrismaticGemReplacementPatches.cs`. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Prismatic/Ancient/release guards (61 passed / 11 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Live reward-screen/hover visual proof remains pending. |
| ASCENSION-BOSS-SEAL-SOUL-TIDE-SPLIT-20260518 | Soul Tide Soul Fysh Intangible tracking, Beckon hand-pressure settlement, and pending Block application now live in `AscensionCombatModifierService.BossSeals.SoulTide.cs`; `AscensionCombatModifierService.BossSeals.MonsterWindows.cs` keeps Holy Daze, Martyr Oath, Ink Return, and Startled Shell. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage/ReleaseSafety guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Live Boss Seal gameplay/save-load/co-op proof remains pending. |
| ASCENSION-BOSS-SEAL-BOILING-CRITICAL-SPLIT-20260518 | Boiling Critical Steam milestone tracking and Waterfall Giant explosion Block telegraph now live in `AscensionCombatModifierService.BossSeals.BoilingCritical.cs`; `AscensionCombatModifierService.BossSeals.MonsterWindows.cs` keeps Holy Daze, Martyr Oath, Ink Return, Startled Shell, and Soul Tide. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage/ReleaseSafety guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Live Boss Seal gameplay/save-load/co-op proof remains pending. |
| ASCENSION-BOSS-SEAL-CHOSEN-DECREE-SPLIT-20260518 | Chosen Decree Bound-card marking, play tracking, obey/miss settlement, Royal Decree cleanup, and Queen/Amalgam payoff now live in `AscensionCombatModifierService.BossSeals.ChosenDecree.cs`; `AscensionCombatModifierService.BossSeals.CardPressure.cs` keeps Misaligned Shell, Marginal Note, and Struggle Bait pressure. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage/ReleaseSafety guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Live Boss Seal gameplay/save-load/co-op proof remains pending. |
| LOTHA-CARD-RULES-SPLIT-20260518 | Removed the misleading `LothaRunHook.cs` file. Lotha hook wrappers stay in `LothaHooks.cs`; card-play, `ShouldPlay`, cost, shared eligibility, and Power replacement rules now live in `LothaBlessingService.CardRules.cs`; combat/turn state reset lives beside lifecycle flow in `LothaBlessingService.CombatLifecycle.cs`. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Lotha/Vakuu save-risk/ReleaseCoverage/high-risk guards (52 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no blocker; old active notes below are now superseded by this row. Live gameplay/save-load/co-op proof remains pending. |
| ASCENSION-REWARD-FISSION-SPLIT-20260518 | A13 Fission reward chance, eligibility, mutation, and diagnostics now live in `AscensionRewardService.Fission.cs`; `AscensionRewardService.cs` keeps card reward option entry flow, Firemarked Elite/Boss Seal reward additions, A20 first-boss terminal reward, and A17 enhanced treasure payout. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage/ReleaseSafety guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no blocker. Live reward-screen/gameplay/save-load/co-op proof remains pending. |
| ROOTBUD-COMBAT-LIFECYCLE-SPLIT-20260518 | Blight Sprout combat-start seeding and combat-end Rootblight growth now live in `RootBudCombatHook.Lifecycle.cs`; immediate hook dispatch stays in `RootBudCombatHook.cs`, and shared room/pile/sprout helpers stay in `RootBudCombatHook.Helpers.cs`. Existing Seedbed combat-end behavior is now explicitly guarded. | `dotnet build EZMicroBalance.sln --no-restore`, focused RootBud/Ascension/ReleaseCoverage guards (43 passed / 4 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no partial/manifest blocker and prompted the Seedbed guard. Live Blight Sprout timing, Rootblight combat-end behavior, save-load, and co-op proof remain pending. |
| ASCENSION-A17-DEEP-BRANCH-PLANNING-SPLIT-20260518 | A17 Deep Branch plan creation, existing-branch matching, branch-column search, and route-safety helpers now live in `AscensionMapService.DeepBranches.Planning.cs`; insertion and metadata marking stay in `AscensionMapService.DeepBranches.cs`. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage/ReleaseSafety guards (58 passed / 10 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no blocker. Live A17 route generation/click/save-load/co-op proof remains pending. |
| ASCENSION-A11-GEOMETRY-DIAGNOSTICS-SPLIT-20260518 | A11 target-shape diagnostics, proof helpers, and logging now live in `AscensionMapService.A11GeometryDiagnostics.cs`; map mutation stays in `AscensionMapService.A11Geometry.cs`. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused A11/Ascension/ReleaseCoverage guards (39 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no blocker; an unused private diagnostic overload was removed. Live A11 traversal/save-load/co-op proof remains pending. |
| URDA-ROOT-SIGHT-UNKNOWN-SPLIT-20260518 | Root Eyes Unknown-room preview and entry-commit helpers now live in `UrdaBlessingService.RootSightUnknown.cs`; concrete encounter/event preview stays in `UrdaBlessingService.RootSightPreviewGeneration.cs`. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Sight/ReleaseCoverage guards (33 passed / 3 skipped), full `dotnet test EZMicroBalance.sln --no-build` (180 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. Independent read-only review found no blocker; suggested cleanup and wider `.Roll(` guard were applied. Live Root Eyes hover/click/save-load proof remains pending. |
| WAITING-TESTS-QUEUE-SLIM-20260518 | The active manual evidence queue is now a compact table, with the full historical detail archived at `docs/archive/issues/waiting-tests-pre-slim-20260518.md`. | Active UTF-8/mojibake scans passed; `dotnet build EZMicroBalance.sln --no-restore`, normal tests (180 passed / 18 skipped), format, and diff-check passed. Live gameplay proof remains pending. |
| ANCIENTS-V4-SOURCE-DESIGN-SLIM-20260518 | The active Ancients v4 source-design file is now a compact readable v4.3 behavior summary. The mojibake-corrupted long draft was archived for traceability and is no longer current source truth. | Active UTF-8/mojibake scans passed; `dotnet build EZMicroBalance.sln --no-restore`, normal tests (180 passed / 18 skipped), format, and diff-check passed. Live gameplay proof remains pending. |
| URDA-ROOT-SIGHT-EXHAUSTED-EVENT-20260518 | Root Eyes event previews now accept repeated events at entry only when the active room set has exhausted all unique allowed events, matching Core's late-act event-repeat rule. | `dotnet build EZMicroBalance.sln --no-restore` and focused Root Sight/player-facing high-risk guard tests passed. Full package validation passed: normal tests 180 passed / 18 skipped, artifact tests 198 passed / 0 skipped, and installed-package hash check passed. Live Root Eyes hover/click/save-load proof remains pending. |
| ASCENSION-INLINE-ZHS-POWERLOC-20260518 | Banner, Firemark, and Boss Seal inline Simplified Chinese `PowerLoc` hover strings were repaired in C# source. Power hover text now uses readable v3.2 terms instead of mojibake. | `dotnet build EZMicroBalance.sln --no-restore` and focused player-facing/Ascension guard tests passed. Full package validation passed: normal tests 180 passed / 18 skipped, artifact tests 198 passed / 0 skipped, and installed-package hash check passed. Live hover visual proof remains pending. |
| ANCIENT-HOOK-DISPATCH-CLEANUP-20260518 | Morvi, Lotha, and Urda RunHooks no longer duplicate combat-only overrides that Core dispatches through CombatHooks. The split keeps RunHooks on run lifecycle/reward/death paths and CombatHooks on card/turn/cost/draw/power combat paths. | `dotnet build EZMicroBalance.sln --no-restore` and focused Morvi/Lotha/Urda high-risk guard tests passed, including a new ownership guard. Full package validation passed: normal tests 180 passed / 18 skipped, artifact tests 198 passed / 0 skipped, and installed-package hash check passed. Live gameplay/save-load proof remains pending. |
| URDA-ROOT-SIGHT-EVENT-QUEUE-FIX-20260518 | Root Eyes event previews now swap the selected event into the current Core event queue position before Core advances event visits. This matches the existing encounter-preview queue swap and avoids skewing later event order when the preview picked a later allowed event. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Sight/high-risk/release guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Independent read-only review found no blocker. Live Root Eyes hover/click/save-load proof remains pending. |
| PRISMATIC-GEM-HOVER-SPLIT-20260518 | Prismatic Gem relic hover count moved into `PrismaticGemHoverPatches.cs`; `PrismaticGemPatches.cs` now keeps only pool/counter/reward replacement, while `PrismaticGemRewardScreenHintPatch.cs` keeps reward-screen banner text. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Prismatic/release guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Live reward-screen/hover visual proof remains pending. |
| VAKUU-COMBAT-STATE-SPLIT-20260518 | Vakuu contract signing, Stolen Vault power maintenance, damage-threshold lock breaking, Blood Debt application, and Vakuu creature lookup now live in `VakuuFightService.CombatState.cs`; `VakuuFightPatch.cs` keeps fight option, start, resume, save, and parent-event restore patches. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Vakuu/UI/ReleaseCoverage guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Live Vakuu victory return/no-black-screen/save-load/death/co-op proof remains pending. |
| BANNER-PRESSING-LINE-SPLIT-20260518 | Pressing Line's card-count tracking, turn-end Block/strike settlement, and constants now live in `AscensionCombatModifierService.Banners.PressingLine.cs`; the shared Banner file keeps Vanguard, Shieldwall, Blood Prize, Last Stand, target selection, and fallback. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Live Banner combat timing/save-load/co-op proof remains pending. |
| ROOTDECK-PENDING-DOWNGRADE-SPLIT-20260518 | Rootblight pending combat downgrade queue serialization now lives in `RootDeckService.PendingDowngrades.cs`; `RootDeckService.cs` keeps public flow, combat-end growth, and diagnostic level state. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Live Rootblight gameplay/save-load/co-op proof remains pending. |
| ROOTDECK-DECKCARDS-SPLIT-20260518 | Rootblight deck-card lookup, add/replace, card creation, enumeration, internal removal marker table, and cap trimming now live in `RootDeckService.DeckCards.cs`; `RootDeckService.cs` keeps public flow, combat-end growth, pending downgrade serialization, and diagnostic level state. Player-visible behavior is intended to be unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests, and `scripts/check-installed-ezmb-package.ps1` passed. Live Rootblight gameplay/save-load/co-op proof remains pending. |
| LOTHA-COMBAT-LIFECYCLE-SPLIT-20260518 | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Lotha combat lifecycle entry flow moved into `LothaBlessingService.CombatLifecycle.cs`; the remaining card/cost/reset responsibilities have since moved out of the former `LothaRunHook.cs`. Player-visible behavior is intended to be unchanged. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. Live gameplay/save-load/co-op proof remains pending. |
| ASCENSION-V32-SOURCE-DESIGN-SYNC | Active A11-A20 source design now matches v3.2 Firemark and Banner values: Firemarks use +1/+2/+4, +20/30/45% HP, 5/10/20 armor, 4/8/16 heal, and current counterplay windows; Banners list all five current banner kinds and their Act-scaled values. | Focused Ascension/release/player-facing guard tests, full tests (178 passed / 18 skipped), opt-in artifact tests (196 passed / 0 skipped), and `git diff --check` passed. Live combat timing remains pending. |
| URDA-ROOT-SIGHT-RNG-SOURCE-AUDIT | Root Eyes preview dice now derive from `runState.Rng.UnknownMapPoint` with a forked `Rng`, so previewing a future room still does not consume the live Unknown-room roll, but the stored preview is tied to the same Core RNG stream that owns Unknown room outcomes. The same pass cleaned active-doc mojibake reintroduced during hash refresh. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Sight/player-facing/release guard tests, full `dotnet test EZMicroBalance.sln --no-build` (178 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (196 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live hover/click/save-load/co-op proof remains pending. |
| PRISMATIC-GEM-HINT-SPLIT | Prismatic Gem reward-screen banner hint logic moved from `PrismaticGemPatches.cs` into `PrismaticGemRewardScreenHintPatch.cs`. The core file now stays focused on pool/counter/reward replacement and hover count; the hint file owns `_banner` reflection, `UI/Banner` fallback, one-time diagnostics, and banner text application. | Included in the same final validation chain as `URDA-ROOT-SIGHT-RNG-SOURCE-AUDIT`; live reward-screen visual proof remains pending. |
| ROOTBLIGHT-PENDING-DOWNGRADE-SAVE-GUARD | Playing Rootblight now queues pending combat downgrades in a save-backed player field instead of a weak in-memory table. Mid-combat save/load has a source path to preserve queued Rootblight II/III downgrades until combat end. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/Ancient/release guard tests, full `dotnet test EZMicroBalance.sln --no-build` (178 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests (196 passed / 0 skipped) passed. Live save/load proof remains pending. |
| ROOTBUD-SPROUT-ROUND-DETERMINISM | Existing boss Blight Sprouts are normalized deterministically by scan order to round 3 then round 4; reversed or duplicate saved round values are corrected before missing buds are added. | Guard tests now reject the older preserve-any-valid-round helper shape. Same validation as `ROOTBLIGHT-PENDING-DOWNGRADE-SAVE-GUARD`; live boss save/load proof remains pending. |
| ROOTBLIGHT-III-SPLIT-CAP-SEMANTIC | Rootblight III consumes its hidden one-time split marker only after a Rootblight I is actually added. If the four-card cap blocks that add, the marker stays available for a later successful split; docs and guards now state this rule. | Focused Ascension guard tests and full validation passed. Manual combat-end behavior still needs in-game proof. |
| PRISMATIC-GEM-HOOK-ORDER-CLEANUP | Prismatic Gem replacement now runs after early reward modifiers and before late reward modifiers. Early added slots such as LastingCandy are included; late modifiers can apply to the final off-color cards. Replacement prioritizes same type plus rarity, cleans failed temporary replacements, and restores the counter if no full replacement set can be built. | Focused Ancient high-risk/player-facing/release guard tests, full tests, publish/package, and artifact tests passed. Live reward-screen visual proof remains pending. |
| DOC-ACTIVE-MOJIBAKE-GUARD | Active current docs and player-facing text notes were cleaned of mojibake found during review; `AncientPlayerFacingPolishGuardTests` now scans non-archive active docs for known mojibake fragments. | Focused player-facing guard tests and full validation passed. |
| V32-WITHERED-HUSK-SKILL | Withered Husk / 鏋３ is now a playable 0-cost temporary Skill that grants 3 Block, exhausts, and removes after combat, matching v3.2. It is no longer an unplayable/ethereal status-style placeholder. | Ancient player-facing and source guard coverage passed in the current validation chain. |
| ASCENSION-MAP-MARKER-HELPER-SPLIT | Ascension map metadata creation, quest-marker attachment, stable marker ordering/hashing, Firemark optional-route selection, route adjacency checks, rest-site row helpers, and path-avoidance traversal were moved from `AscensionMapService.Markers.cs` into `AscensionMapService.MarkerHelpers.cs`. Marker assignment behavior is unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/source-manifest/release-safety guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live route-click/save-load/co-op proof remains pending. |
| ASCENSION-A11-SERIALIZABLE-HELPER-SPLIT | A11 serializable-map lookup, graph conversion, path traversal, coordinate shifting, bridge-point creation, and child-edge helpers were moved from `AscensionMapService.A11Geometry.cs` into `AscensionMapService.A11SerializableHelpers.cs`. A11 geometry behavior and public map hook entry points are unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, focused A11/Ascension/source-manifest guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live route-click/save-load/co-op proof remains pending. |
| ROOTBUD-COMBAT-HOOK-HELPER-SPLIT | RootBud combat helpers were moved from `RootBudCombatHook.cs` into `RootBudCombatHook.Helpers.cs`. Combat seeding, pile/draw/play/death hooks, sprout timing, and combat-end Rootblight resolution remain behavior-preserving source paths. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension RootBud/source-manifest guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live gameplay/save-load/co-op proof remains pending. |
| URDA-ROOT-EYES-FUTURE-QUEUE-COMMIT | Root Eyes can select future reachable Monster, Unknown, or Elite rooms. Normal/elite previews still read from the generated Act `RoomSet` with local preview RNG, and entering the marked room now swaps the selected encounter into the current Core encounter queue before Core marks the room visited. Trial Branch inline zhs text and the Ancient manual verification matrix were also cleaned of mojibake found during the same strict pass. | `dotnet build EZMicroBalance.sln --no-restore`, full `dotnet test EZMicroBalance.sln --no-build` (177 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and `EZMB_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build` (195 passed / 0 skipped) passed. Live map click/hover/save-load proof remains pending. |
| URDA-ROOT-SIGHT-ENCOUNTER-PEEK-HARDENING | Root Eyes normal/elite room previews now choose a concrete enemy group from the already-generated Act `RoomSet` instead of calling `Act.PullNextEncounter(...)` during preview creation. This keeps the map-preview path aligned with the Future Peek rule: preview code reads hidden information and does not use APIs that may advance real room state. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Sight/high-risk/player-facing/release guard tests (23 passed), full `dotnet test EZMicroBalance.sln --no-build` (177 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live hover/click/save-load proof remains pending. |
| URDA-ROOT-EYES-SELECTABLE-MAP-PREVIEW | Root Eyes now visibly marks future reachable Monster, Unknown, or Elite map nodes while selection is active, stores the chosen node's concrete enemy group or event, refreshes the map immediately, and cancels selection when the map closes. Stale current docs that still claimed auto-marking/no map control were corrected. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live hover/click/save-load proof remains pending. |
| URDA-ROOT-SIGHT-MAP-MARKER-RESTORE | Root Eyes preview markers are now restored from saved `RootSightPreviewRecords` whenever the map is generated or loaded. Previewed Monster, Unknown, and Elite nodes keep their visible Root Eyes marker after map rebuild, while mismatched stale coordinates are ignored. | `dotnet build EZMicroBalance.sln --no-restore`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live map hover/click and save-load proof remain pending. |
| MORVI-BLUEPRINT-LIBRARY-SPLIT | Blueprint Proof and Overdue Library behavior were moved out of `MorviRunHook.cs` into dedicated partial service files. The split keeps Morvi's hook entry points, combat state fields, progress serialization, generated-card cleanup, and player-visible behavior unchanged. | `dotnet build EZMicroBalance.sln --no-restore`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. |
| MORVI-OPENBOOK-PAPERSTORM-SPLIT | Open-Book Exam seal/return flow moved into `MorviBlessingService.OpenBook.cs`, and Paperstorm waste-paper/status conversion flow moved into `MorviBlessingService.Paperstorm.cs`. `MorviRunHook.cs` is reduced to shared hook flow, Misprint Press, and temporary-card cleanup. | `dotnet build EZMicroBalance.sln --no-restore`, focused Morvi/source guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. |
| MORVI-MISPRINT-TEMPORARY-SPLIT | Misprint Press replay/draw handling moved into `MorviBlessingService.MisprintPress.cs`, and Morvi temporary-card cleanup moved into `MorviBlessingService.TemporaryCards.cs`. `MorviRunHook.cs` now keeps shared combat flow and generic helper dispatch. | `dotnet build EZMicroBalance.sln --no-restore`, focused Morvi/source guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. |
| LOTHA-SINGLE-SENTENCE-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Single Sentence display/fallback/tracking lives in `LothaBlessingService.SingleSentence.cs`, and shared card-rule dispatch now lives in `LothaBlessingService.CardRules.cs`. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| LOTHA-MIRROR-REBUTTAL-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Mirror Rebuttal deck-card eligibility, selected-card marker writes, marker cleanup, combat-start hand pull, and selected-card lookup live in `LothaBlessingService.MirrorRebuttal.cs`; shared card-rule dispatch now lives in `LothaBlessingService.CardRules.cs`. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| LOTHA-ECHO-VERDICT-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Mirror Hall Echo and Deferred Verdict behavior live in dedicated partial service files; shared Power replacement and card-rule helpers now live in `LothaBlessingService.CardRules.cs`. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| LOTHA-PRESUMPTION-CLOSED-COURT-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Presumption/Innocent handling lives in `LothaBlessingService.Presumption.cs`, Closed Court behavior lives in `LothaBlessingService.ClosedCourt.cs`, and shared card/cost rules now live in `LothaBlessingService.CardRules.cs`. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| MORVI-HOOK-STATE-SPLIT | Morvi run/combat hook wrappers were moved into `MorviHooks.cs`, and selected-blessing state, combat state, deck sync, and progress serialization were moved into `MorviBlessingService.State.cs`. `MorviRunHook.cs` now focuses on combat flow, archive pages, Blueprint Proof, temporary cards, and shared command helpers. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed in this pass. |
| URDA-ROOT-SIGHT-MAP-UI-PEEK | Root Eyes now opens the map when clicked, accepts reachable Monster/Unknown/Elite nodes even when the normal map point is disabled, refreshes map visuals after selection, and changes previewed Unknown nodes to the stored Monster/Elite/Event-style map icon. A pure Root Eyes marker also uses the Root Eyes icon, while hover keeps the concrete encounter or event preview text. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed in this pass. Live hover/click proof remains pending. |
| DOC-DEVENV-22FIELD-STALE | `docs/dev-environment.md` now marks the 22-field Steam loader evidence as historical same-manifest evidence and says the current 25-field package still needs a fresh live loader rerun. `ReleaseSafetyExpandedGuardTests` now rejects the stale phrase shapes that described 22-field evidence as current/current-package runtime parity. | `dotnet build`, `dotnet test --no-build`, `dotnet format --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish`, and opt-in artifact tests passed in this loop. |
| DOC-CURRENT-22FIELD-STALE | Current-facing docs now treat the 22-field Steam/package smoke as historical evidence, not current 25-field loader parity. Updated `private-beta-release-completion-audit`, `private-beta-verification-handoff`, `test-ready-completion-audit`, A11-A20 checklist/API research, and Ancient completion audit wording; `ReleaseSafetyExpandedGuardTests` now scans current-facing docs for 22-field smoke lines that sound current without a historical/pending boundary. | `dotnet build`, `dotnet test --no-build`, `dotnet format --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish`, and opt-in artifact tests passed in this loop. |
| ANCIENT-COMBAT-HOOK-REGISTRATION | Urda, Morvi, Lotha, and gated Vakuu now register combat-state hooks in addition to run-state hooks. This is the source fix for effects that are called through `combatState.IterateHookListeners()`: card play counts, `ShouldPlay`, turn-start combat actions, cost previews, power amount hooks, Trial Branch play tracking, and Vakuu Contract/Stolen Vault combat behavior. | `dotnet build` passed; `dotnet test --no-build` passed with 172 passed / 18 skipped. |
| URDA-TRIAL-BRANCH-RARE-3OF3 | Trial Branch now offers 4 rare cards, upgrades the chosen card, adds it to the deck, applies a visible `Trial Branch` enchantment, and requires the card to be played in each of the next 3 combats. Missing any one of those combats removes the card. The enchantment text shows combats left, current-combat play status, and remaining required plays. | Guard coverage updated in `ReleaseCoverageGuardTests`; `dotnet build` and `dotnet test --no-build` passed. Live gameplay/save-load proof remains pending. |
| LOTHA-SINGLE-SENTENCE-LIVE-PROMPT | Single Sentence now has a visible `Single Sentence` combat Power/counter. It starts at 5 while the Attack/Skill ruling is ready, switches to 4 after the ruling, counts down remaining normal plays, and drops to 0 when further play is blocked. | Guard coverage updated in `LothaPolishGuardTests`; `dotnet build` and `dotnet test --no-build` passed. Live gameplay proof remains pending. |
| URDA-HUMUS-PACT-DETAILED-TEXT | Humus Pact option/relic text now explicitly says it affects Act 1 normal combat card rewards, adds a `Compost Reward` button, skips those reward cards, grants 15 Gold, and pays off on the third compost with up to 2 removals plus 1 upgraded reward card. | `AncientPlayerFacingPolishGuardTests` updated; `dotnet test --no-build` passed. |
| V32-SEEDBED-LOCALIZATION-P0 | Seedbed localization P0 from `docs/review.md` is closed by the main agent. Seedbed description now uses Capacity/UpgradeLine instead of localized prose inside formatter bodies, and the unused immediate-text path was removed or made non-blocking. | Main-agent rerun: `dotnet build` passed; `dotnet test EZMicroBalance.sln --no-build` passed with 171 passed / 18 skipped. |
| V32-SEEDBED-SAVELOAD | Seedbed combat remaining slots now persist in Urda's saved progress string as `SeedbedCombatSlots`, restore into the hook state during combat, update after each planted card, and clear on combat entry/victory. | Source guard added in `ReleaseCoverageGuardTests`; `dotnet build` and `dotnet test EZMicroBalance.sln --no-build` passed. Live save/load remains pending. |
| V32-BANNER-SINGLE-ENEMY-NOOP | Shieldwall and Last Stand no longer no-op in single-primary-enemy combats. At combat start they convert to Blood Prize, and current node metadata is updated so combat/reward source uses the same banner. | Source guard added in `AscensionFeatureGuardTests`; `dotnet build` and `dotnet test EZMicroBalance.sln --no-build` passed. |
| V32-A12-A16-CHECKLIST-STALE | A12/A16 manual checklist rows now use v3.2 Firemark and Banner values, counterplay windows, five banner kinds, and the single-enemy fallback rule. | `dotnet test EZMicroBalance.sln --no-build` passed. |
| MORVI-OVERDUE-LIBRARY-CRASH | Archive Draw Page now uses `CardsVar`; Strength/Dexterity archive pages use matching variables; temporary archive powers use valid Morvi icons instead of NOPE. | `dotnet test EZMicroBalance.sln --no-build` passed in the Morvi worker pass. |
| MORVI-BLUEPRINT-PROOF-NO-EFFECT | Blueprint Proof now initializes at combat start and has a late initialization guard before the first eligible play if the combat-start hook was missed. | Morvi guard tests passed in the worker pass. |
| MORVI-MISPRINTED-PRESS-TEXT | Misprint Press text now says the first manual natural-deck Attack/Skill each turn repeats once, and draws only if that triggering card had base cost 1 or more. | Localization guard updated. |
| MORVI-DEBT-WORDING | Debt text now states 220 Gold gained, 320 Debt recorded, 40 Gold due after each combat, and 3 nonlethal HP lost per missing 10 Gold. | Localization guard updated. |
| MORVI-FORBIDDEN-LOAN-TEXT | Forbidden Loan starts from the card reward: choose and add 1 upgraded Ancient card, then explains the later 180 Gold settlement. | Localization guard updated. |
| URDA-MOSS-MAP-TEXT | Moss Map text now maps all five first Act 1 room rewards: Monster +25 Gold; Event heal 5; Shop Potion; Elite random upgrade; Rest Site +3 Max HP. | Localization guard updated. |
| URDA-SEED-BANK-INTERACTIVE-RELIC | Seed Bank no longer opens a Boss-entry picker. Stored cards appear on relic hover, the relic shows a counter, and clicking it lets the player choose up to 2 stored cards; the first selected card is upgraded, then the relic is used up. | Build and focused Urda coverage guard passed. |
| URDA-ROOT-EYES-PREVIEW-REDESIGN | Root Eyes no longer auto-marks rooms. Clicking the relic enters map selection; future reachable Monster, Unknown, or Elite nodes are valid. The chosen node stores a concrete encounter/event preview, shows it on hover, and enters that result. Unknown nodes exclude Shop, Treasure, Rest Site, and Boss. | `AncientPlayerFacingPolishGuardTests` and `ReleaseCoverageGuardTests` focused pass: 37 passed, 3 skipped. |
| URDA-ROOT-SIGHT-HOVER-CLARITY | Root Eyes option and relic text now tells the player to hover the marked room. Source guards require hover titles to resolve concrete encounter/event names from `ModelDb`, and localization guards require the hover instruction in English and Simplified Chinese. | `dotnet build EZMicroBalance.sln --no-restore`, focused Root Sight/high-risk/player-facing/release guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. |
| URDA-ROOTED-ROUTE-MP-RESEARCH | Source research found Rooted Route can stay local-owned for now, but richer Root Eyes writeback must be host-authoritative and party-visible in co-op. | Review note in `docs/review.md`; no co-op implementation claimed. |
| 2026-05-17-SOURCE-REVIEW-NO-NEW-BLOCKERS | Source review found no new Urda/Morvi P0/P1/P2 implementation blockers. Live gameplay, clicked UI, save-load, death/failure path, and co-op remain pending manual proof gates rather than source blockers. | `docs/review.md` conclusion; source-only review, no game run. |
| URDA-ROOT-EYES-SOURCE-REVIEW | Root Eyes source path is confirmed: relic click enters map selection, map clicks are intercepted only while selection is active, only reachable Act 1 Monster/Unknown/Elite nodes are valid, disallowed Unknown outcomes exclude Shop/Treasure/Rest Site/Boss, and the chosen concrete preview is saved for room entry. | Reviewed in `docs/review.md`; no live UI claim. |
| URDA-SEED-BANK-SOURCE-REVIEW | Seed Bank source path is confirmed: Boss-entry settlement is removed, relic hover/count expose stored cards, clicking the relic opens extraction only when cards are stored, up to 2 cards can be selected, the first is upgraded, storage is cleared, and the relic is marked settled. | Reviewed in `docs/review.md`; no live gameplay claim. |
| MORVI-SOURCE-REVIEW | Morvi source paths are confirmed for Overdue Library variables/icons, Blueprint Proof combat-start and late guards, Misprint Press play-count behavior without hand-copy generation, Forbidden Loan reward/debt flow, and Debt Settlement 220/320/40/3 behavior and text coverage. | Reviewed in `docs/review.md`; no live gameplay/save-load/co-op claim. |
| VAKUU-ENTRY-PRIMAL-CLAW | Vakuu fight entry is confirmed hidden by default and explicit-gated. `鍘熷垵涔嬬埅` has no unique source object; current implemented matching object is `Claws` / `鏋埅`. | Source search and review note in `docs/review.md`. |
| VAKUU-DOC-GUARD-PHRASE | Vakuu current docs now include the exact `ParentEventId` evidence wording required by the save-risk guards, and no longer describe the active fight as using the old Core-rejected shape. | `EZMB_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build`: 189 passed, 0 failed. |
| 2026-05-17-SECOND-SOURCE-REVIEW-NO-NEW-BLOCKERS | Second source review found no P0/P1/P2 findings to extract into `docs/issues.md`; no new source blocker. | `docs/review.md` v3.2 worktree review; no game run or long tests. |
| 2026-05-17-FINAL-STATIC-REVIEW-NO-EXTRACTABLE-ISSUES | Final static review found no P0/P1/P2 source blockers and no active doc hygiene blockers to extract into `docs/issues.md`; remaining risks are live-only manual proof gates. | `docs/review.md` v3.2 Final Static Review; no game, build, test, or publish run in that review. |
| DOC-BOOTSTRAP-SPIREPLUS | `scripts/bootstrap-windows.ps1` now targets Spire Plus / `EZMicroBalance`, asks for previous framework v3.1.2, and no longer tells testers to verify `EzDailyContent`. | Guard added in `ReleaseSafetyExpandedGuardTests`; full validation passed in this pass. |
| DOC-V22-WORKLOG-READABILITY | The corrupted active v2.2 work-log was archived to `docs/archive/feature-work-logs/ancient-expansion-v2.2/work-log-20260517-pre-cleanup.md`; the active work-log is now a compact readable current summary with no common `s...s` delimiter corruption. | Guard added in `ReleaseSafetyExpandedGuardTests`; full validation passed in this pass. |
| DOC-HISTORICAL-LOADER-WORDING | README, beta compatibility notes, and changelog now mark the 22-field loader smoke as historical evidence and keep current 25-field package loader parity pending. | Guarded by existing 22-field documentation checks plus this review pass; full validation passed. |
| CSHARP-ZHS-MOJIBAKE | Inline Simplified Chinese text in Firemark, Banner, Boss Seal, Fission, Royal Decree, Forge Token, and Trial Branch source localization now uses readable short player-facing wording instead of corrupted text. | `dotnet build EZMicroBalance.sln` passed; `AncientPlayerFacingPolishGuardTests` now checks active C# source for known mojibake fragments. |
| DOC-ANCIENT-ART-STATUS-STALE | Current Ancient docs now mark temporary option/icon art notes as historical and point to the browser GPTimage2 rebuilt small-art pass; live UI preview remains pending. | Static docs review recorded in `docs/review.md`; no live UI claim. |
| DOC-TESTREADY-SLIM | `docs/test-ready-development-goal.md` is now a compact current control document with short required reading, a staged refactor plan, manual evidence gates, and validation commands. The old embedded one-shot prompt, subagent roster, and duplicated image-generation contract were removed from the active reading path. | Focused doc guard tests passed: `ReleaseSafetyExpandedGuardTests`, `VakuuLothaSaveRiskGuardTests`, `AncientUiReadinessGuardTests`, and `AncientArtAssetHygieneGuardTests` filter, 42 passed / 6 skipped. |
| ASCENSION-COMBAT-HELPER-SPLIT | `AscensionCombatModifierService` was made partial and its shared activation checks, act-scaling values, enemy filters, and command helpers moved to `AscensionCombatModifierService.Helpers.cs`. Ascension combat source guards now read the whole combat source tree instead of pinning assertions to one oversized file. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| ASCENSION-BANNER-PARTIAL-SPLIT | A16 Banner Room behavior was moved from the central `AscensionCombatModifierService.cs` file into `AscensionCombatModifierService.Banners.cs`. Entry points stay in the main service; Banner-specific fallback, turn, death, card-play, and reward logic now has its own file. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| ASCENSION-FIREMARK-PARTIAL-SPLIT | A12 Firemarked Elite behavior was moved from the central `AscensionCombatModifierService.cs` file into `AscensionCombatModifierService.Firemarks.cs`. Host selection, Heat, Molten Core, Forge Armor, Constant Heal, and Firemark turn/damage handling now have their own file. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| ASCENSION-BOSS-SEAL-PARTIAL-SPLIT | A19/A20 Boss Seal and King Brand behavior was moved from the central `AscensionCombatModifierService.cs` file into `AscensionCombatModifierService.BossSeals.cs`. Entry points stay in the main service; Boss-specific combat start, turn, damage, death, card-play, phase carryover, and courtyard recovery logic now has its own file. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| ASCENSION-BOSS-SEAL-FINE-SPLIT | The first Boss Seal partial was further split into lifecycle dispatch, monster-window effects, card-pressure effects, and phase/courtyard carryover files. Each Boss Seal file is now below 350 lines. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-ROOT-SIGHT-PARTIAL-SPLIT | Root Sight preview selection, map hover, room type/model routing, event/encounter preview selection, and Root Sight map marker were moved from `UrdaRunHook.cs` into `UrdaBlessingService.RootSight.cs`. `UrdaRunHook.cs` keeps the shared `Progress` record and serialized field order. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-SEED-BANK-PARTIAL-SPLIT | Seed Bank reward storage, relic-click extraction, stored-card parsing, stored count/readiness helpers, and relic status refresh were moved from `UrdaRunHook.cs` into `UrdaBlessingService.SeedBank.cs`. `UrdaRunHook.cs` keeps the shared `Progress` record and serialized field order. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-SEEDBED-PARTIAL-SPLIT | Seedbed rest-option acceptance, combat catch, planted-root marking, combat slot persistence, and max-HP payoff logic were moved from `UrdaRunHook.cs` into `UrdaBlessingService.Seedbed.cs`. `UrdaRunHook.cs` keeps the shared `Progress` record and serialized field order. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-TRIAL-BRANCH-PARTIAL-SPLIT | Trial Branch card offers, play tracking, missed-combat removal, and visible enchantment progress were moved from `UrdaRunHook.cs` into `UrdaBlessingService.TrialBranch.cs`. `UrdaRunHook.cs` keeps the shared `Progress` record and serialized field order. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-TRIAL-BRANCH-OFFER-SPLIT | Trial Branch offer generation and selection flow were moved from `UrdaBlessingService.TrialBranch.cs` into `UrdaBlessingService.TrialBranchOffer.cs`. The offer still shows 4 rare cards, upgrades the chosen card, adds it to the deck, marks it as a Trial Branch card, and refreshes the visible enchantment. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-TRIAL-BRANCH-DISPLAY-SPLIT | Trial Branch card lookup, progress text, enchantment refresh, and marker cleanup were moved from `UrdaBlessingService.TrialBranch.cs` into `UrdaBlessingService.TrialBranchDisplay.cs`. Combat tracking and settlement now stay in the smaller Trial Branch core file. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| MORVI-DEBT-LOAN-SPLIT | Forbidden Loan, Red Ink Overdraft, and Debt Settlement logic were moved from `MorviRunHook.cs` into `MorviBlessingService.DebtAndLoan.cs`. Public card entry points still call `MorviBlessingService`, and source guards now read the Morvi source tree where behavior spans partial files. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| DOC-WORKLOG-SLIM-20260518 | The active `docs/features/ancients-rework-v4/work-log.md` and `docs/features/ascension-11-20/work-log.md` files were reduced to compact current summaries. The long chronological histories were moved to `docs/archive/feature-work-logs/ancients-rework-v4/` and `docs/archive/feature-work-logs/ascension-11-20/`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| LOTHA-HOOK-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: Lotha run/combat hook wrapper classes moved from the former `LothaRunHook.cs` into `LothaHooks.cs`, and source guards now read the Lotha source tree. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| LOTHA-STATE-SPLIT | Historical note, superseded by `LOTHA-CARD-RULES-SPLIT-20260518`: selected-blessing state, deck mirror sync, Progress parsing/writing, transient combat state, and Death Reprieve hydration/resolution moved from the former `LothaRunHook.cs` into `LothaBlessingService.State.cs`. The serialized Progress layout is unchanged. | Original validation passed; current source structure is covered by the newer Lotha card-rules split row. |
| LOTHA-DEATH-REPRIEVE-SPLIT | Lotha Death Reprieve lethal prevention, late-death fallback, extra turn setup, temporary Power creation, turn-end resolution, and cost-free play checks were moved into `LothaBlessingService.DeathReprieve.cs`. The existing deck-state phase mirror and hook wrappers are unchanged. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| LOTHA-PUBLIC-EVIDENCE-SPLIT | Lotha Public Evidence Power amount hooks, Enlightenment gain/removal, turn-start Block/draw payoff, and damage-debuff exclusions were moved into `LothaBlessingService.PublicEvidence.cs`. The Power hook wrappers still call `LothaBlessingService`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| ASCENSION-MAP-DEEP-BRANCH-SPLIT | A17 Deep Branch insertion, existing-branch detection, route-safety checks, node typing, and metadata restore were moved from `AscensionMapService.cs` into `AscensionMapService.DeepBranches.cs`. Map guard tests now read the whole `Ascension/Map` source tree so future A11/marker splits stay guarded. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| ASCENSION-MAP-A11-GEOMETRY-SPLIT | A11 geometry application, source-boundary diagnostics, route-row insertion, width-choice insertion, geometry proof conversion, and serializable-map bridge helpers were moved from `AscensionMapService.cs` into `AscensionMapService.A11Geometry.cs`. The public map hook entry point is unchanged. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| ASCENSION-MAP-MARKERS-SPLIT | A12 Firemark, A16 Banner, A19/A20 Boss marker assignment, stable marker ordering, quest-marker attachment, and map path-choice helpers were moved from `AscensionMapService.cs` into `AscensionMapService.Markers.cs`. The main service now holds only entry flow, metadata lookup, and applied-map side-table state. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROUTE-REWARDS-PARTIAL-SPLIT | Shallow-Root Relic and Rooted Route reward logic were moved from `UrdaRunHook.cs` into `UrdaBlessingService.RouteRewards.cs`. The stale `ShallowRootSettlementMaxHpLoss` constant was removed because the implemented fallback removes the unrooted relic at Act 2 and refunds 75 Gold. `UrdaRunHook.cs` keeps the shared `Progress` record and serialized field order. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-STATE-MAP-HELPERS-PARTIAL-SPLIT | Shared Urda map helpers were moved into `UrdaBlessingService.MapHelpers.cs`; progress serialization, selected blessing state, and deck mirror sync were moved into `UrdaBlessingService.State.cs`. `UrdaRunHook.cs` now focuses on hook dispatch and remaining Humus/Molting/Moss/After Rain logic. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| TEST-SHARED-HELPER-EXTRACTION-1 | Package directory content checks and small transparent PNG checks were moved from `ReleaseCoverageGuardTests.cs` into shared `TestRepo` helpers. `TestInfrastructureGuardTests` and the test README now guard that future tests reuse these helpers. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. |
| URDA-AFTER-RAIN-PARTIAL-SPLIT | After Rain lethal prevention, Elite gold, and Act 2 compensation logic were moved from `UrdaRunHook.cs` into `UrdaBlessingService.AfterRain.cs`. Later cleanup moved the remaining Humus/Molting/Moss flow too. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-MOSS-MAP-PARTIAL-SPLIT | Moss Map first-room reward values and room-type settlement logic were moved from `UrdaRunHook.cs` into `UrdaBlessingService.MossMap.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-HUMUS-PACT-PARTIAL-SPLIT | Humus Pact card-reward skip, third-skip completion, optional removals, and upgraded-card payoff were moved from `UrdaRunHook.cs` into `UrdaBlessingService.HumusPact.cs`. The shared card-reward context remains shared with Seed Bank. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-MOLTING-PARTIAL-SPLIT | Molting starter-card removal and Withered Husk addition were moved from `UrdaRunHook.cs` into `UrdaBlessingService.Molting.cs` with the current card-add preview behavior preserved. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-CARD-REWARD-ROUTING-SPLIT | Urda card-reward tracking and alternative routing were moved from `UrdaRunHook.cs` into `UrdaBlessingService.CardRewards.cs`. Seedbed, Humus Pact, and Seed Bank still share the same tracked reward context. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-STATUS-SPLIT | Root Sight setup and relic-status refresh were moved from `UrdaRunHook.cs` into `UrdaBlessingService.RootSightStatus.cs`, leaving map preview and hover handling in `UrdaBlessingService.RootSight.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-RUN-LIFECYCLE-SPLIT | Urda run lifecycle handlers were moved from `UrdaRunHook.cs` into `UrdaBlessingService.RunLifecycle.cs`; shared Urda loc helper moved to `UrdaBlessingService.Localization.cs`; Rooted Route map marker moved to `UrdaRootedRouteMapQuestMarker.cs`. `UrdaRunHook.cs` now contains hook dispatch classes only. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-HOVER-SPLIT | Root Sight hover-tip generation and preview-title lookup were moved from `UrdaBlessingService.RootSight.cs` into `UrdaBlessingService.RootSightHover.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-ROUTING-SPLIT | Root Sight room-type/model routing for the RunManager patches was moved from `UrdaBlessingService.RootSight.cs` into `UrdaBlessingService.RootSightRouting.cs`; patch entry points still call the same service methods. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-PREVIEW-STORE-SPLIT | Root Sight preview record type plus parse/find/format helpers were moved from `UrdaBlessingService.RootSight.cs` into `UrdaBlessingService.RootSightPreviewStore.cs`. The serialized `RootSightPreviewRecords` layout is unchanged. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-PREVIEW-GENERATION-SPLIT | Root Sight preview generation, Unknown-room blacklist rolling, event peeking, and Act room-set reflection were moved from `UrdaBlessingService.RootSight.cs` into `UrdaBlessingService.RootSightPreviewGeneration.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-MARKER-SPLIT | The Root Sight map marker model was moved from `UrdaBlessingService.RootSight.cs` into `UrdaRootSightMapQuestMarker.cs`, matching the existing Rooted Route marker split. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-TRUE-PEEK | Root Sight now previews Unknown rooms with a forked `UnknownMapPoint` RNG instead of calling the live `UnknownMapPoint.Roll` path. When the previewed Unknown room is entered, it commits one live Unknown RNG step and applies vanilla-style odds reset/increase for the stored result from the room-model creation path, guarded by a one-shot run/act/coord key. Event previews now respect `ModifyNextEvent`, and entering a previewed event advances the vanilla event queue before marking the stored event visited. It also stores marked coordinates with the Act index, so Root Eyes remain usable across later acts without same-coordinate collisions. The map-click path now catches disabled map-point mouse releases while selection is active. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROOT-SIGHT-TRANSIENT-STATE | Root Sight one-shot entry commits are now stored per `RunState` instance through a weak table, so same-seed reruns and save-load recreations do not inherit stale in-process commit keys. Granting Root Sight also clears selection and commit transient state before writing the fresh five-eye progress. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| STRICT-REVIEW-RISK-FIXES-20260518 | Fixed reviewed source risks: Root Eyes revalidates stored event/encounter previews at room entry, Seed Bank relic extraction is guarded against double-click re-entry, Morvi Forbidden Loan does not commit without a selectable Ancient card, Vakuu Stolen Vault is applied to the initial custom monster, Vakuu no-reward victory has a visible fallback path, and Firemark/Banner inline zhs PowerLoc text is readable. | `dotnet build EZMicroBalance.sln --no-restore`, focused guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live gameplay/save-load/co-op proof remains pending. |
| VAKUU-FIGHT-VICTORY-SPLIT | Vakuu victory event-state restoration, no-normal-reward resume, malformed victory fallback, source Ancient reward lookup, and Lotha fallback reward choices were moved into `VakuuFightVictory.cs`. `VakuuFightPatch.cs` now keeps patch entry points, fight start, Stolen Vault power maintenance, damage-lock breaking, contract signing, pre-finished parent restore, and asset paths. | `dotnet build EZMicroBalance.sln --no-restore`, focused Vakuu/UI/source-manifest guard tests, full `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, and opt-in artifact tests passed. Live gameplay/save-load/co-op proof remains pending. |
| URDA-SHALLOW-ROOT-RELIC-SPLIT | Shallow-Root Relic choice, Elite rooting, Act 2 fallback, and relic lookup were moved from `UrdaBlessingService.RouteRewards.cs` into `UrdaBlessingService.ShallowRootRelic.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| URDA-ROUTE-MAP-HELPERS-SPLIT | Route reachable-node traversal and coordinate parsing helpers were moved from `UrdaBlessingService.RouteRewards.cs` into `UrdaBlessingService.RouteMapHelpers.cs`. | `dotnet build EZMicroBalance.sln`, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, and `git diff --check` passed. `git diff --check` only reported the existing CRLF/LF warning for `ReleaseCoverageGuardTests.cs`. |
| STRICT-SOURCE-AUDIT-FOLLOWUP-20260518 | Rooted Route now respects visible floor 7, Blight Sprouts sprout before the opening draw for their due turn, Vakuu victory relic choices set owner before option creation, melted Prismatic Gem/Fiddle hooks stop applying, Closed Court only consumes actual discounts, and Lotha/Morvi transient combat state can recover from visible Powers after state loss. | `dotnet build EZMicroBalance.sln --no-restore`, `dotnet test EZMicroBalance.sln --no-build` (179 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, opt-in artifact tests (197 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. Live gameplay/save-load/co-op proof remains pending. |
| ROOTDECK-NOTICE-SPLIT-20260518 | Rootblight deck-state logic stays in `RootDeckService.cs`; local notice UI paths moved to `RootDeckService.Notices.cs`, separating Godot node/VFX dependencies from root-family deck mutation, pending downgrade serialization, card creation, and cap trimming. | `dotnet build EZMicroBalance.sln --no-restore`, focused Ascension/ReleaseCoverage guard tests (43 passed / 4 skipped), full `dotnet test EZMicroBalance.sln --no-build` (179 passed / 18 skipped), `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `dotnet publish EZMicroBalance.sln --no-restore`, `scripts/package-spire-plus.ps1`, opt-in artifact tests (197 passed / 0 skipped), and `scripts/check-installed-ezmb-package.ps1` passed. |

Manual retest focus: Trial Branch four rare-card offer, Trial Branch enchantment/counter, Trial Branch 3/3 play success and missed-combat removal; Single Sentence extra-play/counter/play block; Humus Pact detailed Compost Reward flow; Root Eyes hover and entry result; Seed Bank click/extract and Boss transition; Morvi Overdue Library pages; Blueprint Proof first three eligible plays; Misprint Press text; Debt/Forbidden Loan text; Vakuu explicit-gate entry and Contract/Stolen Vault turn-start behavior; and Claws pickup text/effect.
