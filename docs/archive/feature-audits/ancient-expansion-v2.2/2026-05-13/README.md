# Ancient Expansion v2.2 Audit Archive

Historical archive.

These files were produced during the 2026-05-13 overnight source-audit pass. They are preserved for traceability, but they are no longer the active next-development entry point.

Use current documents instead:

- `PROJECT_STATE.md` for the current project snapshot.
- `docs/issues.md` for active blockers and next implementation issues.
- `docs/test-ready-development-goal.md` for the single long-scope implementation directive.
- `docs/features/ancient-expansion-v2.2/README.md` for the current v2.2 feature status and document map.

Do not force future agents to read every audit matrix by default. Open this archive only when investigating an old audit finding or reconstructing why a previous implementation decision was made.
