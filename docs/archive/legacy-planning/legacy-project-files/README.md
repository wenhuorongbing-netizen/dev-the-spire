# Legacy

This folder preserves migration artifacts that are useful for traceability but are not part of the active private beta build.

| Path | Purpose |
| --- | --- |
| `EzDailyContent/EzDailyContent.csproj.legacy.xml` | Original scaffold project file preserved after the independent `EZMicroBalance` project was created. |

Active code is under `EZMicroBalanceCode/`. Active resources are under `EZMicroBalance/`. Do not rename the legacy `EzDailyContent` manifest id in-place.
