# Worktree Cleanup Audit

Last updated: 2026-06-18

This document tracks cleanup/refactor scope, archive decisions, and current worktree hygiene. It is not a release-readiness claim.

## Objective

Clean and refactor the `D:\Game\FOTN\dev-the-spire` workspace by reducing redundant code and stale files, archiving uncertain material before deletion, and keeping active `Spire Plus` source/package work reviewable.

## Current Include Candidates

These paths are active private-beta source, resource, test, script, or documentation changes and should remain visible in `git status` until intentionally reviewed/staged.

| Path | Reason |
| --- | --- |
| `EZMicroBalance/` | Active mod resources, localization, event backgrounds, Vakuu encounter/monster assets, and export inputs. |
| `EZMicroBalanceCode/` | Active C# source, including Ancient feature gates, Vakuu source slice, diagnostics, and Ascension combat cleanup. |
| `tests/EZMicroBalance.Tests/` | Guard tests plus shared `TestRepo.cs` infrastructure cleanup. |
| `scripts/` | Current helper scripts for screenshot/dev-console/release-evidence validation. |
| `export_presets.cfg` | Active PCK export selection. |
| Current docs under `docs/` | Active project state, feature docs, release handoff, and cleanup/archive indexes. |
| `.gitignore` | Keeps local/generated drafts and browser-output clutter out of release-candidate diffs. |

## Archived Or Ignored Cleanup Results

| Material | Current location/status | Notes |
| --- | --- | --- |
| Original scaffold setup spec | `docs/archive/superseded/setup-spec-original-scaffold.md` | Replaced by current setup docs and retained for archaeology. |
| Feature-local prompt/spec archives | `docs/archive/feature-inputs/` | Moved out of active feature folders. |
| Goal guard architecture/refactor intake | `docs/archive/feature-inputs/goal-architecture-refactor-mojibake-intake-20260526.md` | Long corrupted architecture/refactor analysis was moved out of active `docs/goal.md`; current implementation direction remains in `docs/test-ready-development-goal.md`. |
| Stray implement design intake | `docs/archive/feature-inputs/implement-a19-a20-boss-ability-mojibake-intake-20260526.md` | Untracked corrupted `docs/implement.md` A19/A20 Boss dedicated-ability design intake was moved out of the active docs root. |
| Stray RitsuLib migration intake | `docs/archive/feature-inputs/ritsulib-migration-mojibake-intake-20260526.md` | Regenerated corrupted `docs/implement.md` RitsuLib migration architecture intake was moved out of the active docs root; no dependency or runtime decision is promoted from this archive. |
| Superseded project-state validation history | `docs/archive/project-state-history-20260516.md` | Full pre-cleanup `PROJECT_STATE.md` snapshot retained; active `PROJECT_STATE.md` now carries current status, blockers, commands, and next action. |
| Superseded issues package verification note | `docs/archive/implementation-records/2026-05-15-current-package-verification-note.md` | Long-form 2026-05-15 package verification detail was moved out of the active issues index; `docs/issues.md` keeps only the current hashes, evidence pointer, and pending manual gates. |
| Duplicate goal guard alias | Deleted `docs/implement.md`; canonical content remains in `docs/goal.md` | `git show HEAD:docs/implement.md` matched the active goal guard content, and current docs/tests read `docs/goal.md`. `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` now prevents the duplicate alias from returning. |
| Generated C# UID sidecars and test result output | Deleted tracked `EZMicroBalanceCode/**/*.cs.uid`; future regenerated source/test `.cs.uid` files and VSTest `TestResults/` folders are ignored | These files are Godot/test-run metadata, not compile/package inputs. `export_presets.cfg` excludes `*.cs.uid`, `EZMicroBalance.csproj` compiles `EZMicroBalanceCode/**/*.cs`, and several deleted UID files pointed at source files that no longer exist. |
| Duplicate root mod surfaces | Removed from active root | `EzDailyContent*` and standalone `EZFuturePeek*` surfaces were removed after the project owner chose a single active mod surface. Historical metadata remains in `docs/archive/legacy-planning/`. |
| Superseded architecture audit | `docs/archive/feature-audits/architecture-clean-code-management-audit-2026-05-19.md` | Retained for historical governance context; no longer current because it recommends an independent `EZFuturePeek` path that the owner later folded into Spire Plus. |
| Historical RC1 live log | `docs/archive/implementation-records/rc1-live-validation-log-20260508-20260513.md` | Older v0.105 / pre-current-package evidence retained outside the active reading path. Current package evidence stays in `docs/release-evidence-status.md` and `docs/dev-environment.md`. |
| Historical runtime smoke details | `docs/archive/implementation-records/dev-environment-runtime-smoke-history-20260526.md` | Older launch/helper attempt detail was moved out of active `docs/dev-environment.md`; current environment evidence now keeps compact latest status and pending gates. |
| Top-level legacy project metadata | `docs/archive/legacy-planning/legacy-project-files/` | Former root `legacy/` folder moved into the existing legacy-planning archive. |
| Root local art/calibration folders | `.tools/archive/local-art-and-calibration-20260515/` | Ignored local archive. |
| Root local clutter archives | `.tools/archive/local-root-clutter-20260515/` | Ignored local archive. |
| Static website and Pages workflow | Current tracked `website/` and `.github/workflows/spire-plus-site.yml`; older snapshot remains under `.tools/archive/local-website-preview-20260516/` | The public site was deliberately promoted after cleanup; generated forum build output under `website/forum/` and Godot `.import` metadata under `website/**` remain ignored. A targeted ignored-only clean removed the stale local forum build and 112 website `.import` sidecars while preserving tracked website source. |
| Website localization QA journal | `docs/archive/implementation-records/website-localization-qa-20260522.md` | Former `website/localization_qa.md` was historical QA evidence with old beta.39 package metadata, so it was archived out of the public website surface. Current website package/download state remains in `website/content-data.js`, `website/README.md`, and website guards. |
| Transient Edge browser profiles | Deleted `.tools/edge-chatgpt-profile-clone-20260515/`, `.tools/edge-chatgpt-profile-clone2-20260515/`, `.tools/edge-chatgpt-profile/`, and `.tools/edge-chatgpt-pw-profile/` | These were unreferenced browser cache/profile folders; the retained evidence/art outputs remain under `.tools/art-generation/`, active resources, and current docs. |
| Stale redirected publish outputs | Deleted `.tools/publish-redirect/`, `.tools/publish-redirect2/`, and `.tools/publish-mods/` | These ignored workspace-local DLL/manifest/PCK outputs were historical validation artifacts, not the current `publish/` package or installed-artifact evidence. |
| Stale install backup | Deleted `.tools/install-backups/` | This was an unreferenced backup of older installed `EZMicroBalance` artifacts; current package evidence remains under `publish/` and runtime evidence. |
| Generated local tool/cache folders | Deleted `.tools/browser-automation/`, `.tools/godot-appdata/`, `.tools/godot-localappdata/`, and `.tools/godot-user/` | These were generated Playwright/Godot cache or scratch folders. The actual Godot executable, GDRE/decompilation tools, ILSpy, runtime evidence, and art provenance remain. |
| Local Playwright screenshots/logs | Ignored `/output/playwright/`; stale ignored files pruned on 2026-05-26 | Already tracked evidence files remain reviewable, but new browser screenshots, HARs, logs, and PID files no longer flood the Spire Plus release-candidate status. A targeted ignored-only clean removed 126 local browser/Supabase/forum output files while preserving all 71 tracked evidence files. |
| Stale feature-folder archive guidance | current docs now point historical prompts/specs to `docs/archive/feature-inputs/` | Guarded so active feature folders do not grow local `archive/` prompt/spec folders again. |
| Current worktree batch pathspecs | `.tools/worktree-batches/current/` | Ignored local staging aids generated by `scripts/report-worktree-batches.ps1`; current 2026-06-17 continuation is dirty at HEAD `c5d1893e` with validation-lane script/doc/test edits plus concurrent `docs/goals/*` edits. This is worktree-hygiene context only, not release-readiness or live-proof evidence. |

## Refactor Cleanup Completed

| Area | Result | Guard |
| --- | --- | --- |
| FeatureRegistry environment-helper split | `FeatureRegistry.Environment.cs` owns only the shared force/disable environment override helpers; `FeatureRegistry.cs` remains responsible for bootstrap orchestration, bootstrap records, summary logging, and RewardPipeline diagnostics. Behavior is intended unchanged, and no build/test/package/runtime validation was run for this move-only pass. | `EngineeringGovernanceGuardTests` and active source manifest validation. |
| RewardPipeline diagnostics contract split | `RewardPipelineDiagnosticsContracts.cs` owns the diagnostics-only reward phase, handler, and context contract shapes, while `RewardPipeline.cs` keeps no-op handler registration, phase/priority sorting, diagnostics invocation, and test cleanup. | `ArchitectureSkeletonGuardTests`, active source manifest validation, and test project compile-link coverage. |
| DeathProtection diagnostics contract split | `DeathProtectionDiagnosticsContracts.cs` owns the diagnostics-only request/result/check/priority/provider contract shapes, while `DeathProtectionService.cs` keeps no-op provider registration and check orchestration. Lotha lethal-path behavior remains unwired from this diagnostics service. | `ArchitectureSkeletonGuardTests`, active source manifest validation, and test project compile-link coverage. |
| MultiplayerPolicy diagnostics contract split | `MultiplayerPolicyDiagnosticsContracts.cs` owns only the diagnostics-only multiplayer category enum and policy record, while `MultiplayerPolicy.cs` keeps registry storage, locking, ordinal lookup, category queries, and test cleanup. No gameplay wiring changed, and no build/test/package/runtime validation was run for this static move-only pass. | `ArchitectureSkeletonGuardTests`, active source manifest validation, and test project compile-link coverage. |
| Repository path helpers | Centralized in `tests/EZMicroBalance.Tests/TestRepo.cs`. | `TestInfrastructureGuardTests.GuardTestsUseSharedRepositoryPathHelpers` |
| Source evidence assertions | `AssertSourceContains` centralized in `TestRepo.cs`. | Same infrastructure guard. |
| Mojibake assertions | `AssertNoMojibake` centralized in `TestRepo.cs`; feature-specific bad-fragment lists remain local to their tests. | Same infrastructure guard and test README. |
| Bilingual localization key assertions | `AssertLocalizedKeys` centralized in `TestRepo.cs`; feature tests can pass a feature-specific value validator such as Morvi mojibake checks. | Same infrastructure guard and test README. |
| JSON/source slicing/order/count helpers | `JsonStringMap`, `SliceFrom`, `SliceBetween`, `AssertBefore`, and `CountOccurrences` centralized in `TestRepo.cs`. | Same infrastructure guard and test README. |
| ZIP/PCK/hash/source-tree helpers | `ReadZipBytes`, `ReadZipText`, `ReadPckDirectory`, `ReadSourceTree`, `ReadAllTestSource`, and `Sha256` centralized in `TestRepo.cs`. | Same infrastructure guard and test README. |
| Recursive JSON values, manifest version, PNG byte/dimension, JSON normalization, and exception unwrap helpers | `JsonStringValues`, `ManifestVersion`, `ReadPngBytes`, `ReadPngDimensions`, `NormalizeJson`, and `Unwrap` centralized in `TestRepo.cs`; local duplicate helpers removed from release/art/ancient guard tests. | Same infrastructure guard and test README. |
| Export preset parsing | `ParseExportFiles` centralized in `TestRepo.cs` and removed from four release/art guard test files. | Same infrastructure guard and test README. |
| Active release resource predicates | `IsActiveExportResource` and `IsActiveReleaseResource` centralized in `TestRepo.cs`; duplicate package-boundary predicates removed from release guard tests. | Same infrastructure guard and test README. |
| Ascension source tree test reads | `AscensionFeatureGuardTests` now uses shared `ReadSourceTree` instead of a local source-tree reader. | Same infrastructure guard. |
| Ancient feature gate env parsing | Shared `AncientFeatureGate` helper under `EZMicroBalanceCode/Ancients/Common/`. | Release/source guards. |
| Urda hook file split | `UrdaRunHook.cs` keeps run/reward/map dispatch and co-op guard helpers; `UrdaCombatHook.cs` owns the combat-hook wrapper class. | `AncientHighRiskSourceGuardTests`, `CoopCombatSafetyGuardTests`, and active source manifest validation. |
| Morvi card model split | Removed `MorviArchivePageCards.cs` and `MorviCombatTokenCards.cs`; each Archive Page, Red Ink Overdraft, and Waste Paper card model now owns a dedicated file. | `MorviV22GuardTests`, player-facing polish guards, and active source manifest validation. |
| Urda card model split | Removed `UrdaCards.cs`; `UrdaSeedling.cs`, `UrdaSeedbed.cs`, `UrdaRainBreath.cs`, and `WitheredHusk.cs` now own their respective card models. | `UrdaReleaseCoverageGuardTests` and active source manifest validation. |
| Urda state codec split | `UrdaStateCodec.cs` now owns Urda progress encode/decode and legacy short-state compatibility, while `UrdaBlessingService.State.cs` keeps only player-state read/write orchestration. | `SaveStateContractsGuardTests.UrdaStateCodecKeepsWireFormatAndLegacyDecodeStable` and active source manifest validation. |
| Urda option relic split | Removed the mixed `UrdaOptionRelics.cs` aggregate; `UrdaOptionRelic.cs` owns the base event-relic policy, `UrdaStandardOptionRelics.cs` owns static art relics, `UrdaRootSightOptionRelic.cs` owns Root Eyes counter display, `UrdaSeedBankOptionRelic.cs` owns stored-card hover/counter display, and `UrdaOptionRelicClickPatch.cs` owns relic-click routing. | `UrdaReleaseCoverageGuardTests`, player-facing polish guards, patch inventory, and active source manifest validation. |
| Urda Rooted Route reward split | Removed the misleading `UrdaBlessingService.RouteRewards.cs` aggregate; `UrdaBlessingService.RootedRoute.cs` owns target selection and wither checks, while `UrdaBlessingService.RootedRouteReward.cs` owns reached-node reward creation/offering. | `UrdaReleaseCoverageGuardTests` and active source manifest validation. |
| Urda Trial Branch combat split | Removed the remaining `UrdaBlessingService.TrialBranch.cs` aggregate; `UrdaBlessingService.TrialBranchCombat.cs` owns card-play tracking, while `UrdaBlessingService.TrialBranchResolution.cs` owns combat-end completion/failure resolution. | `UrdaReleaseCoverageGuardTests` and active source manifest validation. |
| Urda Root Sight selection commit split | Removed the mixed `UrdaBlessingService.RootSight.cs` aggregate; `UrdaBlessingService.RootSightSelection.cs` owns Root Eyes lifecycle/open/cancel/context clearing, while `UrdaBlessingService.RootSightSelectionCommit.cs` owns node validation, preview record writes, marker refresh, and first-potion payoff. | `AncientHighRiskSourceGuardTests`, `SaveStateContractsGuardTests`, and active source manifest validation. |
| Urda Seed Bank extraction split | `UrdaBlessingService.SeedBankExtraction.cs` owns relic-click eligibility, picker/cancel handling, and unchosen-card cleanup; `UrdaBlessingService.SeedBankExtractionCommit.cs` owns selected-card deck additions, failed-card preservation, storage clearing, and extraction logs; `UrdaBlessingService.SeedBankExtractionState.cs` owns the public click entry and reentrancy guard. | `UrdaReleaseCoverageGuardTests`, player-facing polish guards, save-state guards, and active source manifest validation. |
| Urda Seedbed state/patch split | `UrdaBlessingService.SeedbedCombat.cs` now owns planting behavior, `UrdaBlessingService.SeedbedState.cs` owns combat-slot persistence and planted-card markers, and `UrdaSeedbedAfterCardDrawnPatch.cs` owns the draw-hook suppression patch. | `UrdaReleaseCoverageGuardTests` and active source manifest validation. |
| Morvi combat-state split | `MorviBlessingService.State.cs` keeps persistent selected-blessing/progress serialization, while `MorviBlessingService.CombatState.cs` owns transient combat state fields and the player `ConditionalWeakTable`. | `MorviV22GuardTests`, `SaveStateContractsGuardTests`, and active source manifest validation. |
| Morvi Open-Book state split | `MorviBlessingService.OpenBook.cs` keeps turn-start draw/Energy, turn-end sealing, and turn-3 return flow, while `MorviBlessingService.OpenBookState.cs` owns sealed-card restore evidence logging and marker cleanup. | `MorviV22GuardTests`, `SaveStateContractsGuardTests`, and active source manifest validation. |
| Morvi hook/lifecycle ownership split | Removed the mixed `MorviHooks.cs` aggregate; `MorviRunHook.cs` owns run/deck-mirror/combat-start/end dispatch and co-op gate helpers, `MorviCombatHook.cs` owns combat-only hook dispatch, and `MorviBlessingService.CombatLifecycle.cs` owns Morvi combat lifecycle service logic. | `AncientHighRiskSourceGuardTests`, `CoopCombatSafetyGuardTests`, `AncientStateMirrorGuardTests`, `MorviV22GuardTests`, `SaveStateContractsGuardTests`, and active source manifest validation. |
| Lotha Death Reprieve split | `LothaBlessingService.State.cs` keeps selected-blessing/progress serialization, `LothaBlessingService.DeathReprieveState.cs` owns Death Reprieve save-hydrate recovery, `LothaBlessingService.DeathReprieve.cs` owns lethal-prevention entry checks, and `LothaBlessingService.DeathReprieveTurn.cs` owns the actual reprieve turn start, Power upkeep, and turn-end resolution. | `VakuuLothaSaveRiskGuardTests`, `SaveStateContractsGuardTests`, `LothaPolishGuardTests`, and active source manifest validation. |
| Lotha play-count split | `LothaBlessingService.CardPlayCount.cs` owns `ModifyCardPlayCount`, extra-play counts, and extra-play branch selection, while `LothaBlessingService.CardRules.cs` keeps ShouldPlay. | `LothaPolishGuardTests` and active source manifest validation. |
| Lotha card-play dispatch split | `LothaBlessingService.CardPlayDispatch.cs` owns `AfterCardPlayed` dispatch for Mirror Rebuttal, Mirror Hall Echo, Deferred Verdict, and Single Sentence, leaving `LothaBlessingService.CardRules.cs` focused on ShouldPlay. | `LothaPolishGuardTests` and active source manifest validation. |
| Lotha hook ownership split | Removed the mixed `LothaHooks.cs` aggregate; `LothaRunHook.cs` owns run/reward/death-prevention dispatch and co-op gate helpers, while `LothaCombatHook.cs` owns combat-only card, turn, cost, and Power hooks. | `AncientHighRiskSourceGuardTests`, `CoopCombatSafetyGuardTests`, `AncientStateMirrorGuardTests`, and active source manifest validation. |
| Vakuu fight hook ownership split | `VakuuFightRunHook.cs` keeps initialization/subscription, `VakuuFightCombatHook.cs` owns combat hooks, and `VakuuContractService.cs` owns Contract/Cash Out injection. | `VakuuTemptationGuardTests` and active source manifest validation. |
| Vakuu child-combat service split | Removed `VakuuFightService.CombatState.cs`; Stolen Vault sync, damage-threshold lock breaks, Contract/Cash Out actions, and Blood Debt mutation now live in focused `VakuuFightService.*.cs` partial files. | `VakuuTemptationGuardTests` and active source manifest validation. |
| Urda active-doc count sweep | Current-facing Urda support docs and the website claim audit now consistently describe eleven source-backed blessing ids, including Elite Root, and reject old ten-count wording. | `DocumentationCompactnessGuardTests.UrdaSupportDocsKeepElevenBlessingCountCurrent` |
| Ascension combat metadata refresh | Duplicated metadata lookup consolidated in `AscensionCombatModifierService`. | `AscensionV2MilestoneGuardTests`. |
| Stale Morvi v2.2 enable constant | Removed unused `LegacyEnableEnvironmentVariable` from `MorviFeatureGate`; Morvi is current default-on with disable/force gates. | Build/test validation. |
| Current display-name docs | Updated current-facing intro/audit/status wording to use `Spire Plus` with `EZMicroBalance` only as the stable manifest id. | Diff-check and release/name guards. |
| Current manual-test display-name wording | Active manual verification and waiting-test instructions now say `Spire Plus`; `EZMicroBalance` remains only for technical ids, paths, commands, and literal historical log evidence. | Default tests and targeted `rg` scan. |
| Ancient v4 current-target wording | Active Ancient API discovery and manual checklist docs now point to the refreshed `v0.106.1` target and keep `v0.104.0` only as historical Batch 2 context. | `DocumentationCompactnessGuardTests.AncientV4DocsUseCurrentRuntimeTargetAndArchiveOldBaseline` |
| Sere Talon visual route split | `SereTalonVisualPatches.cs` keeps only Harmony entry points. Sere Talon art path constants, RelicModel getter routes, UI node fallback routes, texture loading, and once-only route logging now live in focused `SereTalonVisual*` helper files. | `AncientBehaviorGuardTests`, `AncientHighRiskSourceGuardTests`, `GoalCompletionGuardTests`, active source manifest validation, and patch inventory guards. |
| AncientBehavior localization/docs guard split | `AncientBehaviorGuardTests.LocalizationDocs.cs` now owns v4.3/v4.2 doc-current checks, localization placeholder/value parity, Simplified Chinese numeric/mojibake guards, current Ancient docs checks, release-checklist guard coverage, and manual verification matrix row coverage. This split originally left package-shape, patch-target, Ancient reward behavior, Sere Talon/Tanx Claws, and source/mechanic behavior guards in `AncientBehaviorGuardTests.cs`; later rows below record the focused follow-up splits. | Focused `AncientBehaviorGuardTests` validation, default tests, and opt-in release-artifact tests. |
| AncientBehavior Sere Talon route/helper split | `AncientBehaviorGuardTests.SereTalonRoutes.cs` owns the Sere Talon/Tanx Claws source-route and local-core route guards plus `ReadSereTalonVisualSource()`. `AncientBehaviorGuardTests.cs` keeps package shape and implemented Ancient patch-target coverage. | Original route split: build passed with 0 warnings / 0 errors and focused `AncientBehaviorGuardTests` passed 22 / 0 / 2 / 24. Helper ownership follow-up used static-only split checks during the shared validation pause; no build, test, format, publish, package, runtime, gameplay, QA, staging, commit, push, or handoff validation was run from this thread. |
| AncientBehavior Distinguished Cape guard split | `AncientBehaviorGuardTests.DistinguishedCape.cs` owns the Distinguished Cape v4.3 Max HP math/unpayable-selection guard, the low-Max-HP Vakuu visible-option-count guard, and the two Cape cost helpers. `AncientBehaviorGuardTests.cs` keeps surrounding Ancient behavior guards, while `AncientBehaviorGuardTests.PrismaticGem.cs` owns Regex-based banner diagnostics. | Build passed with 0 warnings / 0 errors; focused `AncientBehaviorGuardTests` passed 22 / 0 / 2 / 24; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Sovereign Blade guard split | `AncientBehaviorGuardTests.SovereignBlade.cs` owns the Sovereign Blade jade-boon source/localization guard and its text helper. `AncientBehaviorGuardTests.cs` keeps package shape, patch target, Velvet Choker, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused `AncientBehaviorGuardTests` passed 22 / 0 / 2 / 24; focused `DocumentationCompactnessGuardTests` passed 25 / 0 / 0 / 25; current-doc claims passed 962 / 0; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Prismatic Gem guard split | `AncientBehaviorGuardTests.PrismaticGem.cs` owns the two Prismatic Gem reward/reroll and banner-diagnostics guards, including Regex-based fallback diagnostics checks. `AncientBehaviorGuardTests.cs` keeps package shape, implemented Ancient patch-target coverage, Velvet Choker, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused AncientBehavior plus DocumentationCompactness guards passed 47 / 0 / 2 / 49; current-doc claims passed 962 / 0; static suite passed 15 / 0; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Velvet Choker guard split | `AncientBehaviorGuardTests.VelvetChoker.cs` owns the Velvet Choker soft-limit source/localization/manual-matrix guard. `AncientBehaviorGuardTests.cs` keeps package shape, implemented Ancient patch-target coverage, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused AncientBehavior, RuntimeMonkey, ReleaseSafetyExpanded, and DocumentationCompactness guards passed 88 / 0 / 2 / 90; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior direct deck-gain feedback guard split | `AncientBehaviorGuardTests.DirectDeckGainFeedback.cs` owns the Spire Plus direct deck-gain feedback source guard. `AncientBehaviorGuardTests.cs` keeps package shape, implemented Ancient patch-target coverage, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused ReleaseEvidence, RuntimeMonkey, AncientBehavior, and DocumentationCompactness guards passed 87 / 0 / 2 / 89; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Jeweled Mask guard split | `AncientBehaviorGuardTests.JeweledMask.cs` owns the Jeweled Mask power-enchantment source/manual-matrix guard. `AncientBehaviorGuardTests.cs` keeps package shape, implemented Ancient patch-target coverage, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused ReleaseEvidence, RuntimeMonkey, AncientBehavior, and DocumentationCompactness guards passed 87 / 0 / 2 / 89; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Jewelry Box guard split | `AncientBehaviorGuardTests.JewelryBox.cs` owns the Jewelry Box non-Innate Apotheosis marker source guard. `AncientBehaviorGuardTests.cs` keeps package shape, implemented Ancient patch-target coverage, and remaining Ancient behavior guards. | Build passed with 0 warnings / 0 errors; focused ReleaseEvidence, RuntimeMonkey, AncientBehavior, and DocumentationCompactness guards passed 87 / 0 / 2 / 89; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Pael's Tooth guard split | `AncientBehaviorGuardTests.PaelsTooth.cs` owns the Pael's Tooth saved-counter and stored-card-return source/manual-matrix guard. `AncientBehaviorGuardTests.cs` keeps package shape and implemented Ancient patch-target coverage. | Build passed with 0 warnings / 0 errors; focused ReleaseEvidence, RuntimeMonkey, AncientBehavior, and DocumentationCompactness guards passed 87 / 0 / 2 / 89; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Debt/Folly guard split | `AncientBehaviorGuardTests.DebtAndFolly.cs` owns the Debt and Folly player-text/source-behavior guard. `AncientBehaviorGuardTests.cs` keeps package shape and implemented Ancient patch-target coverage. | Build passed with 0 warnings / 0 errors; focused AncientBehavior plus DocumentationCompactness guards passed 47 / 0 / 2 / 49; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior temporary generated-card guard split | `AncientBehaviorGuardTests.TemporaryGeneratedCards.cs` owns the temporary generated-card cleanup/source/manual-matrix guard. `AncientBehaviorGuardTests.cs` keeps package shape and implemented Ancient patch-target coverage. | Build passed with 0 warnings / 0 errors; focused AncientBehavior plus DocumentationCompactness guards passed 47 / 0 / 2 / 49; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientBehavior Meat Cleaver guard split | `AncientBehaviorGuardTests.MeatCleaver.cs` owns the Meat Cleaver rest-site source/localization/manual-checklist guard. `AncientBehaviorGuardTests.cs` keeps package shape and implemented Ancient patch-target coverage. | Build passed with 0 warnings / 0 errors; focused AncientBehavior plus DocumentationCompactness guards passed 47 / 0 / 2 / 49; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientHighRisk Root Sight guard split | `AncientHighRiskSourceGuardTests.RootSight.cs` owns the Root Sight map-preview/no-consume guard. `AncientHighRiskSourceGuardTests.cs` keeps the other high-risk Ancient source-shape guards and shared helpers; the moved method body, assertions, string literals, and source reads are unchanged. | Static-only method-body parity, ownership scans, scoped diff review, and whitespace checks passed; no build, tests, publish, format, package validation, runtime/game smoke, staging, commit, push, or handoff validation was run from this lane. |
| AscensionFeature rootblight guard split | `AscensionFeatureGuardTests.Rootblight.cs` now owns Root starter, RootBud seeding/gating, and Root family card/localization guards. `AscensionFeatureGuardTests.cs` keeps selection, env gates, localization bridge, multiplayer diagnostics, map/combat slice, map geometry, Firemark/Fission, Boss Seal, metadata, and Ascension source-surface guards. | Focused `AscensionFeatureGuardTests` validation, default tests, and opt-in release-artifact tests. |
| AscensionV2 milestone BossSeals guard split | `AscensionV2MilestoneGuardTests.BossSeals.cs` owns the Core Boss Seal local-source guard and Boss Seal power/localization readability guard. `AscensionV2MilestoneGuardTests.cs` keeps shared helpers plus milestone package/docs checks, and `AscensionV2MilestoneGuardTests.Rootblight.cs` keeps Rootblight milestone guards. | Build passed with 0 warnings / 0 errors; focused `AscensionV2MilestoneGuardTests` plus `RuntimeMonkeyStabilityGuardTests` passed 52 / 0 / 2 / 54; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| Ancient UI PNG size helper | `AncientUiReadinessGuardTests` now uses shared `ReadPngDimensions`; the local `ReadPngSize` duplicate was removed. | Same infrastructure guard. |
| Lotha source slicing helper | `LothaPolishGuardTests` now uses shared `SliceBetween`; the local `SourceSlice` duplicate was removed. | Same infrastructure guard. |
| ReleaseEvidence gate helper split | `ReleaseEvidenceGateTests.EvidenceHelpers.cs` owns release-evidence discovery helpers, PowerShell runner setup, checklist builders, and tiny-PNG creation, leaving `ReleaseEvidenceGateTests.cs` focused on test flows and assertions. | Focused `ReleaseEvidenceGateTests` validation and default tests. |
| ReleaseEvidence manual handoff test split | `ReleaseEvidenceGateTests.ManualHandoff.cs` owns prepare-current-manual-test-handoff generator coverage, pending-evidence scaffold contracts, stale-loader preservation, and stale-loader archival tests, leaving `ReleaseEvidenceGateTests.cs` focused on package path derivation and stale package hash rejection. | Focused `ReleaseEvidenceGateTests` validation and default tests. |
| ReleaseEvidence manual evidence-template split | `ReleaseEvidenceGateTests.ManualEvidenceTemplates.cs` owns Ancient UI prepare-plan helper coverage, co-op evidence-template coverage, Vakuu fight evidence-template coverage, and the default release-ready-claim guard. | Static split checks only during the shared validation pause; focused `ReleaseEvidenceGateTests` validation remains pending. |
| ReleaseEvidence no-launch template split | `ReleaseEvidenceGateTests.NoLaunchTemplates.cs` owns no-launch collector/verifier contracts and versioned package-hash assertions, leaving the main file focused on package path derivation and stale package-hash rejection. | Build passed with 0 warnings / 0 errors; focused `ReleaseEvidenceGateTests` plus `RuntimeMonkeyStabilityGuardTests` passed 53 / 0 / 0 / 53; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| DocumentationCompactness release-journal split | `DocumentationCompactnessGuardTests.ReleaseJournal.cs` owns private beta handoff, private beta release audit, dev-environment, and current release-doc release-journal compactness guards. | Focused `DocumentationCompactnessGuardTests` passed after trimming `PROJECT_STATE.md` back under the first-read compactness limit. |
| DocumentationCompactness issue-doc split | `DocumentationCompactnessGuardTests.IssueDocs.cs` owns source-fixed/live-pending retest rows, current issue detail typo checks, Sere Talon/Tanx Claws queue readability, and current Ancient manual-doc Tanx Claws design guards. | Build passed with 0 warnings / 0 errors; focused runtime/governance/compactness guards passed 69 / 0 / 0 / 69; no publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| EngineeringGovernance feature-bootstrap guard split | `EngineeringGovernanceGuardTests.FeatureBootstrap.cs` now owns feature module bootstrap, bootstrap-record, StS1 event default-off, Vakuu hook/default-hidden, and unified environment override guards. `EngineeringGovernanceGuardTests.cs` keeps diagnostics, repository hygiene, release dashboard/planning docs, worktree batch script coverage, and `RunPowerShell` / `GitLsFiles`. Method bodies, assertions, string literals, and order are unchanged. | Build passed with 0 warnings / 0 errors; focused RuntimeMonkey, EngineeringGovernance, DocumentationCompactness, and TestInfrastructure guards passed 103 / 0 / 0 / 103; current-doc claims 1003 / 0; static-file hygiene 12 / 0; format, diff-check, patch inventory, and batch classification passed; no publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| EngineeringGovernance feature-module metadata split | `EngineeringGovernanceGuardTests.FeatureModuleMetadata.cs` owns feature module display-name and category literal guards. | Focused `EngineeringGovernanceGuardTests` passed. |
| EngineeringGovernance architecture-canary split | `EngineeringGovernanceGuardTests.ArchitectureCanary.cs` owns reward-pipeline diagnostics, multiplayer policy registration, death-protection canary, Ascension reward diagnostics, and co-op evidence metadata guards. | Build passed with 0 warnings / 0 errors; focused runtime/governance/compactness guards passed 69 / 0 / 0 / 69; no publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| AncientPlayerFacingPolish docs split | `AncientPlayerFacingPolishGuardTests.Docs.cs` owns the Temptation docs wording, Urda support-doc authority, active Ancient docs readability/Seed Bank scope, art-finality wording, and Urda/Morvi/Lotha power-status docs guards. `AncientPlayerFacingPolishGuardTests.cs` keeps the remaining player-facing text/localization/rich-text guards and private helpers. | Build passed with 0 warnings / 0 errors; focused `AncientPlayerFacingPolishGuardTests` passed 20 / 0 / 0 / 20; format, touched-file diff-check, and batch-classifier checks passed; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientPlayerFacingPolish rich-text split | `AncientPlayerFacingPolishGuardTests.RichText.cs` owns option-relic marker wording, custom Ancient concept rich-text, and custom card canonical-keyword wording guards. `AncientPlayerFacingPolishGuardTests.cs` keeps markdown backtick, interaction/source-backed text, Ascension text, Forge Token text, and private helper owners. | Build passed with 0 warnings / 0 errors; focused `AncientPlayerFacingPolishGuardTests` passed 20 / 0 / 0 / 20 after fixing a split inline-code marker in `docs/testing/runtime-monkey-stability.md`; format, touched-file diff-check, and batch-classifier checks passed; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientPlayerFacingPolish interaction-text split | `AncientPlayerFacingPolishGuardTests.InteractionText.cs` owns option-hover preview coverage, Seed Bank no-Trial-Plant wording/source guards, and Vakuu fight risk/reward text/source guards. `AncientPlayerFacingPolishGuardTests.cs` keeps markdown backtick, Ascension text, Forge Token text, and private helper owners. | Build passed with 0 warnings / 0 errors; focused `AncientPlayerFacingPolishGuardTests` passed 20 / 0 / 0 / 20; format, touched-file diff-check, and batch-classifier checks passed; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| AncientPlayerFacingPolish Ascension-text split | `AncientPlayerFacingPolishGuardTests.AscensionText.cs` owns Ascension Rootblight/Banner/Firemark/Boss-term wording and Forge Token temporary-wording guards. `AncientPlayerFacingPolishGuardTests.cs` keeps the active-doc inline-code guard and shared helper owners. | Build passed with 0 warnings / 0 errors; focused `AncientPlayerFacingPolishGuardTests` passed 20 / 0 / 0 / 20; current-doc claims passed 962 / 0; format, diff-check, and batch-classifier checks passed; no publish, package, runtime, gameplay, QA, or handoff validation was run. |
| RuntimeMonkey StS1 runtime evidence guard split | `RuntimeMonkeyStabilityGuardTests.Sts1RuntimeEvidence.cs` now owns the StS1 runtime evidence packet/current-slice and enabled-mode copied-log audit recomputation guards. `RuntimeMonkeyStabilityGuardTests.cs` keeps runner, packet, analyzer, AutoSlay, local source-workspace, CI, and batch-classifier guards plus shared helpers. Method bodies, assertions, string literals, and helper calls are unchanged. | Build passed with 0 warnings / 0 errors; focused RuntimeMonkey, EngineeringGovernance, DocumentationCompactness, and TestInfrastructure guards passed 103 / 0 / 0 / 103; current-doc claims 1003 / 0; static-file hygiene 12 / 0; format, diff-check, patch inventory, and batch classification passed; no publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| RuntimeMonkey AutoSlay source-provenance and timing/probe hardening | `check-local-godot-source-workspace.ps1` records installed-game PCK path/hash, recovered-source version/commit/branch/main assembly hash identity, and GDRE opening-file origin. `check-spire-plus-autoslay-packet.ps1` requires `main-menu` plus `runtime` probe phases and parseable ordered run timestamps for future game-native AutoSlay packets. | Parser checks passed for touched scripts; build passed with 0 warnings / 0 errors; focused `RuntimeMonkeyStabilityGuardTests` passed 44 / 0 / 0 / 44; current-doc claims passed 1009 / 0; no publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| Runtime monkey stale-process fail-closed hardening | `run-spire-plus-monkey-stability.ps1` records pre-existing `SlayTheSpire2` processes relative to each iteration's minimum start time, `check-spire-plus-runtime-monkey-packet.ps1` requires clean stale-process telemetry, and `analyze-spire-plus-runtime-failure.ps1` routes stale/unbound runtime evidence to `RuntimeHarness`. | Parser checks passed; build passed with 0 warnings / 0 errors; focused `RuntimeMonkeyStabilityGuardTests` passed 25 / 0 / 0 / 25; no publish, package, live runtime smoke, gameplay, QA, or handoff validation was run. |
| Shared-readable log file helper | `ReleaseSafetyExpandedGuardTests` now uses `ReadSharedText` from `TestRepo.cs` instead of a local `ReadAllTextShared` helper. | Same infrastructure guard and test README. |
| Repository path existence assertions | `TestRepo.cs` now exposes shared file/directory/path-exclusion assertion helpers; `TestInfrastructureGuardTests`, `AncientUiReadinessGuardTests`, `MorviV22GuardTests`, `ReleaseCoverageGuardTests`, `ReleaseSafetyExpandedGuardTests`, and `ReleaseArtifactTests` use them for cleanup/resource boundary checks. | Same infrastructure guard and test README. |
| Release artifact assembly resolver | `ReleaseArtifactTests` now uses one class-local `CreateAssemblyResolver` helper for installed game/BaseLib/mod API checks instead of repeating the same `AssemblyResolve` search loop in each test. | Targeted `ReleaseArtifactTests` validation. |
| Release artifact no-op assertion | Removed a tautological self-hash assertion from `ReleaseArtifactTests` and kept the meaningful source-copy hash parity check. | Targeted `ReleaseArtifactTests` validation. |
| Release artifact localization key helper | `ReleaseArtifactTests` now reuses shared `JsonStringMap` for localization key parity instead of carrying a local `JsonKeys` JSON parser; `TestInfrastructureGuardTests` now prevents `JsonKeys` from being reintroduced as a copied helper. | Targeted `ReleaseArtifactTests` and infrastructure validation. |
| Ascension combat tracker state split | `AscensionCombatTracker.cs` keeps shared/root combat state, while `AscensionCombatTracker.Firemarks.cs`, `AscensionCombatTracker.Banners.cs`, and `AscensionCombatTracker.BossSeals.cs` keep feature-owned transient combat state. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, and default build/test validation. |
| RootDeck combat lifecycle split | `RootDeckService.Lifecycle.cs` keeps run/start Rootblight seeding and explicit Rootblight I additions, while `RootDeckService.CombatLifecycle.cs` owns combat-start Rootblight marking plus combat-end growth, split, and pending downgrade resolution. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, `SaveStateContractsGuardTests`, and default build/test validation. |
| Firemark targeting split | `AscensionCombatModifierService.Firemarks.cs` keeps Firemark combat event and lifecycle dispatch, while `AscensionCombatModifierService.Firemarks.Targeting.cs` owns host selection, overflow candidate ordering, and low-HP/damaged overflow target selection. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, and build/test validation. |
| Firemark turn-flow split | `AscensionCombatModifierService.Firemarks.TurnFlow.cs` owns Firemark round damage-tracking reset, side-turn start, player-turn start, and turn-end dispatch, leaving `CombatLifecycle.cs` focused on hook lifecycle routing and `Firemarks.cs` focused on combat start plus damage-received behavior. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, and build/test validation. |
| Ascension turn lifecycle dispatch split | `AscensionCombatModifierService.TurnLifecycle.cs` owns player-turn start, pre-player-turn-end, side-turn start, and side-turn-end dispatch, leaving `CombatLifecycle.cs` focused on combat start, shuffle, and combat-end dispatch. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, `BossDedicatedAbilityV41GuardTests`, and build/test validation. |
| RootBud combat-end split | `RootBudCombatHook.CombatEnd.cs` owns combat-end Rootblight resolution, Blight Sprout growth, planted-sprout reset, and tracker cleanup, leaving `RootBudCombatHook.Lifecycle.cs` focused on combat-start seeding and Rootblight repair. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, `SaveStateContractsGuardTests`, and build/test validation. |
| RootBud room-rule split | `RootBudCombatHook.RoomRules.cs` owns Blight Sprout room eligibility, per-room bud count, sprout-round selection, and existing-bud round normalization, leaving `RootBudCombatHook.Helpers.cs` focused on combat-state/card/evidence helpers. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, `SaveStateContractsGuardTests`, and build/test validation. |
| Banner targeting split | `AscensionCombatModifierService.Banners.cs` keeps Banner combat event and lifecycle dispatch, while `AscensionCombatModifierService.Banners.Targeting.cs` owns combat fallback resolution, multi-primary-enemy eligibility, and banner target selection. | `ActiveSourceManifestGuardTests`, `AscensionFeatureGuardTests`, and build/test validation. |
| Chosen Decree card-state split | `AscensionCombatModifierService.BossSeals.ChosenDecree.cs` keeps Queen/Torch Head settlement and per-round cap behavior, while `AscensionCombatModifierService.BossSeals.ChosenDecree.Cards.cs` owns Bound-card marking, play tracking, visible-card hydration, and marker cleanup. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, and default build/test validation. |
| Residual Sample source-file rename | `AscensionCombatModifierService.BossSeals.ResidualSample.cs` owns the Test Subject / Residual Sample phase-carryover behavior previously kept under the stale `PhaseCarryover` file name. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, and build/test validation. |
| Residual Sample notice split | `AscensionCombatModifierService.BossSeals.ResidualSample.Notice.cs` owns Experimental Record notice construction, sample display names, reason text keys, and `TalkCmd.Play(...)` routing, leaving `AscensionCombatModifierService.BossSeals.ResidualSample.cs` focused on phase tracking, sample choice/application, and card-play tracking. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, and build/test validation. |
| Ascension localization bridge split | `AscensionLocalizationTablePatches.cs` keeps only Harmony localization entry points, while `AscensionLocalizationBridge.cs` owns table identity reflection, cache, JSON loading, language fallback, and merge behavior. | `AscensionFeatureGuardTests`, `ActiveSourceManifestGuardTests`, patch inventory, and package artifact validation. |
| Aeonglass Hourglass service split | `AscensionCombatModifierService.BossSeals.AeonglassHourglass.cs` now keeps Time Sand / Wither settlement; `AscensionCombatModifierService.BossSeals.AeonglassHourglass.State.cs` owns visible-power hydration and move tracking; `AscensionCombatModifierService.BossSeals.AeonglassHourglass.LaserEcho.cs` owns Eye Lasers preview/counter behavior. | `AscensionV2MilestoneGuardTests`, `AscensionFeatureGuardTests`, `ActiveSourceManifestGuardTests`, and build/package validation. |
| Aeonglass Time Sand energy split | `AscensionCombatModifierService.BossSeals.AeonglassHourglass.Energy.cs` owns energy-spend Time Sand clearing, visible power amount reduction, Laser Echo cleanup when Time Sand clears, and enemy intent refresh, leaving `AscensionCombatModifierService.BossSeals.AeonglassHourglass.cs` focused on Ebb creation, Time Sand settlement, and pending Wither application. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, `AscensionFeatureGuardTests`, `AscensionV2MilestoneGuardTests`, and build/test validation. |
| Ascension card-event dispatch split | `AscensionCombatModifierService.CardEvents.cs` owns card-play, energy-spend, and card-entered-hand dispatch, leaving `AscensionCombatModifierService.CombatEvents.cs` focused on HP, damage, power-amount, and death dispatch. `RootBudCombatHook.CardFlow.cs` remains the hook-forwarding owner. | `ActiveSourceManifestGuardTests`, `BossDedicatedAbilityV41GuardTests`, `AscensionFeatureGuardTests`, `AscensionV2MilestoneGuardTests`, and build/test validation. |
| Preview transform RNG patch split | `TransformPredictionRngContext.cs` owns only per-player RNG snapshots, while event RNG source patches, Niche RNG source patches, and selection-lifetime clearing live in focused Preview files. | `PreviewToolsGuardTests`, patch inventory, and active source manifest validation. |
| Boss dedicated ability power split | `BossSealPower.cs` owns the shared runtime base class, visibility marker text now lives in focused marker files, and runtime behavior powers live by boss/mechanic in dedicated files such as `MartyrOathPowers.cs`, `MarginalNotePowers.cs`, `ChosenDecreePowers.cs`, `AeonglassHourglassRuntimePowers.cs`, and `TestSubjectSamplePowers.cs`. | `AscensionV2MilestoneGuardTests.SplitBossSealPowerFilesKeepBehaviorAndReadableLocalization`, A19/A20 guards, and active source manifest validation. |
| Boss seal marker power split | Removed the final `BossSealMarkerPowers.cs` aggregate; `BossSealMarkerPower.cs` owns the shared zero-display marker base, while each visible A19/A20 Boss dedicated ability marker owns a focused `BossSealMarkerPowers.*.cs` file. | `AscensionV2MilestoneGuardTests.SplitBossSealPowerFilesKeepBehaviorAndReadableLocalization` and active source manifest validation. |
| Active project-state doc length | Superseded per-pass validation/package history was archived from `PROJECT_STATE.md` into `docs/archive/project-state-history-20260516.md`; the active first-read state file now focuses on current status and pending gates. | `TestInfrastructureGuardTests.ProjectStateStaysCurrentAndHistoricalPassLogIsArchived`, release/source/doc guards, and default validation. |
| Duplicate goal guard alias | Removed the deleted duplicate `docs/implement.md` from the active docs surface and kept `docs/goal.md` as the single compact guard. | `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` |
| Goal guard prompt-drift cleanup | Archived the long corrupted architecture/refactor analysis from `docs/goal.md` to `docs/archive/feature-inputs/goal-architecture-refactor-mojibake-intake-20260526.md` and restored `docs/goal.md` to a compact live-proof guard. | `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` and `GoalCompletionGuardTests.GoalGuardRulesAreBackedByCurrentSourceAndArchives` |
| Stray implement prompt cleanup | Moved the regenerated `docs/implement.md` A19/A20 Boss dedicated-ability design intake into `docs/archive/feature-inputs/implement-a19-a20-boss-ability-mojibake-intake-20260526.md` instead of keeping a second active root goal/spec file. | `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` and `GoalCompletionGuardTests.GoalGuardRulesAreBackedByCurrentSourceAndArchives` |
| Stray RitsuLib migration prompt cleanup | Moved the regenerated `docs/implement.md` RitsuLib migration intake into `docs/archive/feature-inputs/ritsulib-migration-mojibake-intake-20260526.md`; the archive is traceability input only and does not change the active BaseLib/current-dependency decision. | `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` and default validation. |
| Generated C# UID sidecars and VSTest output | Removed tracked `EZMicroBalanceCode/**/*.cs.uid` metadata and expanded `.gitignore` to keep regenerated source/test C# UID files plus `tests/**/TestResults/` out of the active deliverable. | `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` |
| Website mechanism codex entries | `website/content-data.js` now derives mechanism-codex cards from `mechanicGlossary` through `mechanicCodexItem`, and `website/app.js` uses glossary relations in the inspector instead of duplicate hand-written mechanism summaries. | `WebsiteContentGuardTests.WebsiteHardcodedGameplaySummariesStayCurrent` |
| Website package metadata sync | Website download URLs, package size, and package SHA now match the current package evidence instead of stale beta package metadata. | `WebsiteContentGuardTests.WebsitePackageMetadataMatchesCurrentPackageHash` and `WebsiteContentGuardTests.WebsiteHardcodedGameplaySummariesStayCurrent` |
| Website public-info draft review | Promoted the used QoL banner styling for the existing `website/app.js` banner, repaired the accidental omission of the Soul Fysh / Soul Tide Boss dedicated ability card, and guarded both surfaces. | `WebsiteContentGuardTests.WebsiteHardcodedGameplaySummariesStayCurrent` |
| FeatureRegistry bootstrap split | `MainFile.cs` no longer owns every feature initializer directly; `EZMicroBalanceCode/Core/Features/` owns module order, bootstrap gate logging, and default initialization while keeping current gameplay gates inside each feature. | `EngineeringGovernanceGuardTests.MainFileUsesFeatureRegistryForModuleBootstrap` |
| Multiplayer policy gate split | `MultiplayerFeaturePolicy.cs` now keeps network-mode/run-state authority, while `MultiplayerFeaturePolicy.CoopGates.cs` owns fail-closed unverified co-op gameplay/combat gates, override environment variables, and the shared once-only gate logger. | `CoopCombatSafetyGuardTests`, `MultiplayerPolicyGuardTests`, `PreviewToolsGuardTests`, and active source manifest validation. |
| Generated sidecar prune command | `scripts/prune-generated-sidecars.ps1` gives future agents a narrow cleanup path for regenerated source/test `.cs.uid` and website `.import` sidecars. It checks each file remains under the intended root and avoids broad `git clean -fdX`. | `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` |
| GitHub workflow run checker | `scripts/check-github-workflow-runs.ps1` queries GitHub Actions without requiring `gh`, so `GOV-CI-FIRST-RUN` can be closed only after a completed successful `Full Local Validation` run is found. | `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` |
| Preview transform queue split | `TransformPreviewPatch.cs` owns Harmony hook flow plus prediction preparation/display, while `TransformPreviewPredictionQueue.cs` owns per-preview queue state, clearing, and dequeue behavior. | `PreviewToolsGuardTests`, active source manifest validation. |
| Preview transform generated-slot policy split | `PreviewTransformPolicy` now owns the vanilla-aligned decision that only transform slots without an explicit replacement receive queued predictions, while `TransformPreviewPatch.cs` keeps prediction orchestration and `TransformPredictionService.cs` keeps replacement-model calculation. | Build 0 warnings / 0 errors; focused `PreviewToolsGuardTests` passed 11 / 0 / 1 / 12. No publish, package, runtime smoke, gameplay, QA, or handoff validation was run. |
| Development environment runtime history | `docs/dev-environment.md` now keeps only current environment, package hashes, compact runtime evidence, and pending manual checks; old smoke-attempt detail lives in `docs/archive/implementation-records/dev-environment-runtime-smoke-history-20260526.md`. | `DocumentationCompactnessGuardTests.DevEnvironmentAvoidsReleaseJournalLines` |
| Chinese intro path normalization | Preserved the active Chinese intro as `docs/intro.zh.md` and removed the non-ASCII `docs/娴犲绮?md` path that appeared quoted/octal-escaped in Git output. | `TestInfrastructureGuardTests.WebsiteSurfaceIsPromotedAndGeneratedForumOutputStaysIgnored` |
| Root forum QA note archive | Moved root `web_issue.md` into `docs/archive/implementation-records/forum-public-integration-qa-20260526.md`; current forum follow-up stays in `website/web_issue.md` and `docs/features/forum/`. | `TestInfrastructureGuardTests.WebsiteSurfaceIsPromotedAndGeneratedForumOutputStaysIgnored` |
| Website localization QA archive | Moved `website/localization_qa.md` into `docs/archive/implementation-records/website-localization-qa-20260522.md` so historical beta.39 QA notes no longer ship in the public website source. | `TestInfrastructureGuardTests.WebsiteSurfaceIsPromotedAndGeneratedForumOutputStaysIgnored` and `BossDedicatedAbilityV41GuardTests.WebsiteA19A20ContentUsesV41DedicatedAbilityTerminology` |
| Forum dependency cache prune | Deleted ignored `forum/node_modules/` after confirming it is dependency cache covered by `forum/package-lock.json`; restore with `npm ci` under `forum/` before forum build/test work. | `.gitignore`, `forum/package-lock.json`, `forum/.gitignore` |
| Stale publish package prune command | `scripts/prune-stale-publish-packages.ps1` now removes only old ignored `publish\SpirePlus-v0.1.0-private-beta.N` folders and ZIPs after confirming the manifest-current ZIP exists. | `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` |
| Commit-boundaries stale snapshot cleanup | Replaced the stale 2026-05-24 dirty worktree snapshot in `docs/month-plan/commit-boundaries.md` with a current clean baseline and a count-free batch ownership map. | `EngineeringGovernanceGuardTests.ReleasePlanningDocsExistAndKeepLiveRowsOpen` |
| Release-evidence package artifact paths and hash defaults | Collector and verifier scripts now derive the versioned package zip, ZIP SHA256, and package artifact paths from `EZMicroBalance.json` plus the current package file through `scripts/spire-plus-package-evidence.ps1` instead of repeating the same `publish\SpirePlus-v...` paths and current hash defaults in every script. | `ReleaseEvidenceGateTests.ReleaseEvidenceScriptsDeriveVersionedPackageArtifactPathsFromManifest` |
| Current-package test expectations | `TestRepo.cs` exposes shared current-package helpers, and guard tests now use them instead of repeating the current beta package path and ZIP hash in package, platform, release-hash, handoff, and verifier assertions. | `ReleaseEvidenceGateTests`, `AncientBehaviorGuardTests`, `CrossPlatformTestingGuardTests`, `ReleaseHashGuardTests`, and `TestInfrastructureGuardTests` |
| Release/readiness test state de-duplication | `ReleasePackageArtifactGuardTests`, `ReleaseArtifactParityGuardTests`, `AscensionV2MilestoneGuardTests`, and `WebsiteContentGuardTests` now use shared current-package helpers and `TestRepo.CurrentFacingDocs` instead of rebuilding the package ZIP path or copying active documentation lists. | `TestInfrastructureGuardTests.GuardTestsUseSharedCurrentReleaseState` and test README. |
| Worktree batch classifier for shared test infrastructure | `scripts/report-worktree-batches.ps1` now classifies `tests/EZMicroBalance.Tests/TestRepo.cs` with other scripts/CI/validation tests so shared test-helper edits do not become unclassified cleanup noise. | `scripts/report-worktree-batches.ps1 -FailOnUnclassified` |
| Worktree batch classifier for source research docs | `scripts/report-worktree-batches.ps1` now classifies `docs/source-research/` with governance and architecture docs so save/RNG boundary notes stay reviewable without becoming unclassified cleanup noise. | `scripts/report-worktree-batches.ps1 -FailOnUnclassified` |
| Worktree batch classifier for legacy planning archives | `scripts/report-worktree-batches.ps1` now classifies `docs/archive/legacy-planning/` with stray docs and audits so preserved goal snapshots stay reviewable without becoming unclassified cleanup noise. | `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` |
| Root local clutter regression guard | `TestInfrastructureGuardTests` now checks that root `art_pipeline/` and `asset/` do not return, local archive folders exist, ignore rules are present, and docs map the archives. | Same infrastructure guard. |
| Owner-sensitive local material guard | `TestInfrastructureGuardTests` now checks that `source code/`, `publish/`, `.tools/`, local props, and binary/package outputs remain ignored and decision-tracked instead of silently committed or deleted. | Same infrastructure guard. |

## Completion Audit Against Cleanup Goal

Goal restated as concrete deliverables:

1. Remove or refactor clearly redundant code without changing external behavior.
2. Move stale or superseded documentation out of the active reading path.
3. Preserve uncertain material in an archive before any deletion.
4. Delete only material that has an archive copy or is proven generated/local-only and safe to ignore.
5. Keep active `Spire Plus` source/resource/test/docs changes reviewable.
6. Record a completion audit based on actual files and command output.

| Requirement | Evidence inspected | Status |
| --- | --- | --- |
| Redundant code/test infrastructure reduced | `tests/EZMicroBalance.Tests/TestRepo.cs` exists; `TestInfrastructureGuardTests` guards duplicate repository/path/source/JSON/PCK/hash/export/Png helpers; `rg` over guarded duplicate helper names returns no active duplicate definitions outside `TestRepo.cs`. | Complete for the audited helper classes. |
| Feature-local historical prompt/spec archives moved out of active feature folders | `Get-ChildItem docs\features -Recurse -Directory -Filter archive` reports `0`; archived copies live under `docs/archive/feature-inputs/`. | Complete for the known feature-local archive folders. |
| Superseded setup spec moved out of active docs | `docs/SETUP_SPEC.md` no longer exists; `docs/archive/superseded/setup-spec-original-scaffold.md` exists; docs point current setup to `README.md`, `docs/README.md`, and `docs/dev-environment.md`. | Complete. |
| Superseded project-state validation history moved out of first-read docs | `PROJECT_STATE.md` now links to `docs/archive/project-state-history-20260516.md`; archive indexes map the snapshot. | Complete. |
| Duplicate active goal guard alias removed | `docs/implement.md` is absent; `docs/goal.md` remains as the canonical compact guard; `docs/review.md` records the historical duplicate. | Complete. |
| Generated C# UID sidecars and VSTest output removed from tracked source | `EZMicroBalanceCode/**/*.cs.uid` are absent, source/test `.cs.uid` and `tests/**/TestResults/` are ignored, and compile/package inputs use `.cs` files and `EZMicroBalance/**` resources, not these sidecars. | Complete. |
| Uncertain/generated root clutter preserved before deletion | Root `art_pipeline/`, `asset/`, and website preview snapshots were moved to ignored `.tools/archive/...` locations and documented in `docs/doc-inventory.md` / `docs/PROJECT_MAP.md`. | Complete for the handled local clutter. |
| Current website source is visible, generated forum/editor output is ignored | `website/` and `.github/workflows/spire-plus-site.yml` are tracked current surfaces; `.gitignore` ignores only `/website/forum/` and `/website/**/*.import`. | Complete: source changes remain reviewable, local Vite build output and Godot sidecar metadata stay out of the release-candidate diff. The 2026-05-26 website prune left 140 tracked website files, 0 website `.import` files, no `website/forum/` directory, and an empty `git clean -ndX website` dry run. |
| Local browser screenshots/logs kept out of release-candidate diff | `.gitignore` ignores `/output/playwright/`; existing tracked evidence stays visible, while new local browser output is ignored. | Complete unless the owner deliberately promotes a screenshot as evidence. The 2026-05-26 targeted prune left `git clean -ndX output/playwright` empty and preserved the tracked evidence count at 71. |
| Broad ignored-file deletion is unsafe | `git clean -ndX` | Current dry run shows ignored build/editor caches plus `.tools/`, `publish/`, `source code/`, and `Directory.Build.props`; do not run broad `git clean -fdX` for this workspace. | Complete as a cleanup boundary. |
| Active release/source/test changes remain reviewable | Pre-commit `git status --short` exposed active `EZMicroBalanceCode/`, tests, docs, and website changes; post-commit `git status --short --branch` is clean against `origin/main`. | Complete. |
| Owner-sensitive material not deleted blindly | `source code/`, `publish/`, retained `.tools` evidence/tool folders, and the promoted website are documented below. The old root `legacy/` folder was archived, duplicate root mod surfaces were removed after the owner rule change, the earlier website draft snapshot was preserved, and generated `.tools` clutter was pruned by exact path. | Complete: remaining retained areas have current evidence or hard-rule justification. |
| Validation covers cleanup edits | Build, default tests, format verification, and diff whitespace checks pass as recorded below. | Complete for current cleanup edits. |

Conclusion: the cleanup/refactor pass is validated. Remaining large ignored/local areas are retained intentionally because they are current package output, source evidence, runtime evidence, art provenance, or local tool installations rather than confirmed redundant clutter.

## Prompt-To-Artifact Checklist

This maps the cleanup prompt to concrete artifacts and current evidence. Passing tests are not treated as sufficient by themselves; each row records what was actually inspected.

| Prompt requirement / deliverable | Concrete artifact or command | Current evidence | Status |
| --- | --- | --- | --- |
| Clean `D:\Game\FOTN\dev-the-spire`, not another workspace | `pwd` | Output path is `D:\Game\FOTN\dev-the-spire`. | Complete. |
| Remove or refactor redundant implementation/test code | `tests/EZMicroBalance.Tests/TestRepo.cs`, `TestInfrastructureGuardTests`, `AncientFeatureGate`, `AscensionCombatModifierService` | Shared test helpers now centralize repository paths, source reads, JSON/string helpers, PCK/ZIP/hash helpers, PNG dimension reads, export parsing, and active resource predicates; Ancient feature gates share env parsing; Ascension combat metadata refresh duplication was reduced. | Complete for the audited duplication classes; additional feature-specific helpers may remain by design. |
| Do not change external behavior while cleaning | Default build/test/format/diff validation | `dotnet build`, `dotnet test --no-build`, `dotnet format --verify-no-changes`, and `git diff --check` pass for the cleanup edits recorded below. | Complete for static validation; live gameplay behavior is outside this cleanup pass. |
| Move stale or superseded docs out of the active reading path | `docs/archive/superseded/setup-spec-original-scaffold.md`; `docs/archive/feature-inputs/`; `docs/archive/project-state-history-20260516.md`; `docs/archive/implementation-records/2026-05-15-current-package-verification-note.md` | `Test-Path docs\SETUP_SPEC.md` is `False`; archived setup spec exists; `Get-ChildItem docs\features -Recurse -Directory -Filter archive` reports `0`; current docs point to central archives; active `PROJECT_STATE.md` keeps current status while the historical pass log is archived; active `docs/issues.md` keeps the package snapshot compact and links the archived long note. | Complete for identified stale setup, project-state history, issue-note history, and feature prompt/spec docs. |
| Delete confirmed duplicate active docs | `docs/implement.md`, `docs/goal.md`, `DocumentationCompactnessGuardTests` | `docs/implement.md` was an exact goal-guard duplicate; the preserved canonical copy is `docs/goal.md`, and the guard test asserts the alias remains absent. | Complete. |
| Delete generated metadata that is not an active deliverable | `EZMicroBalanceCode/**/*.cs.uid`, `.gitignore`, `EngineeringGovernanceGuardTests`, `export_presets.cfg`, `EZMicroBalance.csproj` | `*.cs.uid` is excluded from export, `.csproj` compiles `EZMicroBalanceCode/**/*.cs`, not `.cs.uid`, and the governance guard now requires source/test C# UID sidecars to stay absent. | Complete. |
| Archive uncertain material before deletion | `.tools/archive/local-art-and-calibration-20260515/`, `.tools/archive/local-root-clutter-20260515/`, `.tools/archive/local-website-preview-20260516/`, `docs/archive/**` | Local clutter and website draft snapshots are documented under archive locations; historical prompt/spec files were moved to tracked `docs/archive/feature-inputs/`; original setup spec was moved to tracked `docs/archive/superseded/`. | Complete for handled uncertain material. |
| Delete only material proven safe or already archived | Deleted active-path copies of feature-local archive files, `docs/SETUP_SPEC.md`, the ignored website/Pages workflow draft, transient Edge browser profile/cache folders, stale redirected publish-output folders, an old install backup, and generated Playwright/Godot cache folders; archived root `legacy/` into `docs/archive/legacy-planning/legacy-project-files/` | Deleted tracked docs and moved legacy project metadata have archive copies; website and Pages workflow were ignored local drafts and now exist only in the cleanup snapshot; Edge profile/cache directories were unreferenced local browser state, while `.tools/art-generation/` and active resources preserve the actual art evidence; redirected publish folders, install backup, and generated cache folders were local outputs separate from the current `publish/` package and runtime evidence. | Complete for performed deletions; no scaffold/source/current-package/runtime-evidence directories were deleted. |
| Keep active `Spire Plus` source/resource/test/docs reviewable | `git status --short`, beta.85 package-evidence baseline | Active beta.85 package-evidence changes are tied to the current validated HEAD instead of a hard-coded historical commit; use `git log -1 --oneline --decorate` after commit/push for the exact baseline. Local/generated drafts remain ignored. | Complete. |
| Prevent regression into feature-local archive clutter | `TestInfrastructureGuardTests.FeaturePromptArchivesStayInCentralArchive` | Guard checks that historical prompt/spec archives live under `docs/archive/feature-inputs` and current docs/tests do not reference old feature-local archive paths. | Complete. |
| Keep current website source reviewable while ignoring generated forum/editor output | `.gitignore`, `website/`, `.github/workflows/spire-plus-site.yml`, `.tools/archive/local-website-preview-20260516/` | Root `website/` and the Pages workflow are tracked current surfaces; older snapshot remains archived; generated `website/forum/` output and `website/**/*.import` metadata are ignored and have been pruned from the local worktree. | Complete unless owner demotes the website again. |
| Preserve project hard rules and active manifest boundary | `README.md`, `AGENTS.md`, `docs/architecture-ez-micro-balance.md`, release guard tests | Active mod id remains `EZMicroBalance`; duplicate root mod surfaces are blocked from returning by repository hygiene guards. | Complete. |
| Confirm whether every uncertain area is useless before permanent deletion | Owner Deletion Decision Checklist below | `legacy/`, duplicate root mod surfaces, website draft, transient browser profiles, stale redirected publish outputs, old install backup, and generated cache folders were archived/deleted after exact-path checks. `source code`, current `publish`, and retained `.tools` evidence/tool folders are kept with explicit justification. | Complete. |
| Final completion audit based on actual state | This document plus validation commands | Audit records inspected files, commands, status, and retained boundaries. | Complete as an audit artifact. |
| Avoid broad ignored-file deletion | `git clean -ndX` dry run | Current dry run includes `.godot/`, `.tools/`, `Directory.Build.props`, `publish/`, `source code/`, and test `bin/obj`; cleanup must stay targeted. | Complete. |

## Owner Deletion Decision Checklist

Use this checklist before any permanent deletion. Do not delete a row unless the owner explicitly chooses `Delete after verification` and the verification column passes. If the owner is unavailable, use the default recommendation.

| Path / Area | Current evidence | Default recommendation | Owner choices | Verification before delete/promote |
| --- | --- | --- | --- | --- |
| Removed duplicate root mod surfaces | `EzDailyContent*`, `EZFuturePeek*`, `tests/EZFuturePeek.Tests/`, and `scripts/export-future-peek.ps1` are absent from the active root. | Keep removed. | Restore only through a deliberate new-mod decision with a stable manifest id. | Repository hygiene guards must keep these paths absent; any future new mod needs its own architecture decision before files return. |
| Former root `legacy/` | Top-level historical migration area contained only `.gdignore`, a README, and the old `EzDailyContent.csproj.legacy.xml`. | Archived. | Keep archived unless project history policy changes. | Moved to `docs/archive/legacy-planning/legacy-project-files/`; update docs/tests that referenced the old root path; run `rg -n "legacy/EzDailyContent|../legacy/README|`legacy/`" README.md PROJECT_STATE.md docs tests scripts --glob "!docs/archive/**"` and validation. |
| `source code/` | `docs/test-ready-development-goal.md` names `source code/src/Core/**` as primary source evidence; opt-in local-source tests read files from this path. | Keep as ignored local reference. | Keep; refresh from current game version; delete after replacing evidence path. | Keep normal tests independent of this ignored tree through `LocalSourceFactAttribute`; before deleting it, provide an alternate local game-source path or fixture strategy, update source-evidence docs, run `rg -n "source code" tests docs README.md PROJECT_STATE.md`, run normal tests without the tree, then run `SPIREPLUS_RUN_LOCAL_SOURCE_GUARDS=1 dotnet test EZMicroBalance.sln --no-build` against the replacement source. |
| `publish/` | Package refresh scripts and opt-in release-artifact tests use `publish/SpirePlus-v0.1.0-private-beta.91.zip` and hash parity evidence. | Keep generated output ignored. | Keep; clean and rebuild; delete after package rebuild. | Run `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, and `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build`; confirm package hashes/docs are refreshed before deleting stale artifacts. |
| `.tools/` | Remaining subfolders are runtime evidence, generated art provenance, local archives, downloaded/decompiled game tooling, Godot, or ILSpy. | Keep remaining subfolders; never delete wholesale. | Future targeted prune only for newly proven generated clutter. | For a proposed subfolder, run `rg -n "<subfolder-name>|.tools/" README.md PROJECT_STATE.md docs tests scripts`; keep current release evidence, art provenance, owner-accepted image sources, and source-inspection tools; after pruning, run relevant script/test guards plus `git diff --check`. |
| `website/` and `.github/workflows/spire-plus-site.yml` | Current tracked public site source and GitHub Pages workflow. `.tools/archive/local-website-preview-20260516/` remains as historical pre-promotion snapshot only. | Keep tracked and reviewable. | Keep current; demote back to archive only with owner approval. | If demoting later, archive the current tree, restore ignore rules for root `website/` and the workflow, update `docs/PROJECT_MAP.md`, `docs/doc-inventory.md`, and run website/source guard tests. |

Owner decision statuses:

| Path / Area | Status |
| --- | --- |
| Removed duplicate root mod surfaces | Removed after the 2026-05-20 owner rule change; keep absent from active root. |
| Former root `legacy/` | Archived to `docs/archive/legacy-planning/legacy-project-files/`; top-level folder removed. |
| `source code/` | Default keep because current tests/docs require it. |
| `publish/` | Retained current beta.91 package/staging/cover-source output; stale beta.0-beta.80 ZIPs and expanded folders are removed by the guarded prune after confirming current-package hash/path parity. Future prune should happen only after a new package rebuild/hash refresh. |
| `.tools/` | Unreferenced Edge browser profile/cache folders, stale redirected publish outputs, an old install backup, and generated Playwright/Godot cache folders were deleted; remaining `.tools/` subfolders are retained as current evidence, art provenance, local archives, or local tool installations. Wholesale deletion is not recommended. |
| `output/playwright/` | Default ignored for new browser screenshots/logs/HARs/PID files. Existing tracked evidence remains visible until reviewed in its own batch. On 2026-05-26, ignored-only cleanup deleted 126 stale local files and reduced the directory from 197 files / 192,037,582 bytes to the 71 tracked files / 186,704,922 bytes. |
| `website/` and `.github/workflows/spire-plus-site.yml` | Promoted and tracked as current public site source and Pages workflow. Generated `website/forum/` output and `website/**/*.import` metadata remain ignored; stale local copies were deleted by targeted ignored-only cleanup on 2026-05-26. |

## Latest Validation For Cleanup Changes

- Runtime monkey stale-process fail-closed hardening: `run-spire-plus-monkey-stability.ps1` now treats pre-existing `SlayTheSpire2` processes as shared-log contamination, packet validation requires clean stale-process telemetry, and the analyzer routes stale or unbound runtime evidence to `RuntimeHarness`. Parser checks passed; build passed with 0 warnings / 0 errors; focused `RuntimeMonkeyStabilityGuardTests` passed 25 / 0 / 0 / 25. No publish, package refresh, live runtime smoke, gameplay, QA, or handoff validation was run.
- AncientBehavior Sere Talon route/helper split: moved `VakuuSereTalonAndTanxClawsStayOnSeparateSourceRoutes`, `CoreVakuuSereTalonAndTanxClawsStayOnSeparateRoutes`, and the `ReadSereTalonVisualSource()` helper into `AncientBehaviorGuardTests.SereTalonRoutes.cs`, and updated the test README partial-file row. Original route split validation passed build 0 warnings / 0 errors and focused `AncientBehaviorGuardTests` 22 / 0 / 2 / 24; the later helper ownership follow-up used static-only checks during the shared validation pause. No publish, package, runtime, gameplay, QA, or handoff validation was run.
- `dotnet build EZMicroBalance.sln --no-restore`: pass, 0 warnings, 0 errors.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~TestInfrastructureGuardTests`: pass, 8 passed.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~AncientUiReadinessGuardTests`: pass, 11 passed.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~MorviV22GuardTests`: pass, 5 passed.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~ReleaseCoverageGuardTests`: pass, 23 passed / 3 skipped.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~ReleaseSafetyExpandedGuardTests`: pass, 12 passed / 6 skipped.
- `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~ReleaseSafetyExpandedGuardTests`: pass, 18 passed / 0 skipped.
- `dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~ReleaseArtifactTests`: pass, 9 passed / 7 skipped.
- `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build --filter FullyQualifiedName~ReleaseArtifactTests`: pass, 16 passed / 0 skipped.
- Combined current-doc guard filter (`ReleaseCoverageGuardTests`, `ReleaseSafetyExpandedGuardTests`, `AncientStateMirrorGuardTests`, `AncientUiReadinessGuardTests`, `AncientArtAssetHygieneGuardTests`, `TestInfrastructureGuardTests`): pass, 68 passed / 9 skipped.
- `PROJECT_STATE.md` cleanup check: active file is 81 lines; `docs/archive/project-state-history-20260516.md` exists; stale `Latest ...` per-pass history scan reports no active `PROJECT_STATE.md` hits.
- `docs/issues.md` package-note cleanup guard filter (`ReleaseCoverageGuardTests`, `AncientStateMirrorGuardTests`, `AncientUiReadinessGuardTests`, `AncientArtAssetHygieneGuardTests`, `VakuuLothaSaveRiskGuardTests`, `VakuuTemptationGuardTests`, `TestInfrastructureGuardTests`): pass, 70 passed / 3 skipped.
- Website promotion check: `git ls-files website .github\workflows\spire-plus-site.yml` lists the tracked current source and workflow; `.tools\archive\local-website-preview-20260516\website\index.html` remains as historical snapshot; `git check-ignore -v website\forum\index.html website\assets\relics\relic.png.import` confirms generated forum output and Godot sidecar metadata remain ignored.
- Edge browser profile/cache deletion check: `Test-Path .tools\edge-chatgpt-profile-clone-20260515`, `Test-Path .tools\edge-chatgpt-profile-clone2-20260515`, `Test-Path .tools\edge-chatgpt-profile`, and `Test-Path .tools\edge-chatgpt-pw-profile` return `False`; `rg -n "edge-chatgpt-profile|edge-chatgpt-pw-profile" README.md PROJECT_STATE.md docs tests scripts --glob "!docs/archive/**"` returns only cleanup-audit references after deletion; `.tools` size scan no longer lists those browser profile/cache directories.
- Redirected publish output deletion check: `Test-Path .tools\publish-redirect`, `Test-Path .tools\publish-redirect2`, and `Test-Path .tools\publish-mods` return `False`; references are limited to historical work-log entries that explicitly describe redirected local publish output and do not replace installed/current package evidence.
- Install backup deletion check: `Test-Path .tools\install-backups` returns `False`; `rg -n "install-backups" README.md PROJECT_STATE.md docs tests scripts --glob "!docs/archive/**"` returns no references.
- Generated local tool/cache deletion check: `Test-Path .tools\browser-automation`, `Test-Path .tools\godot-appdata`, `Test-Path .tools\godot-localappdata`, and `Test-Path .tools\godot-user` return `False`; the only remaining active-doc reference among these names is a historical work-log line for `.tools/godot-user/` paired with now-deleted redirected publish output.
- Legacy root archive check: `Test-Path legacy` returns `False`; `docs/archive/legacy-planning/legacy-project-files/EzDailyContent/EzDailyContent.csproj.legacy.xml` and `docs/archive/legacy-planning/legacy-project-files/README.md` exist; current docs point to the archive path.
- Final ignored-file dry run: `git clean -ndX` now lists only `.godot/`, `.tools/`, `Directory.Build.props`, `publish/`, `source code/`, and test `bin/obj`; previously deleted website, Pages workflow, browser profile/cache, redirected publish, install-backup, and root `legacy/` clutter do not appear.
- Final targeted cleanup/path guard filter (`TestInfrastructureGuardTests`, `ReleaseArtifactTests`, `ReleaseCoverageGuardTests`, `AncientBehaviorGuardTests`): pass, 59 passed / 11 skipped.
- Post-cleanup guard update: `dotnet test EZMicroBalance.sln --filter "FullyQualifiedName~TestInfrastructureGuardTests|FullyQualifiedName~ReleaseCoverageGuardTests|FullyQualifiedName~AncientStateMirrorGuardTests|FullyQualifiedName~AncientUiReadinessGuardTests|FullyQualifiedName~AncientArtAssetHygieneGuardTests|FullyQualifiedName~VakuuLothaSaveRiskGuardTests|FullyQualifiedName~VakuuTemptationGuardTests"` passed, 70 passed / 3 skipped.
- Duplicate goal guard alias cleanup: `docs/implement.md` is deleted because its content duplicated `docs/goal.md`; `DocumentationCompactnessGuardTests.ActiveGoalGuardStaysCompactAndReadable` now asserts the duplicate alias stays absent.
- Generated output cleanup: tracked `EZMicroBalanceCode/**/*.cs.uid` files are deleted, `.gitignore` ignores regenerated source/test C# UID sidecars and VSTest `TestResults/` folders, and `EngineeringGovernanceGuardTests.RepositoryHygieneWorkflowAndTemplatesExist` now checks the policy.
- Current cleanup validation: `dotnet test EZMicroBalance.sln --filter "FullyQualifiedName~EngineeringGovernanceGuardTests|FullyQualifiedName~DocumentationCompactnessGuardTests"` passed, 25 passed / 0 skipped.
- Website mechanism-codex cleanup validation: `node --check website\app.js`, `node --check website\content-data.js`, `dotnet test EZMicroBalance.sln --filter FullyQualifiedName~WebsiteContentGuardTests`, and targeted `git diff --check` passed.
- Governance stale-state cleanup validation: `dotnet test EZMicroBalance.sln --filter FullyQualifiedName~DocumentationCompactnessGuardTests` passed, 20 passed / 0 skipped; current docs no longer reference beta.26 as the current pending loader target and no longer say root `EzDailyContent.json` still exists. Current beta.86 evidence should be read from the latest validated HEAD.
- Release-evidence package-path/hash helper validation: current helper smoke derives `publish\SpirePlus-v0.1.0-private-beta.87.zip` and ZIP SHA256 `97C65F040F7269738778368878E7946D1563F622D0D8959644C54DBC6806A0B1`; focused release-evidence, release-safety, governance, and documentation guard tests passed in the beta.86 validation lane, and the beta.84 helper run is historical package-path context.
- Current-package test helper validation: focused package/platform/release-hash/test-infrastructure/mod-info guard tests passed, 10 passed; `rg` found no hardcoded current package path or old ZIP hash left under `tests\EZMicroBalance.Tests`.
- Current baseline-drift cleanup validation: removed active-doc/test dependence on old hard-coded package-guard baselines, kept current package evidence derived from the manifest/versioned artifacts, and reran `dotnet build EZMicroBalance.sln`, focused current-state guards, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, and `scripts/report-worktree-batches.ps1 -FailOnUnclassified`.
- Current batch classification: `scripts/report-worktree-batches.ps1 -FailOnUnclassified` passed with 35 dirty entries and 0 unclassified paths during the historical beta.84 pre-commit package sync. In the current 2026-06-17 continuation, the classifier also reports 0 unclassified paths, but the worktree is dirty at HEAD `c5d1893e`; rerun it before final handoff after resolving intended edits and concurrent `docs/goals/*` changes.
- Current website/document hygiene validation: deleted ignored `tests\EZMicroBalance.Tests\*.cs.uid` sidecars, synced `website/content-data.js` package size to the current beta.86 package size, kept referenced website PNG assets in the public website surface, and focused website/current-doc/governance guard tests passed. Earlier validation also repaired current Sere Talon/Tanx Claws Chinese queue text.
- Development-environment runtime-history cleanup validation: `dotnet build EZMicroBalance.sln`, focused documentation/release/package guard tests, `dotnet test EZMicroBalance.sln --no-build`, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified`, and `scripts/prune-generated-sidecars.ps1 -DryRun` passed. The old smoke-attempt journal is archived, not deleted.
- Website localization QA archive / beta.53 sync validation: archived `website/localization_qa.md` to `docs/archive/implementation-records/website-localization-qa-20260522.md`, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.53.zip`, synced current package hashes and website download metadata, and reran `dotnet build EZMicroBalance.sln`, targeted website/document/governance/release-evidence guards, default `dotnet test EZMicroBalance.sln --no-build`, opt-in release artifact tests, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified`, and generated-sidecar pruning.
- Stale publish package prune validation: added `scripts/prune-stale-publish-packages.ps1`, verified old beta.34-beta.66 package outputs are absent, pruned stale beta.80 publish outputs after the beta.84 package refresh, and confirmed the script dry-run reported 0 stale publish package outputs. Current beta.86 package, `publish\package-staging`, cover-source files, `.tools`, `source code`, and local props were retained.
- Goal guard prompt-drift cleanup validation: archived `docs/goal.md` architecture/refactor prompt drift to `docs/archive/feature-inputs/goal-architecture-refactor-mojibake-intake-20260526.md`, restored the active goal guard to a compact closure-rule file, and reran `dotnet build EZMicroBalance.sln`, focused documentation/goal/infrastructure guards, and `dotnet test EZMicroBalance.sln --no-build` with 291 passed / 20 skipped.
- Stray implement prompt cleanup validation: moved the regenerated `docs/implement.md` A19/A20 Boss dedicated-ability intake to `docs/archive/feature-inputs/implement-a19-a20-boss-ability-mojibake-intake-20260526.md`, verified `docs/implement.md` is absent, and reran `dotnet build EZMicroBalance.sln`, focused documentation/goal/infrastructure guards, and `dotnet test EZMicroBalance.sln --no-build` with 291 passed / 20 skipped.
- Boss seal marker split / RitsuLib intake archive validation: removed the `BossSealMarkerPowers.cs` aggregate, split the marker base and each visible A19/A20 marker into focused source files, verified the split files recombine to the original source text, archived regenerated `docs/implement.md` RitsuLib migration intake to `docs/archive/feature-inputs/ritsulib-migration-mojibake-intake-20260526.md`, verified `docs/implement.md` is absent, and reran `dotnet build EZMicroBalance.sln`, focused source/documentation/goal guards with 56 passed / 1 skipped, default `dotnet test EZMicroBalance.sln --no-build` with 292 passed / 20 skipped, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 24 dirty entries / 0 unclassified, and `scripts/prune-generated-sidecars.ps1 -DryRun` with 0 generated sidecars.
- Release/readiness test state de-duplication validation: removed duplicate current-facing doc arrays and hardcoded current package ZIP path construction from release, Ascension, and website guard tests; added `TestInfrastructureGuardTests.GuardTestsUseSharedCurrentReleaseState`; reran `dotnet build EZMicroBalance.sln`, default `dotnet test EZMicroBalance.sln --no-build` with 293 passed / 20 skipped, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 0 unclassified entries, and `scripts/prune-generated-sidecars.ps1 -DryRun` with 0 generated sidecars. The external uncommitted `website/content-data.js` and `website/styles.css` entries are outside this test-infrastructure cleanup scope and are not included in the intended commit.
- FeatureRegistry bootstrap split validation: added `EZMicroBalanceCode/Core/Features/` for ordered module bootstrap, updated initializer guards to read `SpirePlusFeatureRegistry`, kept `docs/implement.md` absent, and reran `dotnet build EZMicroBalance.sln --no-restore` plus focused source/governance guard tests. This was a source-architecture pass only; no game was opened.
- Website public-info draft validation: promoted the existing QoL banner styling after confirming the banner is rendered by `website/app.js`, restored the Soul Fysh / Soul Tide website entry, and reran `node --check website\content-data.js`, `node --check website\app.js`, `dotnet build EZMicroBalance.sln`, default `dotnet test EZMicroBalance.sln --no-build` with 294 passed / 20 skipped, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 0 unclassified entries, and `scripts/prune-generated-sidecars.ps1 -DryRun` with 0 generated sidecars. A Playwright CLI smoke check opened the local website over a temporary HTTP server and confirmed the QoL banner exists, uses CSS grid, the preview image loads, Soul Tide/Soul Fysh text is present, and the browser console has 0 errors.
- Multiplayer policy gate split validation: split `MultiplayerFeaturePolicy.CoopGates.cs` out of the network-mode policy surface, consolidated gameplay/combat gate once-only logging behind one helper, and reran `dotnet build EZMicroBalance.sln`, focused multiplayer/preview/source-manifest guards with 19 passed, default `dotnet test EZMicroBalance.sln --no-build` with 294 passed / 20 skipped, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 0 unclassified entries, and `scripts/prune-generated-sidecars.ps1 -DryRun` with 0 generated sidecars.
- Urda state codec split validation: extracted `UrdaStateCodec.cs`, updated state mirror/save-state/source-manifest guards for the new codec boundary, and reran `dotnet build EZMicroBalance.sln`, focused Urda/save-state/source-manifest guards with 15 passed, default `dotnet test EZMicroBalance.sln --no-build` with 295 passed / 20 skipped, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 0 unclassified entries, and `scripts/prune-generated-sidecars.ps1 -DryRun` with 0 generated sidecars.
- Urda Seed Bank extraction commit split validation: split selected-card deck add, failed-card preservation, storage clearing, and extraction logs from `UrdaBlessingService.SeedBankExtraction.cs` into `UrdaBlessingService.SeedBankExtractionCommit.cs`; added the GitHub workflow-run checker script for `GOV-CI-FIRST-RUN`; rebuilt and packaged the then-current private-beta package, synced package hashes plus website size/hash metadata, pruned stale publish outputs, and reran build, publish, package refresh, focused Urda/save-state/source-manifest/governance/documentation guards, website syntax checks, default tests, opt-in release artifact tests, format, diff-check, installed-package checking, batch classification, generated-sidecar pruning, and stale publish pruning.
- Transform Preview display split validation: split the `CycleThroughCards` display override, concrete predicted-card assignment, local UI-only evidence logging, and vanilla fail-open fallback from `TransformPreviewPatch.cs` into `TransformPreviewCyclePatch.Display.cs`; rebuilt and packaged the then-current private-beta package, synced package hashes plus website size/hash metadata, and reran build, publish, package refresh, focused preview/source-manifest/release-coverage guards, website syntax checks, default tests, opt-in artifact tests, format, diff-check, installed-package checking, batch classification, generated-sidecar pruning, and stale publish pruning.
- Lotha card-play dispatch / ReleaseEvidence helper split / beta.70 package sync validation: split `AfterCardPlayed` dispatch from `LothaBlessingService.CardRules.cs` into `LothaBlessingService.CardPlayDispatch.cs`, split release-evidence/checklist helpers from `ReleaseEvidenceGateTests.cs` into `ReleaseEvidenceGateTests.EvidenceHelpers.cs`, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.70.zip`, synced package hashes plus website size/hash metadata, and reran build, publish, package refresh, focused Lotha/source-manifest/documentation/release-evidence guards, website syntax checks, default tests, opt-in artifact tests, format, diff-check, installed-package checking, batch classification, generated-sidecar pruning, and stale publish pruning.
- Residual Sample source-file rename / beta.71 package sync validation: renamed the stale `AscensionCombatModifierService.BossSeals.PhaseCarryover.cs` file to `AscensionCombatModifierService.BossSeals.ResidualSample.cs`, updated source-manifest and boss dedicated ability guards, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.71.zip`, synced package hashes plus website size/hash metadata, and kept method names, hook callers, saved state, and player-visible behavior unchanged.
- Firemark turn-flow split / beta.72 package sync validation: split Firemark round damage-tracking reset, side-turn start, player-turn start, and turn-end dispatch from the general combat lifecycle and Firemarks files into `AscensionCombatModifierService.Firemarks.TurnFlow.cs`; updated the active source manifest, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.72.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, and player-visible behavior unchanged.
- Ascension turn lifecycle dispatch split / beta.73 package sync validation: split player-turn start, pre-player-turn-end, side-turn start, and side-turn-end dispatch from `AscensionCombatModifierService.CombatLifecycle.cs` into `AscensionCombatModifierService.TurnLifecycle.cs`; updated source/guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.73.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, and player-visible behavior unchanged.
- Ascension card-event dispatch split / beta.78 package sync validation: split card-play, energy-spend, and card-entered-hand dispatch from `AscensionCombatModifierService.CombatEvents.cs` into `AscensionCombatModifierService.CardEvents.cs`; updated source/boss dedicated ability guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.78.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, service method names, and player-visible behavior unchanged.
- AncientBehavior localization/docs guard split / beta.80 package sync validation: split v4.3/v4.2 doc-current checks, localization parity, Simplified Chinese text guards, current Ancient docs checks, release-checklist guard coverage, and manual verification matrix rows from `AncientBehaviorGuardTests.cs` into `AncientBehaviorGuardTests.LocalizationDocs.cs`; rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.80.zip`, synced package hashes plus website size/hash metadata, and kept test names, assertions, package evidence semantics, and player-visible behavior unchanged.
- AscensionFeature rootblight guard split / beta.84 package sync validation: split Root starter, RootBud seeding/gating, and Root family card/localization guard coverage from `AscensionFeatureGuardTests.cs` into `AscensionFeatureGuardTests.Rootblight.cs`; rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.84.zip`, synced package hashes plus website size/hash metadata, and kept test names, assertions, package evidence semantics, and player-visible behavior unchanged.
- Aeonglass Time Sand energy split / beta.77 package sync validation: split energy-spend Time Sand clearing, visible power amount reduction, Laser Echo cleanup, and enemy intent refresh from `AscensionCombatModifierService.BossSeals.AeonglassHourglass.cs` into `AscensionCombatModifierService.BossSeals.AeonglassHourglass.Energy.cs`; updated source/boss dedicated ability guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.77.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, move ids, and player-visible behavior unchanged.
- Residual Sample notice split / beta.76 package sync validation: split Experimental Record notice construction, sample display names, reason text keys, and `TalkCmd.Play(...)` routing from `AscensionCombatModifierService.BossSeals.ResidualSample.cs` into `AscensionCombatModifierService.BossSeals.ResidualSample.Notice.cs`; updated source/boss dedicated ability guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.76.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, and player-visible behavior unchanged.
- RootBud room-rule split / beta.75 package sync validation: split Blight Sprout room eligibility, per-room bud count, sprout round selection, and existing-bud round normalization from `RootBudCombatHook.Helpers.cs` into `RootBudCombatHook.RoomRules.cs`; updated source/save-state guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.75.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, and player-visible behavior unchanged.
- RootBud combat-end split / beta.74 package sync validation: split combat-end Rootblight resolution, Blight Sprout growth, planted-sprout reset, and tracker cleanup from `RootBudCombatHook.Lifecycle.cs` into `RootBudCombatHook.CombatEnd.cs`; updated source/save-state guard coverage, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.74.zip`, synced package hashes plus website size/hash metadata, and kept hook callers, saved state, and player-visible behavior unchanged.
- Lotha play-count split / beta.69 package sync validation: split `ModifyCardPlayCount`, extra-play counts, and the Lotha extra-play branch selection from `LothaBlessingService.CardRules.cs` into `LothaBlessingService.CardPlayCount.cs`; rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.69.zip`, synced package hashes plus website size/hash metadata, and reran build, publish, package refresh, focused Lotha/source-manifest/documentation/release-coverage guards, website syntax checks, default tests, opt-in artifact tests, format, diff-check, installed-package checking, batch classification, generated-sidecar pruning, and stale publish pruning.
- Lotha card eligibility split / beta.62 package sync validation: split shared Lotha card eligibility predicates, auto-play modifier consumption, and extra-play evidence logging from `LothaBlessingService.CardRules.cs` into `LothaBlessingService.CardEligibility.cs`; rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.62.zip`, synced package hashes plus website size/hash metadata, and reran build, publish, isolated publish, package refresh, focused Lotha/source-manifest guards, website syntax checks, default tests, opt-in artifact tests, format, diff-check, installed-package checking, batch classification, generated-sidecar pruning, and stale publish pruning.
- Lotha Death Reprieve turn split / beta.59 package sync validation: split the actual reprieve turn start, Power upkeep, cost-free check, and turn-end resolution from `LothaBlessingService.DeathReprieve.cs` into `LothaBlessingService.DeathReprieveTurn.cs`; ignored VSTest `TestResults/` output after the opt-in suite exposed it as transient worktree noise; rebuilt and packaged the beta.59 package, synced package hashes plus website size/hash metadata, pruned stale beta.58 publish outputs, and reran the normal source/package validation chain in that pass.
- Ascension localization bridge split validation: split table identity/cache/JSON/language-fallback/merge logic into `AscensionLocalizationBridge.cs`, kept `AscensionLocalizationTablePatches.cs` focused on Harmony entry points, regenerated `docs/patch-inventory.md`, and reran build, focused Ascension/source-manifest guards, publish/package, and package hash sync for beta.53.
- Beta.53 package/version sync validation: updated the manifest to `v0.1.0-private-beta.53`, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.53.zip`, synced package hashes and website size/hash metadata, and reran `dotnet build EZMicroBalance.sln`, `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, `node --check website\content-data.js`, `node --check website\app.js`, default `dotnet test EZMicroBalance.sln --no-build` with 296 passed / 20 skipped, `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build` with 316 passed, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, `scripts/report-worktree-batches.ps1 -FailOnUnclassified` with 0 unclassified entries, and `scripts/prune-generated-sidecars.ps1` followed by a dry-run with 0 generated sidecars.
- Aeonglass Hourglass split validation: split state hydration/move tracking and Laser Echo preview/counter behavior out of the Time Sand / Wither settlement file, updated the active source manifest, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.53.zip`, synced package hashes plus website size/hash metadata, and reran `dotnet build EZMicroBalance.sln`, `dotnet publish EZMicroBalance.sln`, isolated publish, `scripts\package-spire-plus.ps1`, `node --check website\content-data.js`, `node --check website\app.js`, default `dotnet test EZMicroBalance.sln --no-build` with 296 passed / 20 skipped, `SPIREPLUS_RUN_RELEASE_ARTIFACT_TESTS=1 dotnet test EZMicroBalance.sln --no-build` with 316 passed, `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`, `git diff --check`, installed-package checking, `scripts\report-worktree-batches.ps1 -FailOnUnclassified` with 37 dirty entries / 0 unclassified entries, `scripts\prune-generated-sidecars.ps1` plus dry-run, and `scripts\prune-stale-publish-packages.ps1` plus dry-run.
- A20 Courtyard recovery split validation: split post-Boss-1 A20 courtyard healing from `AscensionCombatModifierService.BossSeals.PhaseCarryover.cs` into `AscensionCombatModifierService.BossSeals.A20Courtyard.cs`, updated the active source manifest and Ascension README, and reran the normal source/package validation chain in that pass.
- Chosen Decree card-state split / Rootblight combat-start repair validation: split Bound-card marking, play tracking, visible-card hydration, and marker cleanup from `AscensionCombatModifierService.BossSeals.ChosenDecree.cs` into `AscensionCombatModifierService.BossSeals.ChosenDecree.Cards.cs`; added a combat-start Rootblight repair retry before `MarkCombatStartRootblight`; and reran the normal source/package validation chain in that pass.
- Banner targeting split validation: split Banner combat fallback resolution, multi-primary-enemy eligibility, and banner target selection from `AscensionCombatModifierService.Banners.cs` into `AscensionCombatModifierService.Banners.Targeting.cs`, updated source/guard coverage, and reran the normal source/package validation chain in that pass.
- Firemark targeting split validation: split Firemark host selection, overflow candidate ordering, and low-HP/damaged overflow target selection from `AscensionCombatModifierService.Firemarks.cs` into `AscensionCombatModifierService.Firemarks.Targeting.cs`, updated source/guard coverage, and reran the normal source/package validation chain in that pass.
- RootDeck combat lifecycle split validation: split combat-start Rootblight marking and combat-end Rootblight resolution from `RootDeckService.Lifecycle.cs` into `RootDeckService.CombatLifecycle.cs`, updated source/guard coverage, and reran the normal source/package validation chain in that pass.
- Urda option relic split and beta.50 package sync validation: split the mixed Urda option relic aggregate into base event-relic policy, static art relics, Root Eyes counter display, Seed Bank stored-card hover/counter display, and relic-click routing files; updated the manifest to `v0.1.0-private-beta.50`, rebuilt and packaged `publish\SpirePlus-v0.1.0-private-beta.50.zip`, synced package hashes and website size/hash metadata, pruned stale beta.47 publish outputs, and reran `dotnet build EZMicroBalance.sln`, `dotnet publish EZMicroBalance.sln`, `scripts/package-spire-plus.ps1`, focused Urda/player-text/source-manifest guards, default tests, opt-in release artifact tests, website syntax checks, format, diff-check, batch classification, stale package pruning, and generated-sidecar pruning.
- Urda option relic split validation: split the mixed Urda option relic aggregate into base policy, static art relics, Root Eyes counter display, Seed Bank stored-card hover/counter display, and relic-click routing files; regenerated patch inventory and reran focused Urda/player-text/source-manifest guards before the normal package validation chain.
- Lotha hook ownership split validation: removed the mixed `LothaHooks.cs` aggregate, split run/reward/death-prevention dispatch into `LothaRunHook.cs`, split combat-only card/turn/cost/Power dispatch into `LothaCombatHook.cs`, and updated source-manifest, high-risk hook, co-op safety, and state-mirror guards.
- Preview transform RNG patch split validation: split event RNG source patches, Niche RNG source patches, and transformation-selection lifetime cleanup out of `TransformPredictionRngContext.cs`, and updated preview source guards plus the active source manifest.
- Previous Lotha/Preview package sync validation: rebuilt and packaged the then-current package after the Lotha hook and Preview transform RNG ownership splits, synced package hashes and website size/hash metadata, regenerated patch inventory, pruned stale beta.48 publish outputs, and reran the normal package validation chain.
- Urda Seed Bank extraction-state split validation: split Seed Bank relic-click reentrancy state and the public click entry out of `UrdaBlessingService.SeedBankExtraction.cs`, rebuilt and packaged the then-current beta package, synced package hashes and website size/hash metadata, and reran focused Urda/source-manifest/save-state guards plus the normal package validation chain.
- `git clean -ndX`: dry-run only; confirms broad ignored-file deletion would hit owner-sensitive/local-evidence paths and must not be used as cleanup.
- Final `dotnet test EZMicroBalance.sln --no-build`: pass, 296 passed / 20 skipped.
- Final `dotnet format EZMicroBalance.sln --verify-no-changes --no-restore`: pass.
- Final `node --check website\content-data.js`: pass.
- Final `git diff --check`: pass.
- Targeted publish-output prune: deleted ignored old versioned package ZIPs and expanded folders; retained the then-current package output, `publish\package-staging`, and release cover-source files. Current package SHA evidence lives in `docs/issues.md`.
- Local `.tools` prune correction: an over-broad ignored-output clean removed local `.tools` tool/evidence folders. Godot 4.5.1 mono was restored from the official `4.5.1-stable` mono Windows archive, `.tools\publish-game-root` was rebuilt with `dotnet publish EZMicroBalance.sln -p:ModsPath=.tools\publish-game-root\mods\`, and `scripts\package-spire-plus.ps1 -GameRoot .tools\publish-game-root` recreated the isolated package root. Historical raw runtime-evidence folders were not fabricated after cleanup; docs now treat those rows as historical records and require fresh recapture before any release-ready claim.
- Targeted Playwright-output prune: `git clean -fdX output/playwright` deleted 126 ignored local browser/Supabase/forum screenshots, `.import` sidecars, and Vite logs; all 71 tracked website evidence files remain, total directory size is now 186,704,922 bytes, and `git clean -ndX output/playwright` returns no remaining ignored candidates.
- Targeted website-output prune: `git clean -fdX website` deleted the ignored `website/forum/` generated build output and 112 ignored website `.import` sidecars; `git ls-files website` still reports 140 tracked website source files, `Get-ChildItem website -Recurse -Filter *.import` reports 0, `Test-Path website\forum` is `False`, and `git clean -ndX website` returns no remaining ignored candidates.

Re-run validation after every subsequent cleanup batch.
