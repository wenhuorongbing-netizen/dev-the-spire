# Patch Boundaries

Patch inventory source: `docs/patch-inventory.md`, generated by `scripts/generate-patch-inventory.ps1`.

Current count: see `docs/patch-inventory.md` (source of truth). As of 2026-05-28: 141 total, 22 high-risk.

## Rules

- Harmony patches are entry points and should forward into named services.
- Patches touching run, room, save, lobby, multiplayer, or lifecycle are release-sensitive.
- Moving or adding a patch requires regenerating `docs/patch-inventory.md`.
- No patch may be used as proof that live UI, save/load, or co-op works.
- **Phase 2 adapter rule:** Every high-risk patch must be a thin adapter. The Harmony prefix/postfix forwards into a named service method; business logic lives in the service.
- **No same-PR move + behavior change** for any high-risk patch. Move first, verify build/tests pass, then change behavior in a separate PR.
- Forbidden behaviors (see table above) are enforced by source guard tests where possible.

## Phase 2 Patch Adapter Rule — Status: Started / adapter checklist drafted (2026-05-29)

Every high-risk patch group must be a **thin adapter**: the Harmony patch forwards into a named service; business logic, state mutation, and fallback logging live in the service, not the patch. A high-risk patch must **never** combine a file move and a behavior change in the same PR.

## High-Risk Owners

| Owner | Patch surface | Service seam | Forbidden behavior | Manual evidence row |
| --- | --- | --- | --- | --- |
| Vakuu child combat | `CombatRoom.ToSerializable`, `EventRoom.EnterInternal`, `CombatRoom.OfferRoomEndRewards` | `VakuuFightService` (current); future `VakuuFightFlow` | No direct `ParentEventId` mutation in patch; no reward bypass without logging; no screen state change without service delegation. | Start Vakuu fight, win, lose/die, save/load, restore prefinished no-reward state, confirm no black screen. |
| Urda Root Eyes | `RunManager.RollRoomTypeFor`, `RunManager.CreateRoom`, map click/hover UI | `UrdaBlessingService.RootSight*`; future `RootSightPreviewPolicy` | No RNG advancement outside committed room entry; no co-op mutation without gate; no room commit without preview validation. | Preview monster/event/unknown nodes, enter selected nodes, verify room matches preview, verify non-selected previews refund. |
| A20 dual boss | `RunManager.GenerateRooms`, `RunManager.ProceedFromTerminalRewardsScreen`, reward screen patches | A20 services/events; future `A20BossChainFlow` | No direct screen black-box state mutation; no boss chain without courtyard transition; no duplicate reward injection. | Run A20 through Boss 1 rewards, courtyard heal, Boss 2 reveal/entry, victory, save/load between stages, confirm no duplicate rewards. |
| Ascension selector | `StartRunLobby` private selection/update methods and `SyncAscensionChange` | `AscensionSelectionService`; future `LobbyAdapterGate` | No global progress/character stat patch in selection methods; no multiplayer downgrade without explicit env gate. | Single-player A11-A20 selection, host multiplayer selection, mismatch warning, host/client downgrade, env-gated vanilla A1-A10 fallback. |
| Ascension map | `ActModel.CreateMap`, map hover/icon patches | `AscensionMapService` | No map graph mutation outside service; no metadata assignment in patch body; no chokepoint removal without source evidence. | Natural route-click traversal, save/load on map, Firemark/Banner/Deep Branch/Root Eyes stacked hover, A17 branch completion. |
| Ascension combat | combat/card/power hooks | Banner/Firemark/boss combat services; future `BannerCombatPolicy`, `FiremarkWindowPolicy` | No direct power application in patch; no boss dedicated ability logic inline; no damage scaling without service delegation. | A19 dedicated boss ability proof, A20 Banner/Firemark effect timing, save/load mid-combat. |
| Multiplayer diagnostics | `JoinFlow`, `StartRunLobby`, `NGame`, `SaveManager`, `RunManager.EnterAct` | Diagnostics logging service | **Absolutely no state mutation**: no join/save/quit/act-entry changes; log-only. | Two-client join, start, save, quit, reconnect/failure logs, no unexpected disconnect beyond intended mismatch guard. |
| Reward sync | `RewardsSetSynchronizer`, reward button/screen patches | Reward service; future `RewardIdentityPreserver` | No client-local reward state; no reward identity change; no alternative reward bypass without vanilla authority. | Boss reward screen after A20 Boss 1, Crystal Sphere preview toggle, deterministic transform preview, save/reopen, co-op disposition. |
| Preview tools | `NCrystalSphereScreen`, `NTransformPreview`, transform command contexts | `PreviewTransformPolicy` | No real transform mutation; no reward reveal; no RNG state change outside forked preview RNG. | Crystal Sphere preview toggle, deterministic transform preview result, save/reopen, co-op disposition. |
| StS1Events | `EventRoom.EnterInternal`, `EventModel` lifecycle, `Sts1EventRegistrationService` | `Sts1EventsFeatureModule` / `Sts1EventRegistrationService`; future `Sts1EventPoolService` | No unconditional `RegisterAll` in default init path; no StS1 event registration unless feature gate explicitly enables it; no same-PR move + behavior change for event model files. | Default Off loads 0 StS1 events; CanaryOnly registers exactly 4 (Big Fish, Golden Idol, Lab, Divine Fountain); feature gate env var toggles mode; `Sts1EventFeatureGuardTests` pass. |

## High-Risk Manual Evidence Map

These rows keep `STRICT-AUDIT-PATCH-SURFACE` source-mapped while live proof is still pending. They are not release proof by themselves.

| Surface | High-risk patches | Required no-game evidence | Required manual evidence |
| --- | --- | --- | --- |
| Vakuu child combat | `CombatRoom.ToSerializable`, `EventRoom.EnterInternal`, `CombatRoom.OfferRoomEndRewards` | Source guard proves active Vakuu combat does not store `ParentEventId`, prefinished save writes parent id only after victory, no-reward restore resumes parent event, and invariant-culture custom state is used. | Start Vakuu fight, win, lose/die, save/load during active fight if allowed, restore prefinished no-reward state, and confirm no black screen. |
| Urda Root Eyes room commit | `RunManager.RollRoomTypeFor`, `RunManager.CreateRoom` | Source guard proves preview selection is separate from room commit and co-op mutation is gated. | Preview monster/event/unknown nodes, enter selected nodes, verify generated room matches preview, and verify non-selected queued previews refund. |
| A20 dual boss flow | `RunManager.GenerateRooms`, `RunManager.ProceedFromTerminalRewardsScreen`, reward-screen patches | Source guard proves Boss 1 uses A19 dedicated ability, Boss 2 uses Branded Form, courtyard transition is explicit, and boss card reward injection is bounded. | Run A20 through Act 3 Boss 1 rewards, courtyard heal, Boss 2 reveal/entry, victory, save/load between stages, and confirm no duplicate rewards. |
| Ascension lobby selection | `StartRunLobby.BeginRunLocally`, `BeginRunForAllPlayers`, `UpdateMaxMultiplayerAscension`, `UpdatePreferredAscension`, `SyncAscensionChange`, `SetSingleplayerAscensionAfterCharacterChanged` | Source guard proves no global progress/character stat patch and keeps public/multiplayer disable env gates. | Single-player A11-A20 selection, host multiplayer selection, mismatch warning, host/client downgrade behavior, and env-gated vanilla A1-A10 fallback. |
| Multiplayer diagnostics | `JoinFlow.HandleInitialGameInfoMessage`, `NGame.StartNewMultiplayerRun`, `SaveManager.SaveRun`, `RunManager.EnterAct`, `NGame.ReturnToMainMenu`, `NGame.Quit` | Source guard proves diagnostics log context only and do not mutate join/save/quit/act-entry state. | Two-client join, start, save, quit, reconnect/failure logs, and no unexpected disconnect beyond intended mismatch guard. |
| A11-A20 map generation | `ActModel.CreateMap`, `RunManager.GenerateRooms`, map hover/icon patches | Source guard proves A11 inserted columns keep non-chokepoint routes, A17 branch reconnects, and hover/icon composition preserves stacked markers. | Natural route-click traversal, save/load on map, Firemark/Banner/Deep Branch/Root Eyes stacked hover, and A17 branch completion. |
| Reward and preview UI | `NRewardsScreen._Ready`, `UpdateScreenState`, `NCrystalSphereScreen._Ready`, `NTransformPreview.Initialize` | Source guard proves reward identity is preserved, Crystal Sphere only changes mask/button UI, and transform preview uses forked RNG. | Boss reward screen after A20 Boss 1, Crystal Sphere preview toggle, deterministic transform preview result, save/reopen, and co-op disposition. |
| StS1Events registration | `SpirePlusFeatureRegistry`, `Sts1EventRegistrationService`, `Sts1EventsFeatureModule` | Source guard proves default Off registers 0 events, CanaryOnly registers exactly 4, env var toggles mode, and no unconditional `RegisterAll` in default init. | Set env var Off → 0 StS1 events registered; set CanaryOnly → exactly 4 shared events registered; verify guard tests pass; verify no event appears in default Spire Plus run. |

## Required Service Seams

| Seam | Purpose | First acceptance test |
| --- | --- | --- |
| `RootSightPreviewPolicy` | Choose, store, validate, and consume future map previews. | Preview does not consume live RNG until entry commit. |
| `BannerCombatPolicy` | Calculate Banner effects from act, enemy count, and player card count. | Shieldwall block lands on the useful side of turn timing. |
| `FiremarkWindowPolicy` | Track Molten Core, Forge Armor, Constant Heal, and Heat windows. | Breaking Forge Armor suppresses only the intended next armor. |
| `VakuuFightFlow` | Represent parent event, child combat, victory, failure, and fallback states. | Victory returns to a valid parent/fallback path in simulation. |
| `PreviewTransformPolicy` | Predict transform results from a forked RNG. | Preview leaves the source transformation RNG counter unchanged. |

## Review Checklist

- Patch has an owner in `docs/patch-inventory.md`.
- Patch has a source evidence file if it touches high-risk game APIs.
- Patch delegates to service/state code.
- Tests check the intended boundary.
- Runtime rows remain open until manual evidence exists.
- Every high-risk patch group above has a matching row in `docs/toreview.md` or `docs/issues.md` before user handoff.

## Core Invariants

- `RunState.Rng` and `PlayerRngSet` counters are advanced only by committed game actions.
- `RewardsSetSynchronizer` remains the reward click authority in multiplayer.
- `StartRunLobby.SyncAscensionChange` remains the multiplayer ascension synchronization boundary.
- `NClickableControl` intercepts are narrowly scoped to the intended screen/control type.
- `CombatRoom.ToSerializable` must not see an active non-prefinished child combat with `ParentEventId`.
