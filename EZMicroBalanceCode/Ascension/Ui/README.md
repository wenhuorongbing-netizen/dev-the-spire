# Ascension/Ui

Reserved for Ascension UI-related patches and helpers.

This directory will contain UI patches separated from the main `Patches/`
directory when the move-only refactor executes (PR3 scope).
