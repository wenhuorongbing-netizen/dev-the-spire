# EzDailyContentCode

This is legacy scaffold code retained for traceability. It is not the active private beta implementation and is not part of the current `EZMicroBalance.sln` build.

Use `EZMicroBalanceCode/` for active mod work. Do not change the legacy `EzDailyContent` manifest id in-place.
